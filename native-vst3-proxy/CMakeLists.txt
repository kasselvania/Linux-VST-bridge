if(NOT EXISTS "${AP2_RUST_LIBRARY}")
 message(FATAL_ERROR "AP2_RUST_LIBRARY must name the separately bound PIC GNU Rust static library")
endif()
smtg_add_vst3plugin(AGainOfflineBridge
 "${VST3_SDK_ROOT}/public.sdk/source/main/linuxmain.cpp"
 source/processor.cpp source/factory.cpp)
target_include_directories(AGainOfflineBridge PRIVATE include)
target_link_libraries(AGainOfflineBridge PRIVATE sdk "${AP2_RUST_LIBRARY}" pthread dl m)
target_compile_features(AGainOfflineBridge PRIVATE cxx_std_20)
target_compile_options(AGainOfflineBridge PRIVATE -Wall -Wextra -Wpedantic -Werror -Wno-error=stringop-truncation)
set_target_properties(AGainOfflineBridge PROPERTIES CXX_VISIBILITY_PRESET hidden VISIBILITY_INLINES_HIDDEN ON)
target_link_options(AGainOfflineBridge PRIVATE "-Wl,--exclude-libs,ALL" "-Wl,-z,defs")
smtg_add_vst3plugin(AGainQueuedBridge
 "${VST3_SDK_ROOT}/public.sdk/source/main/linuxmain.cpp"
 source/processor.cpp source/factory.cpp)
target_include_directories(AGainQueuedBridge PRIVATE include)
target_compile_definitions(AGainQueuedBridge PRIVATE AP3_PREVIEW=1)
find_package(X11 REQUIRED)
find_library(AP11_XCB xcb REQUIRED)
target_link_libraries(AGainQueuedBridge PRIVATE X11::X11 sdk "${AP2_RUST_LIBRARY}" pthread dl m)
target_compile_features(AGainQueuedBridge PRIVATE cxx_std_20)
target_compile_options(AGainQueuedBridge PRIVATE -Wall -Wextra -Wpedantic -Werror -Wno-error=stringop-truncation)
set_target_properties(AGainQueuedBridge PROPERTIES CXX_VISIBILITY_PRESET hidden VISIBILITY_INLINES_HIDDEN ON)
target_link_options(AGainQueuedBridge PRIVATE "-Wl,--exclude-libs,ALL" "-Wl,-z,defs")
add_executable(ap2-offline-host host/main.cpp
 "${VST3_SDK_ROOT}/public.sdk/source/vst/hosting/module_linux.cpp")
target_link_libraries(ap2-offline-host PRIVATE sdk_hosting pthread dl)
target_compile_features(ap2-offline-host PRIVATE cxx_std_20)
target_compile_options(ap2-offline-host PRIVATE -Wall -Wextra -Wpedantic -Werror)

add_executable(ap3-sustained-host host/sustained.cpp
 "${VST3_SDK_ROOT}/public.sdk/source/vst/hosting/module_linux.cpp")
target_link_libraries(ap3-sustained-host PRIVATE sdk_hosting pthread dl)
target_compile_features(ap3-sustained-host PRIVATE cxx_std_20)
target_compile_options(ap3-sustained-host PRIVATE -Wall -Wextra -Wpedantic -Werror)
add_library(ap3-callback-audit SHARED tests/callback_audit.cpp)
target_link_libraries(ap3-callback-audit PRIVATE dl)
target_compile_features(ap3-callback-audit PRIVATE cxx_std_20)
target_compile_options(ap3-callback-audit PRIVATE -Wall -Wextra -Wpedantic -Werror)

option(AP10_RESULTS_TESTS "Test callback result lifetime against the pinned SDK" OFF)
if(AP10_RESULTS_TESTS)
 add_executable(ap10-result-lifetime tests/result_lifetime/main.cpp source/processor.cpp)
 target_include_directories(ap10-result-lifetime PRIVATE include source tests/result_lifetime)
 target_compile_definitions(ap10-result-lifetime PRIVATE AP3_PREVIEW=1 AP8_PREVIEW=1)
 target_compile_features(ap10-result-lifetime PRIVATE cxx_std_20)
 target_compile_options(ap10-result-lifetime PRIVATE -Wall -Wextra -Wpedantic -Werror -Wno-error=stringop-truncation)
 target_link_libraries(ap10-result-lifetime PRIVATE sdk sdk_hosting "${AP2_RUST_LIBRARY}" pthread dl m)
 foreach(symbol ap9_open ap5_report_path ap10_setup ap4_activate ap4_deactivate ap3_transition if2_close if2_terminal_status if2_process ap10_take_results ap10_fail_results)
  target_link_options(ap10-result-lifetime PRIVATE "-Wl,--wrap=${symbol}")
 endforeach()
 # Preserve the original collector/deep-copy sink coverage alongside the
 # SDK shallow-copy full-callback regression.
 add_executable(ap10-results-tests "${PROJECT_SOURCE_DIR}/tools/ap10-tests/results.cpp")
 target_include_directories(ap10-results-tests PRIVATE "${PROJECT_SOURCE_DIR}" include)
 target_compile_features(ap10-results-tests PRIVATE cxx_std_20)
 target_compile_options(ap10-results-tests PRIVATE -Wall -Wextra -Wpedantic -Werror -UNDEBUG)
 target_link_libraries(ap10-results-tests PRIVATE sdk)
 enable_testing()
 add_test(NAME ap10-result-lifetime COMMAND "${CMAKE_COMMAND}" -E env
  "LD_PRELOAD=$<TARGET_FILE:ap3-callback-audit>" $<TARGET_FILE:ap10-result-lifetime>)
 add_test(NAME ap10-results-tests COMMAND ap10-results-tests)
 add_executable(ap18-auxiliary-input tests/auxiliary_input/main.cpp source/processor.cpp)
 target_include_directories(ap18-auxiliary-input PRIVATE include source tests/auxiliary_input)
 target_compile_definitions(ap18-auxiliary-input PRIVATE AP3_PREVIEW=1 AP8_PREVIEW=1)
 target_compile_features(ap18-auxiliary-input PRIVATE cxx_std_20)
 target_compile_options(ap18-auxiliary-input PRIVATE -Wall -Wextra -Wpedantic -Werror -Wno-error=stringop-truncation -UNDEBUG)
 target_link_libraries(ap18-auxiliary-input PRIVATE sdk sdk_hosting "${AP2_RUST_LIBRARY}" pthread dl m)
 foreach(symbol ap9_open ap5_report_path ap10_setup ap4_activate ap4_deactivate if2_close if2_terminal_status ap3_transition if2_process ap10_take_results)
  target_link_options(ap18-auxiliary-input PRIVATE "-Wl,--wrap=${symbol}")
 endforeach()
 add_test(NAME ap18-auxiliary-input COMMAND ap18-auxiliary-input)
 add_executable(ap11-native-tests "${PROJECT_SOURCE_DIR}/tools/ap11-tests/native.cpp" source/processor.cpp)
 target_include_directories(ap11-native-tests PRIVATE include source tests/result_lifetime)
 target_compile_definitions(ap11-native-tests PRIVATE AP3_PREVIEW=1 AP8_PREVIEW=1)
 target_compile_features(ap11-native-tests PRIVATE cxx_std_20)
 target_compile_options(ap11-native-tests PRIVATE -Wall -Wextra -Wpedantic -Werror -Wno-error=stringop-truncation)
 target_link_libraries(ap11-native-tests PRIVATE "${AP11_XCB}" X11::X11 sdk sdk_hosting "${AP2_RUST_LIBRARY}" pthread dl m)
 foreach(symbol ap9_open ap5_report_path ap10_setup ap4_activate ap4_deactivate ap3_transition if2_close if2_terminal_status ap10_notices if1_terminal ap11_gui_generation ap11_gui_command ap11_gui_take ap11_gui_capabilities ap11_gui_failure ap8_validate ap4_state if2_process ap10_take_results)
  target_link_options(ap11-native-tests PRIVATE "-Wl,--wrap=${symbol}")
 endforeach()
 add_test(NAME ap11-native-tests COMMAND ap11-native-tests)
 add_test(NAME if2-native-host-survival COMMAND ap11-native-tests --if2)
 set_tests_properties(if2-native-host-survival PROPERTIES TIMEOUT 30)
 add_executable(ap11-state-tests "${PROJECT_SOURCE_DIR}/tools/ap11-tests/state.cpp")
 target_include_directories(ap11-state-tests PRIVATE "${PROJECT_SOURCE_DIR}")
 target_compile_features(ap11-state-tests PRIVATE cxx_std_20)
 target_link_libraries(ap11-state-tests PRIVATE sdk sdk_hosting)
 add_test(NAME ap11-state-tests COMMAND ap11-state-tests)
 option(AP11_PANEL_TESTS "Test real X11 panel event routing on an isolated display" OFF)
 if(AP11_PANEL_TESTS)
  find_program(AP11_XVFB_RUN xvfb-run REQUIRED)
  find_library(AP11_XTEST Xtst REQUIRED)
  add_executable(ap11-activation-tests "${PROJECT_SOURCE_DIR}/tools/ap11-tests/activation.cpp")
  target_include_directories(ap11-activation-tests PRIVATE source include)
  target_compile_features(ap11-activation-tests PRIVATE cxx_std_20)
  target_compile_options(ap11-activation-tests PRIVATE -Wall -Wextra -Wpedantic -Werror)
  target_link_libraries(ap11-activation-tests PRIVATE "${AP11_XCB}" X11::X11)
  add_test(NAME ap11-desktop-activation COMMAND "${AP11_XVFB_RUN}" -a $<TARGET_FILE:ap11-activation-tests>)
  add_executable(ap11-panel-tests "${PROJECT_SOURCE_DIR}/tools/ap11-tests/panel.cpp")
  target_include_directories(ap11-panel-tests PRIVATE source include)
  target_compile_features(ap11-panel-tests PRIVATE cxx_std_20)
  target_compile_options(ap11-panel-tests PRIVATE -Wall -Wextra -Wpedantic -Werror)
  target_link_libraries(ap11-panel-tests PRIVATE "${AP11_XCB}" X11::X11 "${AP11_XTEST}" sdk)
  add_test(NAME ap11-panel-tests COMMAND "${AP11_XVFB_RUN}" -a $<TARGET_FILE:ap11-panel-tests>)
 endif()

endif()

if(AP8_DESCRIPTOR)
 smtg_add_vst3plugin(CommercialInstrumentBridge "${VST3_SDK_ROOT}/public.sdk/source/main/linuxmain.cpp" source/processor.cpp source/factory.cpp)
 get_filename_component(AP8_DESCRIPTOR_DIR "${AP8_DESCRIPTOR}" DIRECTORY)
 target_include_directories(CommercialInstrumentBridge PRIVATE include "${AP8_DESCRIPTOR_DIR}")
 target_compile_definitions(CommercialInstrumentBridge PRIVATE AP3_PREVIEW=1 AP8_PREVIEW=1)
 target_link_libraries(CommercialInstrumentBridge PRIVATE "${AP11_XCB}" X11::X11 sdk "${AP2_RUST_LIBRARY}" pthread dl m)
 target_compile_features(CommercialInstrumentBridge PRIVATE cxx_std_20)
 target_compile_options(CommercialInstrumentBridge PRIVATE -Wall -Wextra -Wpedantic -Werror -Wno-error=stringop-truncation)
 set_target_properties(CommercialInstrumentBridge PROPERTIES CXX_VISIBILITY_PRESET hidden VISIBILITY_INLINES_HIDDEN ON)
 target_link_options(CommercialInstrumentBridge PRIVATE "-Wl,--exclude-libs,ALL" "-Wl,-z,defs")
endif()
