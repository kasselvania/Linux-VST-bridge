[package]
name = "ap2-backend"
version = "0.1.0"
edition = "2021"
[lib]
crate-type = ["staticlib"]
[dependencies]
ap1-native-client = { path = "../../native-audio-client" }
[profile.release]
panic = "unwind"
