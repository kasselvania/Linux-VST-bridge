# Current Slice: None Selected

## Status

```text
last completed activity: D0 — Repository and Soft Design Seed
active implementation slice: none
implementation status: forbidden until one bounded slice is selected
```

## D0 result

D0 establishes:

- repository authority and governance;
- the human soft design dossier;
- provisional architecture and language ruling;
- exact research basis;
- Serum 2 and Kontakt fixture cards;
- evidence and proprietary-material rules;
- accepted/provisional/open decision register;
- no-active-slice posture.

D0 makes no bridge, runtime, Flatpak, Serum 2, Kontakt, real-time, editor, preset, authorization, or compatibility implementation claim.

## One bounded next decision

Three candidate starts are described in `docs/DESIGN_DOSSIER.md` under **One bounded next decision**.

Technical-lead recommendation:

1. select a small **Serum 2 fixture reconnaissance** slice that observes the maintainer's exact lawful installer/license channel and current host/runtime behavior without bridge code;
2. separately select a **native Bitwig VST3 host probe** slice that proves the Linux bundle/toolchain/Flatpak publication seam with an open deterministic module;
3. only then select the first cross-boundary factory/proxy slice.

This ordering preserves Serum 2 as the first real commercial target while keeping commercial activation and native ABI uncertainty from becoming one uninterpretable failure.

## Stop condition

Do not add `Cargo.toml`, CMake projects, empty crates, a runner download, compatibility profiles, or implementation stubs until the operator selects one active claim and the exact current fixture is inspected.
