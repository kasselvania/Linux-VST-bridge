# RPI2 critical-path attribution

Base: 1fb8b6fc1876ae289d90c588ad6acdb16774952b (draft PR #152).
The user authorizes this targeted mechanism-identification slice. Work in the
existing isolated checkout on codex/rpi2-critical-path-profile; sole Pi owner.
The coordinator independently checks pinned FEX profiling support.

## Outcome

Obtain one attributable profile of the busy execution path in actual Pigments,
or establish a precise profiling limitation promptly. Prior successful timing
already placed 4.977 ms inside processor.process() within 5.130 ms service per
256 frames (5.333 ms budget). Subdivide that inclusive call; do not restart a
broad layer search. Busy CPU percentage is not cost per rendered frame.

First prove that installed ARM64EC FEX can emit usable guest-library/block JIT
mapping for Linux sampling. Check perf/PMU/permissions and scheduler facilities.
If this route is unavailable, test the narrow reason, preserve the limitation
and stop rather than create a replacement profiler or rebuild the runtime.

If attribution is usable, reuse the private c9709c05 native / 9a1e93f5 Windows
pair at quantum 256, map v2 capacity 512, Pigments 7.0.1.6772, 24 AM Poly 4,
master 0.35, four notes 60/64/67/71 velocity 96, 48 kHz, JACK 512 and reserve
2048. Headless with no desktop/computer control. Existing 20-second captures
and 12-second holds suffice; retain first-use failures.

## Measurement and limits

A bounded profile should distinguish generated guest code, our host, native
Wine/FEX support, kernel and unresolved regions. Distinguish productive work,
active synchronization, runnable waiting, blocking and faults where available.
Record current progress, completed frames, timely delivery and backlog age
around first loss; request_high is historical maximum, not current depth.
Sampling overhead and mapping/coverage gaps stay explicit. Compare CPU cost
only against aligned completed work; never combine mismatched intervals.

Temporary performance governor and existing scheduler statistics are authorized
for an owned bounded run, with exact original settings restored. Root access
still requires legitimate operator authentication; do not bypass permissions.
No priority/affinity tuning without mechanism, runtime rebuild, kernel swap,
graphics/quality/polyphony/activation change, state patching or preset sweep.
Keep 512 experimental. Stop at 75°C or current power flags.

## Delivery

Preserve binaries/source, original 8-Bit Crystals state/master, preferences and
metadata, graph and runtime identity. Clean up only owned processes. Keep vendor
captures/state and detailed maps private; publish sanitized attribution or the
precise limitation. Update the short existing optimization table from findings.
Commit/push one draft stacked PR on #152, attach it and do not merge. A clear
mechanism may justify one targeted comparison within this scope; otherwise
report the missing capability without manufacturing a diagnosis.

## Completed disposition

One controlled warm-up and measured 256-frame capture completed. The warm-up
profiler materially perturbed execution, so it is retained as a failed capture,
not a baseline. The measured repeat has low profiler overhead and shows an
execution-dominated caller; the Pigments worker has substantial system CPU.
An aligned interval wholly
inside the chord hold completed 1,757 calls at about 5.109 ms caller CPU and
2.396 ms concurrent worker CPU per 256 frames, with little runnable wait and
no new delivery gaps after its early attack loss.

FEX guest labels identify a hot Pigments arithmetic-loop family, confirmed by
bounded private inspection of the exact module. Overlapping JIT map ranges
prevent exact guest-block or instruction cost attribution; generated ARM code
was not retained. No bridge, synchronization or scheduler change is justified
by this profile. The next narrow inquiries are generated ARM code quality for
that scalar floating-point family and kernel stacks for the worker, before any
codegen or synchronization optimization. Sanitized
results are in docs/experiments/RPI2.md and
evidence/rpi2/pigments-critical-profile.json. Original state, master,
preferences, governor, scheduler statistics and graph were restored; no owned
session remains.
