# IF1 — corrected candidate product check

IF1 engineering source was accepted by review 5188299869 at
`f7f0997b5e5d1ac7c691a20b192ccc90e30c88d2` and merged through PR #98 as
`cf212582b916fd4c09b5584cb5c2fde2c3960cb7`. The operator authorized the exact
merge and the following bounded continuation on the existing branch.

## Scope

Build the corrected native and Windows artifacts from merged IF1 source.
Bind a new immutable Pigments revision 15 ReviewCandidate through the existing
sealed `if1_failure` qualification route. Ordinary Pigments 11 stays active
through preparation and remains the exact rollback parent. Preserve revisions
1–14, all retained evidence and LoFi/FRAGMENTS ordinary revision 10.

Separate diagnostic command files from captured-frame metadata. Prove the
collision repair with a generated test before interacting with the product.

## One automatic check

Normally launch Bitwig with the protected test project, load revision 15, open
its editor, open the top-left menu and choose the resize operation once.
No manual desktop takeover, repeated menu sequence or broad audio/UI campaign.

If resize succeeds, retain live window/control responsiveness, absent terminal
custody and positive normal removal/quit cleanup. If the instance fails, retain
exact terminal custody, Bitwig failure presentation, native terminal failure
view on reopening, dead forwarding refusal and positive removal cleanup.
Stop on a different material failure; do not invent a successful resize or
in-place commercial recovery claim.

## Retained result — product check incomplete

Revision 15 was built, staged and published from the merged IF1 source. The
command/frame namespace collision is fixed and covered by a generated test.
The diagnostic census now also accepts the healthy idle UI scope, while still
requiring the exact live process/thread and refusing terminal instances.

One menu Down/Up reached the correct vendor child. The separate popup was
outside the root-window capture contract; its capture was refused and the
bounded harness ended before any resize choice. No menu action was repeated.
The instance was still healthy at the last pre-cleanup snapshot.

After one Escape during cleanup, the outer launcher exited 5. Durable terminal
custody retained native transport failure 3 at generation 1, epoch 1, request
120858, completed position 30938624 and confirmed snapshot 3. The Windows root
exit cause is unresolved. No normal retirement-ready record was committed.

Bitwig showed its crashed-device state, and its native plug-in-host separately
exited 255. One editor action did not expose the native terminal failure view.
Thus neither the crash screen nor cleanup alone proves the complete live
processor/controller failure-presentation path. Generated source acceptance
remains intact; product acceptance and resize qualification remain withheld.

The one authorized sequence has ended. Do not repeat it. Any next implementation
must address the popup action-observation gap and account for the native host
exit before claiming a live terminal view. No new cause, repair or campaign is
selected here. See `evidence/if1/revision-15/live-check.json`.

## Final state (verified)

Ordinary Pigments 11 is restored to the exact baseline physical publication.
Revisions 12–15 are inactive. LoFi/FRAGMENTS 10 and all protected project hashes
are unchanged. Bitwig quit normally; its engine exited zero. DSP leases,
transactions, stale transports and diagnostic units/helpers are absent; service
and keeper are active. Tracing is off. CPUWeight remains SteamOS-owned at
effective 10000, with no performance claim. 512 remains recommended and 256
unqualified. See `evidence/if1/revision-15/installed-final.json`.

PR #98 is merged source acceptance only. Revision 14 must never be promoted;
revision 15 is not ordinary authority. No root cause for previous Windows exits,
resize qualification or successful in-place recovery is implied by source acceptance.
