# AP10 — Fix delivery stalls and prove commercial audio-effect processing

## Outcome and starting point

Make the existing bridge deliver work more consistently at low latency, then use that same core for actual commercial audio input/output. Preserve Serum instrument playback and add a real effect: FRAGMENTS preferred, installed Serum 2 FX as a bounded fallback. Issue #70; branch `codex/ap10-deadline-repair-and-effects`. Implementation, necessary builds, ordinary debugging and focused device verification are authorized under AGENTS.md. This is not another measurement-only campaign.

Start from AP9 `b45b76332a4fc06cd314f7463b7a86ac6601865c`. #67 and #69 are unmerged dependencies, not new acceptance claims. Do not push to their branches, reset their evidence or merge them as part of implementation. Open an incremental PR against `codex/ap9-bridge-latency-performance` while it is unmerged; retarget to main after normal upstream integration. The dossier incorporates main's direction update `1adde984cb33cc29a7c9aab79ec4508c977887e1`.

## Diagnose, change, compare

Use the retained AP9 48 kHz / 128-frame host / 256-added-frame workload to locate the **first** stalled request, not its queued followers. The owner CPU repair is useful but did not prove lower latency. Read `docs/AP9_OWNER_COST.md` and relevant `evidence/AP9/owner-cost.json` records. Extend existing bounded traces only enough to separate request receipt, Windows preparation, process-call elapsed time, reply preparation/send, Linux receipt/publication and the host's actual callback spacing. Preserve a short pre-gap history; drain diagnostics away from delivery deadlines.

Distinguish actual thread CPU execution, runnable-but-unscheduled time and blocking with platform facilities that work on this fixture. Validate clock/counter resolution and profiling overhead; never subtract uncorrelated clocks or call all time inside process() synthesis work. The Linux worker polls an empty queue with a 50-us sleep; Windows Socket::receive/transfer and surrounding host work remain candidate locations, not established causes. Follow measured waiting before rewriting any of them.

Test one causal hypothesis at a time and make the smallest effective queue, wakeup, IPC, scheduling or lifetime repair. Compare the same workload/configuration before and after, then confirm without intrusive tracing. A failed trial should eliminate/refine a hypothesis, not trigger another blind soak. Changes using existing permitted scheduling facilities are allowed when justified; no blind priority escalation, disabled supervision or runtime replacement.

Triage the Bitwig close-time engine crash separately using existing logs and available stack/core evidence plus native teardown. Repair demonstrated ownership/lifetime faults with a targeted regression. Missing crash artifacts or an unreproduced historical event stay explicit; a later clean close does not explain them. Do not indefinitely block independent effects work on recreating an old outlier.

Keep 512 added frames as the comparison baseline. First try to make 256 defensible in Bitwig, then pursue 128 at 48 kHz (2.67 ms added) when the evidence supports it. These are targets, not guaranteed results. Preserve truthful host latency, ordered inputs/events, counted gaps and genuine-failure recovery. No callback waits, hidden buffering, rate/precision conversion or increased dropouts to manufacture a fast result. Offline/burst processing is not the paced SDK workload; explicitly distinguish unsupported modes from real-time results.

## Let the actual class determine I/O

Inspect the already-installed FRAGMENTS and Serum FX class/module/bus metadata. Xfer documents a distinct Serum 2 FX plug-in; availability in our older installed build is unverified. Do not assume the tested zero-input synth is an audio effect. Use FRAGMENTS first; fix bounded bridge defects rather than switch fixtures to conceal them. If a demonstrated vendor/content/authorization requirement blocks it, try installed Serum FX and report the boundary. Do not exhaust unrelated installers or consume new activations.

Replace instrument-only descriptor/bus assumptions with bounded SDK-derived configuration. Start with the existing zero-input stereo instrument and a stereo-main-input/stereo-main-output effect; MIDI and audio may coexist when declared. `tools/ap8_descriptor.py` currently rejects every audio input. In `mapped_processing.cpp::configure`, an input count is currently passed with one scalar SpeakerArrangement: do not lift the existing one-input restriction without supplying correctly sized bus arrays. Preserve native class/parameter identities and old projects.

Retain actual bus indices, arrangements and activation on both endpoints. Auxiliary buses must be represented and explicitly inactive, or implemented and tested; never flatten a sidechain into the main input. Negotiate/reconfigure while inactive. Forward supported kIoChanged/kLatencyChanged requests through the owning UI/control path, and explicitly reject unsupported configurations rather than acknowledge no-ops or change buffers under callbacks. Full arbitrary multichannel/hot-swapping is not this task.

Preserve input integrity with in-place buffers, variable blocks and chunk-relative note/control offsets. Carry actual host transport/tempo context and valid flags needed for the selected effect, adjusted coherently across chunks; no invented 120-BPM context. Silent input does not imply silent output: preserve tails/frozen material while the host continues processing. Query and report vendor delay separately from bridge delay; verify relevant dry/bypass/parallel alignment without demanding bit-identical granular sound.

## Verify useful product behavior and finish

Use AGain/known deterministic data for exact transport displacement and routing tests. Verify real Serum notes and a recorded audio clip through the selected commercial effect in normally Applications-launched Bitwig: input-dependent wet output, real control changes, stop/start/tail behavior, state recall and host close. Measure relevant service tails, gaps and CPU at matched conditions. Sequential commercial fixtures suffice; simultaneous-commercial support is not required. Keep hardware/converter latency separate and use Moonlight for control, not a latency oracle.

Reuse the working transport, owner, vendor environments and tests. A minimal detached editor for the **same processing instance** is allowed if needed to operate the effect; editor-only success or a second disconnected plug-in does not count. Preserve closing the editor independently of audio. Full editor embedding, the manager UI, broad format/rate matrices, a new profiler/broker framework and unrelated installers are deferred.

Return one unmerged implementation PR with the causal finding, repair and comparable result, working instrument/effect behavior, remaining limits and cleanup. A table saying 256 still glitches is not completion. If an evidenced platform limit prevents the target, state the exact mechanism and discriminating observations; preserve partial improvements without fabricating AP10 completion. Do not invent per-source approval receipts or arbitrary retry quotas. If the next step requires changing the agreed realtime/security architecture, return that concrete decision with evidence instead of thrashing.

Targeted code: backend `queued.rs`, `lib.rs`, `preview.rs`, `performance.rs`; native `processor.cpp`; Windows `mapped_processing.cpp`, `offline_processing.cpp`, `inspect_module.cpp`; `tools/ap8_descriptor.py`. References: [VST3 dynamic I/O](https://steinbergmedia.github.io/vst3_dev_portal/pages/Technical%2BDocumentation/Change%2BHistory/3.0.0/Multiple%2BDynamic%2BIO.html), [processing context](https://steinbergmedia.github.io/vst3_doc/vstinterfaces/structSteinberg_1_1Vst_1_1ProcessContext.html), [latency/tail interfaces](https://steinbergmedia.github.io/vst3_doc/vstinterfaces/classSteinberg_1_1Vst_1_1IAudioProcessor.html), [Linux scheduler counters](https://www.kernel.org/doc/html/latest/scheduler/sched-stats.html), [Windows thread execution counters](https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-getthreadtimes), [Xfer FX clarification](https://xferrecords.com/forums/general/serum-effects-as-separate-plugins).
