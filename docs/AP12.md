# AP12 — installation and registered startup, qualification pending

**Later removal failure:** after further operator edits in the recalled Pure LoFi
instance, Bitwig could not save the state required to remove the device. Windows
reported `state parameter readback`; the native side reported `state response
correlation`. This is an unresolved failure beyond the successful initial-sound
test below. The recalled session also accumulated gaps; its earlier live snapshot
was not a final zero-gap result. See
[the retained removal failure](../evidence/AP12/pure-removal-failure.json).
The focused diagnostic now identifies parameter **1066, MIDI CC helper 93 [1]**,
returning **-1** after two vendor-editor edits in a fresh project. The bridge's
additional readback rejects that value after opaque state capture. Why this
module returns -1 remains unknown; no causal repair is claimed.

AP12 is **incomplete**. The normal Arturia installation and management core are
implemented. The original installed Pure LoFi presented the vendor's Demo restriction.
A later operator-replaced module now loads, plays notes and saves in normally
launched Bitwig; that is a different artifact, not qualification of the original
installer or vendor authorization. The actual two-device chain, automation and
session-restart workflow have not yet been qualified. No everyday-usability claim
is made. AP8/#67, AP9/#69, AP10/#71 and AP11/#76 results remain unchanged.

## Installed fixture and observations

The user explicitly accepted both normal installer agreements. Only the existing
normal installer executables were run; no adjacent patches, licensing material,
account state or proprietary content was copied from historical environments.
Regular-file type, ownership, size, SHA256 and PE version/company fields were
checked. Arturia certificate subjects were present, but cryptographic Authenticode
verification was not performed and is not claimed.

| Normal installer | Version | Bytes | SHA256 | Exit |
| --- | --- | ---: | --- | ---: |
| `Pure_LoFi__1_0_0_6121.exe` | 1.0.0.6121 | 1,037,139,776 | `9e06e171eaf0278053be7a426361af0dad644a35be07f2b1e996537f4de6e464` | 0 |
| `Efx_FRAGMENTS__1_0_0_2925.exe` | 1.0.0.2925 | 83,679,008 | `b9b15be251a707ed6f6490ae410712d81ff6f579b54fb76c9c77c6ff01c74ddc` | 0 |

Both installers used their real foreground UI in a new persistent bridge-owned
Arturia environment. VST3, normal resources/content and the vendor companion were
installed; unnecessary VST2/AAX formats were deselected. Pure LoFi's standalone was
also installed. Default vendor locations were retained. Its requested Windows
restart was completed with the normal prefix restart command, exit 0. This was
not a Deck or graphical-session restart.

The environment uses Proton **11.0-2c**, build **25118279**, and Steam Linux Runtime
4 **4.0.20260805.254769**, build **24599767**, with 31 critical runner/runtime files
pinned for installed execution. Steam's files remain their observed local source
location; no Steam client process is required. No alternate runner, graphics DLL
mixing or environment-wide accessibility override was introduced. Bitwig is the
existing **6.1 Flatpak** fixture; AP12 has not changed its preferences or audio
settings.

Actual Factory3 Unicode metadata from the new modules:

| Class | Vendor | Declared subcategories | Exact Windows CID | Parameters |
| --- | --- | --- | --- | ---: |
| Pure LoFi 1.0.0.6121 | Arturia | `Instrument\|Synth` | `417274754156495350724C4650726F63` | 3,053 |
| Efx FRAGMENTS 1.0.0.2925 | Arturia | `Fx\|Tools` | `41727475415649536772616E50726F63` | 2,348 |

Installed module hashes and sanitized SDK observations are retained in
[installation-observations.json](../evidence/AP12/installation-observations.json).
These newly installed binaries differ from the historical copied fixtures. Old
FRAGMENTS processing/editor/state results are preserved, not relabeled as tests of
this installation.

## Rechecked failure and lawful access

Pure LoFi's historical AP8 `initialize()` failure **does not persist** after normal
installation: `initializeComponent` returns 0. This establishes the result of the
normal-installation comparison, not which individual missing resource caused the
old failure. Both new modules enumerate their SDK buses and parameters, then
return 1 from `getComponentState`.

Bounded stream instrumentation shows **zero bytes, zero writes, zero reads, zero
seeks and zero unknown-interface queries** for both failed calls. The stream did
not overflow or report a lifetime error. Increasing state capacity or fabricating
an empty successful state would not address the demonstrated failure.

Pure LoFi's normal standalone window explicitly displays **DEMO**, a **20-minute
limit**, and **save/load and import/export disabled**. The lawful Arturia sign-in
window was presented; authorization remains an operator/vendor action. No sign-in
credentials or activation material were handled. The visible restriction supports
the Pure LoFi state-access diagnosis. FRAGMENTS has the same no-write state refusal,
but its own authorization posture has not yet been independently established;
that cause remains unconfirmed. Its resource-integrity warning after normal
installation also remains unverified because the bridged editor has not opened.

The user clarified that they accidentally closed the earlier standalone window
after unexpected desktop input. That closure is **not** a bridge crash or evidence
of sibling cleanup failure. The window was reopened, desktop input was handed
back for authorization, and future input groups will be announced first.

## Repairs and retained failures

- The first proper-install scan was refused by our old `C:\\wf0\\fixture` path
  restriction before entering the SDK. The host now accepts exact canonical
  installed C: module paths with the existing hash checks. It also accepts distinct
  `C:\\bridge\\sessions\\<id>` transport directories. This was a bridge restriction,
  not another Pure LoFi initialize failure.
- Factory vendor/name/version/subcategories now drive native presentation and
  role. Factory1-only or ambiguous roles are inspectable but refused for
  publication. Existing UUIDv5 processor/controller derivation and real parameter
  IDs are preserved. Discovery facts can now be emitted before a later failed
  state capture; state failure remains explicit and runtime startup still fails.
- One Pure LoFi scan reported the SDK state failure and closed its SDK objects,
  but its outer launcher lingered with a vendor companion until the 180-second
  inspection deadline. The installed supervisor now recognizes the SDK terminal
  result separately from launcher exit and performs bounded positive cleanup.
  The original timeout and exit -15 remain in retained evidence. A real Linux
  subprocess regression verifies the repair; a new commercial recheck is pending.
- Rust now installs exact registration/publication state and an automatic user
  service. Separate supervised hosts share only the Arturia environment; a
  plug-in-free environment owner holds shared Wine infrastructure. Each instance
  receives distinct transport/state/editor ownership. Routine tracking retains
  AP9's stat-only PID/start identity approach. Unconfirmed cleanup blocks new
  admissions and survives service restart through persisted ownership leases.

The implementation and bounded administrative CLI are described in
[bridge-manager/README.md](../bridge-manager/README.md). No manager or service work
enters the callback, and opening/focusing an editor still does not trigger a false
full-parameter refresh.

## Verified development results

- Seven Rust management tests pass: metadata/roles, idempotent publication,
  interrupted publication, changed/missing artifact refusal, duplicate service
  ownership, independent mappings, and persisted cleanup across restart.
- Five Python tests pass on the actual Deck with real Linux subprocesses,
  including healthy-sibling survival, terminal failure with a lingering launcher,
  and native release before removing transport files. These are supervision
  tests, not proprietary DSP or GUI evidence.
- All 43 existing native backend tests pass with registered mode enabled.
- All five descriptor tests pass, including instrument-with-audio-input role,
  unchanged class IDs after a rename and discovery despite later state failure.
- Windows CI **34262362072** passed at **47a7ecd131b16081cd192bc39cffd199b433393e**,
  including existing return-event, editor/state, socket/mailbox and path tests.
- The Linux manager at **aaf07286f16aa39efd791b2bdade9845f70d336c** was cross-built,
  installed and started on the Deck. Two consecutive setup calls exited 0 with
  one enabled, active user service. Setup exited; its installed executable path
  uses neither a checkout nor a build directory. Exact artifacts and the
  deliberately empty registration list are recorded in
  [service-observation.json](../evidence/AP12/service-observation.json).

The initial service observation above predates publication. Pure LoFi is now
published and has activated the installed service through Bitwig. FRAGMENTS is
not yet published in this environment. Simultaneous vendor instances and
service/session restart with a saved project remain unproved.

## Operator-replaced Pure LoFi: focused Bitwig check

After the normal installation observations, the operator replaced Pure LoFi's
main VST3 and its two internal module copies. All three now have SHA256
`b3e8ca7477487d0dc7fbd3f8815e2f9aec7c8b701f4cb65cf96433c14fa11636`,
while retaining reported version 1.0.0.6121. These are not the original installer
bytes. The agent did not perform those replacements or handle licensing material.
The missing Demo label does not establish vendor authorization. Original failures
and hashes remain in the installation evidence; these results cannot be attributed
to a bridge source repair or to the normal vendor installation.

The installed manager's inspection now succeeds: component initialization returns
0 and initial state capture returns 0 with 73,987 bytes in one write. The native
proxy built from 7d1bb8d uses the unchanged Windows host from 47a7ecd. Registration
twice exits 0, with one valid published mapping. Normally Applications-launched
Bitwig 6.1 shows Pure LoFi as an Arturia Synth instrument.

An earlier operator load reached processing, then failed with `delivery response
deadline` at position 302,592. It delivered 302,592 frames, recorded 239,616
underrun frames and 1,273,344 rejected silent frames. Its initiating action is
unknown, and correlated request tracing was absent. Native failure is retained
even though the supervisor later confirmed cleanup; a null supervisor error is
not a clean audio result.

One focused reproduction enabled the existing trace without changing source or
buffering. Pure LoFi opened its real editor, accepted an editor-keyboard note,
closed the editor while retaining DSP, and played a four-note Bitwig clip. A local
speaker-monitor capture contained 286,720 stereo frames at 48 kHz, peak 0.05594 and
RMS 0.01731. This is local output evidence, not a physical round-trip measurement.
The stopped project saved as **AP12 Pure LoFi Check**. Project close and Bitwig
quit completed without a visible error; SDK close, native close and positive
supervisor cleanup succeeded. The outer launcher still required cleanup (raw exit
-15), separately from the SDK's successful terminal result.

The completed session processed 7,331,840 frames: 1,024 priming frames across two
intervals, 7,330,816 delivered frames, **zero underrun/expired frames and zero
rejected callbacks**. No observation jobs, samples or gap traces were lost. The
reported delay is 512 bridge + 48 vendor = **560 frames (11.67 ms at 48 kHz)**,
before the DAW/device path. Float32 is supported; float64 is refused. The inspection
reported 44 vendor frames before the 48 kHz processing configuration.

The earlier timeout did not recur. This unmatched successful retry does not
identify or repair its cause. Both sessions, bounded timing records, artifact
identities and limitations are retained in
[pure-operator-candidate.json](../evidence/AP12/pure-operator-candidate.json).

After quitting and relaunching Bitwig through Applications, **AP12 Pure LoFi
Check** reopened with the four notes. Native state restore succeeded with the
saved 184,626-byte payload and matching digest. Replayed notes again produced
local speaker-monitor output (286,720 stereo frames, peak 0.05311, RMS 0.01748).
The project remains open with playback stopped. This verifies the tested initial
sound and note clip after Bitwig restart, not an edited preset/automation recall,
Deck/session restart or bit-exact audio match. Final counters for this still-open
recalled instance have not been collected.

The operator subsequently edited several controls and attempted device removal.
The exact edit sequence is unknown; the visible LFO mapping is not established as
the cause. Windows state capture reached the additional controller-parameter
readback loop and rejected a non-finite or out-of-range normalized value. That
exception terminated processing. A generic wire Error response then surfaced as
native `state response correlation`, masking the specific Windows failure. SDK
host failure 90 and positive cleanup are both retained. A diagnostic follow-up
adds the rejected parameter ID and full-precision value, with an SDK regression
for invalid readbacks; it does not clamp values or claim a repair.

The diagnostic host from `f3f1ce1` (Windows CI run `34269208672`) was deployed
with the previous installed software and registration retained privately for
rollback. This was a development replacement, not a shipped update transaction;
the native proxy, runner, environment and vendor module were unchanged.
One fresh-project attempt timed out during the editor test before the planned
state check. A second changed only LoFi Amount and Filter Cutoff, minimized the
editor, and attempted Save. It reproduced `state parameter readback: id=1066
value=-1`, followed by host failure 90 and the native correlation error. Neither
test had an LFO mapping. Both failures and final gap/rejected-frame counts are
retained in [pure-state-diagnostic.json](../evidence/AP12/pure-state-diagnostic.json).
The traced attempt's first gap preceded state failure: 4,206 us in the request
queue and 9,738 us awaiting the reply. These timings do not isolate computation
from scheduling. No buffer change or further Pure LoFi replay was performed.

The next focused comparison rechecked the normally installed FRAGMENTS
1.0.0.2925 with that same host and its explicit per-process accessibility
override. Initialization and factory-derived `Arturia / Efx FRAGMENTS /
Fx|Tools` metadata succeed, but initial component state still returns
`kResultFalse`, with zero bytes and no stream writes, reads, seeks or unknown
interface queries. This is distinct from Pure LoFi's later parameter-readback
failure. The current host stops before editor/processing startup; its cause and
authorization posture remain unestablished. See
[fragments-current-recheck.json](../evidence/AP12/fragments-current-recheck.json).
The inspected FRAGMENTS application directory contains an uninstaller but no
standalone executable. No replacement vendor files or licensing state were copied.

All three owned diagnostic transports retired with confirmed cleanup. The
disposable project was closed without saving; the original operator project
remains open with its audio engine inactive. Temporary tracing is off, and the installed automatic service is
enabled and active. Further work returns to FRAGMENTS and the original AP12
workflow; this diagnostic is not a new Pure LoFi compatibility campaign.

## Remaining work and retained setup

Complete the remaining ordinary Bitwig workflow from CURRENT_SLICE.md with exact
artifact and authorization provenance. FRAGMENTS still needs its initial state
refusal resolved, registered publication and qualification in this environment.
It includes both real editors, one recorded/replayed parameter each, save/quit/
session restart/reopen, and removal of FRAGMENTS while Pure LoFi continues.

Registered mode fixes added delay at **512 frames per proxy**, preserving mailbox
delivery and honest gap accounting. A serial pair's bridge contribution is
therefore **1,024 frames / 21.33 ms at 48 kHz**. Actual whole-chain latency, vendor
latency and gap counts are **unmeasured for AP12**; no matrix or buffer change was
used to mask the current state-access failure.

The intended new Arturia environment and automatic service remain installed.
Existing vendor environments, old publications/projects and unrelated audio
settings remain intact. Temporary subprocess tests ended with confirmed cleanup.
The temporary standalone window and diagnostic environment owner have retired
with confirmed cleanup. The installed product service now owns playback startup.
The Pure LoFi publication and AP12 Pure LoFi Check project remain installed.
The temporary trace-enable setting was restored to absent. Full AP12 cleanup and
the pair's everyday-use qualification remain pending.
