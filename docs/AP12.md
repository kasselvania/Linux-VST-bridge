# AP12 — installation and registered startup, qualification pending

**Latest follow-up: the operator-replaced FRAGMENTS now loads, but LoFi's
intermittent delivery timeout remains unresolved.** The operator subsequently
changed FRAGMENTS' actual VST3. Its old exact-fingerprint registration correctly
refused admission. Inspection of the replacement succeeded; a new native proxy
and explicit inactive publication update now permit loading its real editor,
silent processing, saving and clean host close in normally launched Bitwig.
This is a different artifact from the original installer, not authorization proof.

The operator's new LoFi removal error is preceded by a delivery-response timeout,
then `getState` reports an already failed instance. Two stopped-transport,
editor-open removal checks succeeded with FRAGMENTS remaining healthy. They do
not repair or explain that intermittent timeout. The full AP12 workflow remains
unqualified. See [the current replacement/removal evidence](../evidence/AP12/operator-replacement-load-removal.json)
and the final section below.

**Review 5146648137 repairs remain implemented.** At the earlier checkpoint,
the normally installed FRAGMENTS was blocked by authorization. Opaque state now
survives unavailable supplemental
readback, ordinary completed save refusals leave healthy audio/editor service
alive, fresh editor access does not require a successful save, and reporting
failure cannot skip transport retirement. Production SDK/transport/native tests
pass. The actual operator-replaced LoFi passed the former two-control save/removal
failure and edited-sound recall; its recorded automation also survived a later
Bitwig restart. Both metadata-derived devices are published. This does not qualify
the original LoFi installer binary or the unactivated two-device chain.

See [automated repairs](../evidence/AP12/state-repair-automated.json),
[commercial repair checks](../evidence/AP12/state-repair-commercial.json) and the
[final installed check and cleanup](../evidence/AP12/state-repair-final-installed.json).
Historical evidence below remains unchanged and is not relabeled as a repair run.

The new wire minor 11 uses a correlated Error payload of four little-endian
32-bit fields: schema 1, operation 16 (GetState), stage 1 (component) or 2
(controller), and the signed Windows SDK result. Only completed ordinary refusals
on a bounded/quiescent read-only capture are recoverable. C ABI `ap4_state` returns
5 without changing output/written or confirming a snapshot; diagnostics retain
stage/result. Restore/protocol/crash/deadline failures remain terminal.

Commercial state envelope 3 carries payload mirror version 2 (header flags bit 1):
one 16-byte record per parameter, containing ParamID, availability 0/1 and eight
value bytes. Unavailable records have canonical zero bytes with **no numeric
meaning**. Envelope 2 / legacy 12-byte records remain readable; identity, digest,
extent and duplicate checks remain enforced. Actual automation still requires a
finite normalized value. UI ABI layout 2 remains fixed; independent UI protocol 3
marks a Parameter refresh message's `result=1` as unavailable. Every parameter
still participates in refresh completeness. Bootstrap refresh flags are zero and
do not request a host restart or opaque save.

`vendor-editor INSPECTION.json` uses the installed service and an exact inspected
module/environment/class request. It opens an explicitly labelled access-only
vendor view without publication, DAW audio or a recall claim, using the existing
Windows host and view lifecycle. Its 30-minute bound is host ownership policy,
not an extension of any vendor demo limit. Close the vendor window to retire the
session. The owning service continues cleanup if the requesting CLI disappears.

**Historical removal failure, before the repair:** after further operator edits
in the recalled Pure LoFi instance, Bitwig could not save the state required to remove the device. Windows
reported `state parameter readback`; the native side reported `state response
correlation`. This was a failure beyond the successful initial-sound
test below; its bridge readback mechanism is repaired by this continuation. The
recalled session also accumulated gaps; its earlier live snapshot
was not a final zero-gap result. See
[the retained removal failure](../evidence/AP12/pure-removal-failure.json).
The focused diagnostic identified parameter **1066, MIDI CC helper 93 [1]**,
returning **-1** after two vendor-editor edits in a fresh project. The bridge's
additional readback formerly rejected that value after opaque state capture. Why this
module returns -1 remains unknown. The repair preserves its successful opaque
state and marks the extra getter unavailable, without guessing its meaning.

AP12 is **incomplete**. The normal Arturia installation and management core are
implemented. The original installed Pure LoFi presented the vendor's Demo restriction.
A later operator-replaced module now loads, plays notes and saves in normally
launched Bitwig; that is a different artifact, not qualification of the original
installer or vendor authorization. LoFi recorded automation and application-restart
recall are now verified on that artifact. The actual two-device chain and Deck/
user-session restart workflow remain unqualified. Original-installation FRAGMENTS
saving is restricted; the later replacement has only the bounded checks below. No everyday-usability claim is made. AP8/#67, AP9/#69, AP10/#71
and AP11/#76 results remain unchanged.

## Installed fixture and observations

The user explicitly accepted both normal installer agreements. Only the existing
normal installer executables were run; no adjacent patches, licensing material,
account state or proprietary content was copied from historical environments.
Regular-file type, ownership, size, SHA256 and PE version/company fields were
checked. Arturia certificate subjects were present, but cryptographic Authenticode
verification was not performed and is not claimed.

| Normal installer | Version | Bytes | SHA256 | Exit |
| --- | --- | ---: | --- | ---: |
| `Pure_LoFi__1_0_0_6121.exe` | 1.0.0.6121 | 1,037,139,776 | `9e06e171eaf0278053be7a426361af0dad644a35be07f2b1e996537f4de6e464` | 0 |
| `Efx_FRAGMENTS__1_0_0_2925.exe` | 1.0.0.2925 | 83,679,008 | `b9b15be251a707ed6f6490ae410712d81ff6f579b54fb76c9c77c6ff01c74ddc` | 0 |

Both installers used their real foreground UI in a new persistent bridge-owned
Arturia environment. VST3, normal resources/content and the vendor companion were
installed; unnecessary VST2/AAX formats were deselected. Pure LoFi's standalone was
also installed. Default vendor locations were retained. Its requested Windows
restart was completed with the normal prefix restart command, exit 0. This was
not a Deck or graphical-session restart.

The environment uses Proton **11.0-2c**, build **25118279**, and Steam Linux Runtime
4 **4.0.20260805.254769**, build **24599767**, with 31 critical runner/runtime files
pinned for installed execution. Steam's files remain their observed local source
location; no Steam client process is required. No alternate runner, graphics DLL
mixing or environment-wide accessibility override was introduced. Bitwig is the
existing **6.1 Flatpak** fixture; AP12 has not changed its preferences or audio
settings.

Actual Factory3 Unicode metadata from the new modules:

| Class | Vendor | Declared subcategories | Exact Windows CID | Parameters |
| --- | --- | --- | --- | ---: |
| Pure LoFi 1.0.0.6121 | Arturia | `Instrument\|Synth` | `417274754156495350724C4650726F63` | 3,053 |
| Efx FRAGMENTS 1.0.0.2925 | Arturia | `Fx\|Tools` | `41727475415649536772616E50726F63` | 2,348 |

Installed module hashes and sanitized SDK observations are retained in
[installation-observations.json](../evidence/AP12/installation-observations.json).
These newly installed binaries differ from the historical copied fixtures. Old
FRAGMENTS processing/editor/state results are preserved, not relabeled as tests of
this installation.

## Rechecked failure and lawful access

Pure LoFi's historical AP8 `initialize()` failure **does not persist** after normal
installation: `initializeComponent` returns 0. This establishes the result of the
normal-installation comparison, not which individual missing resource caused the
old failure. Both new modules enumerate their SDK buses and parameters, then
return 1 from `getComponentState`.

Bounded stream instrumentation shows **zero bytes, zero writes, zero reads, zero
seeks and zero unknown-interface queries** for both failed calls. The stream did
not overflow or report a lifetime error. Increasing state capacity or fabricating
an empty successful state would not address the demonstrated failure.

Pure LoFi's normal standalone window explicitly displays **DEMO**, a **20-minute
limit**, and **save/load and import/export disabled**. The lawful Arturia sign-in
window was presented; authorization remains an operator/vendor action. No sign-in
credentials or activation material were handled. The visible restriction supports
the Pure LoFi state-access diagnosis. FRAGMENTS originally had the same no-write
state refusal without an independent UI observation. The repaired managed access
route now opens its real editor and
authorization dialog: the software is not activated on this computer, and demo
mode disables Save/Load and import/export with a 20-minute limit. No resource
corruption warning appeared in that opening; this is not a universal resolution
of the older warning. No account or activation action was performed.

The user clarified that they accidentally closed the earlier standalone window
after unexpected desktop input. That closure is **not** a bridge crash or evidence
of sibling cleanup failure. The window was reopened, desktop input was handed
back for authorization, and future input groups will be announced first.

## Repairs and retained failures

- The first proper-install scan was refused by our old `C:\\wf0\\fixture` path
  restriction before entering the SDK. The host now accepts exact canonical
  installed C: module paths with the existing hash checks. It also accepts distinct
  `C:\\bridge\\sessions\\<id>` transport directories. This was a bridge restriction,
  not another Pure LoFi initialize failure.
- Factory vendor/name/version/subcategories now drive native presentation and
  role. Factory1-only or ambiguous roles are inspectable but refused for
  publication. Existing UUIDv5 processor/controller derivation and real parameter
  IDs are preserved. Discovery facts can now be emitted before a later failed
  state capture. The continuation now permits fresh editor admission despite an
  ordinary save refusal, while unsafe restore/protocol failures remain terminal.
- One Pure LoFi scan reported the SDK state failure and closed its SDK objects,
  but its outer launcher lingered with a vendor companion until the 180-second
  inspection deadline. The installed supervisor now recognizes the SDK terminal
  result separately from launcher exit and performs bounded positive cleanup.
  The original timeout and exit -15 remain in retained evidence. A real Linux
  subprocess regression verifies the repair. Later LoFi and FRAGMENTS access
  hosts also completed SDK close and positive cleanup; the original lingering
  scan was not deliberately reproduced.
- Rust now installs exact registration/publication state and an automatic user
  service. Separate supervised hosts share only the Arturia environment; a
  plug-in-free environment owner holds shared Wine infrastructure. Each instance
  receives distinct transport/state/editor ownership. Routine tracking retains
  AP9's stat-only PID/start identity approach. Unconfirmed cleanup blocks new
  admissions and survives service restart through persisted ownership leases.

The implementation and bounded administrative CLI are described in
[bridge-manager/README.md](../bridge-manager/README.md). No manager or service work
enters the callback, and opening/focusing an editor still does not trigger a false
full-parameter refresh.

## Initial development results (retained history)

- Seven Rust management tests pass: metadata/roles, idempotent publication,
  interrupted publication, changed/missing artifact refusal, duplicate service
  ownership, independent mappings, and persisted cleanup across restart.
- Five Python tests pass on the actual Deck with real Linux subprocesses,
  including healthy-sibling survival, terminal failure with a lingering launcher,
  and native release before removing transport files. These are supervision
  tests, not proprietary DSP or GUI evidence.
- All 43 existing native backend tests pass with registered mode enabled.
- All five descriptor tests pass, including instrument-with-audio-input role,
  unchanged class IDs after a rename and discovery despite later state failure.
- Windows CI **34262362072** passed at **47a7ecd131b16081cd192bc39cffd199b433393e**,
  including existing return-event, editor/state, socket/mailbox and path tests.
- The Linux manager at **aaf07286f16aa39efd791b2bdade9845f70d336c** was cross-built,
  installed and started on the Deck. Two consecutive setup calls exited 0 with
  one enabled, active user service. Setup exited; its installed executable path
  uses neither a checkout nor a build directory. Exact artifacts and the
  deliberately empty registration list are recorded in
  [service-observation.json](../evidence/AP12/service-observation.json).

The initial service observation above predates publication. Both Pure LoFi and
FRAGMENTS are now published and visible with their correct Arturia roles in
normally launched Bitwig. LoFi automatically starts its own managed processing
host. A concurrent FRAGMENTS editor-only access session survived LoFi removal,
but that is not two simultaneous DAW audio instances or pair-project recall.

## Operator-replaced Pure LoFi: historical checks before the state repair

This section records the earlier sequence and its then-current limits. The
state-repair results and final installed disposition follow below.

After the normal installation observations, the operator replaced Pure LoFi's
main VST3 and its two internal module copies. All three now have SHA256
`b3e8ca7477487d0dc7fbd3f8815e2f9aec7c8b701f4cb65cf96433c14fa11636`,
while retaining reported version 1.0.0.6121. These are not the original installer
bytes. The agent did not perform those replacements or handle licensing material.
The missing Demo label does not establish vendor authorization. Original failures
and hashes remain in the installation evidence; these results cannot be attributed
to a bridge source repair or to the normal vendor installation.

The installed manager's inspection now succeeds: component initialization returns
0 and initial state capture returns 0 with 73,987 bytes in one write. The native
proxy built from 7d1bb8d uses the unchanged Windows host from 47a7ecd. Registration
twice exits 0, with one valid published mapping. Normally Applications-launched
Bitwig 6.1 shows Pure LoFi as an Arturia Synth instrument.

An earlier operator load reached processing, then failed with `delivery response
deadline` at position 302,592. It delivered 302,592 frames, recorded 239,616
underrun frames and 1,273,344 rejected silent frames. Its initiating action is
unknown, and correlated request tracing was absent. Native failure is retained
even though the supervisor later confirmed cleanup; a null supervisor error is
not a clean audio result.

One focused reproduction enabled the existing trace without changing source or
buffering. Pure LoFi opened its real editor, accepted an editor-keyboard note,
closed the editor while retaining DSP, and played a four-note Bitwig clip. A local
speaker-monitor capture contained 286,720 stereo frames at 48 kHz, peak 0.05594 and
RMS 0.01731. This is local output evidence, not a physical round-trip measurement.
The stopped project saved as **AP12 Pure LoFi Check**. Project close and Bitwig
quit completed without a visible error; SDK close, native close and positive
supervisor cleanup succeeded. The outer launcher still required cleanup (raw exit
-15), separately from the SDK's successful terminal result.

The completed session processed 7,331,840 frames: 1,024 priming frames across two
intervals, 7,330,816 delivered frames, **zero underrun/expired frames and zero
rejected callbacks**. No observation jobs, samples or gap traces were lost. The
reported delay is 512 bridge + 48 vendor = **560 frames (11.67 ms at 48 kHz)**,
before the DAW/device path. Float32 is supported; float64 is refused. The inspection
reported 44 vendor frames before the 48 kHz processing configuration.

The earlier timeout did not recur. This unmatched successful retry does not
identify or repair its cause. Both sessions, bounded timing records, artifact
identities and limitations are retained in
[pure-operator-candidate.json](../evidence/AP12/pure-operator-candidate.json).

After quitting and relaunching Bitwig through Applications, **AP12 Pure LoFi
Check** reopened with the four notes. Native state restore succeeded with the
saved 184,626-byte payload and matching digest. Replayed notes again produced
local speaker-monitor output (286,720 stereo frames, peak 0.05311, RMS 0.01748).
At that observation the project remained open with playback stopped. This verified the tested initial
sound and note clip after Bitwig restart, not an edited preset/automation recall,
Deck/session restart or bit-exact audio match. Final counters were not yet
collected at that observation; its later failure is retained below.

The operator subsequently edited several controls and attempted device removal.
The exact edit sequence is unknown; the visible LFO mapping is not established as
the cause. Windows state capture reached the additional controller-parameter
readback loop and rejected a non-finite or out-of-range normalized value. That
exception terminated processing. A generic wire Error response then surfaced as
native `state response correlation`, masking the specific Windows failure. SDK
host failure 90 and positive cleanup are both retained. A diagnostic follow-up
adds the rejected parameter ID and full-precision value, with an SDK regression
for invalid readbacks; it does not clamp values or claim a repair.

The diagnostic host from `f3f1ce1` (Windows CI run `34269208672`) was deployed
with the previous installed software and registration retained privately for
rollback. This was a development replacement, not a shipped update transaction;
the native proxy, runner, environment and vendor module were unchanged.
One fresh-project attempt timed out during the editor test before the planned
state check. A second changed only LoFi Amount and Filter Cutoff, minimized the
editor, and attempted Save. It reproduced `state parameter readback: id=1066
value=-1`, followed by host failure 90 and the native correlation error. Neither
test had an LFO mapping. Both failures and final gap/rejected-frame counts are
retained in [pure-state-diagnostic.json](../evidence/AP12/pure-state-diagnostic.json).
The traced attempt's first gap preceded state failure: 4,206 us in the request
queue and 9,738 us awaiting the reply. These timings do not isolate computation
from scheduling. No buffer change or further Pure LoFi replay was performed.

The next focused comparison rechecked the normally installed FRAGMENTS
1.0.0.2925 with that same host and its explicit per-process accessibility
override. Initialization and factory-derived `Arturia / Efx FRAGMENTS /
Fx|Tools` metadata succeed, but initial component state still returns
`kResultFalse`, with zero bytes and no stream writes, reads, seeks or unknown
interface queries. This is distinct from Pure LoFi's later parameter-readback
failure. That host stopped before editor/processing startup; its cause and
authorization posture were then unestablished. See
[fragments-current-recheck.json](../evidence/AP12/fragments-current-recheck.json).
The inspected FRAGMENTS application directory contains an uninstaller but no
standalone executable. No replacement vendor files or licensing state were copied.

All three owned diagnostic transports retired with confirmed cleanup. The
disposable project was closed without saving; the original operator project
remained open with its audio engine inactive. Temporary tracing was off, and the
installed automatic service was enabled and active. This diagnostic was part of
the original AP12 workflow, not a separate Pure LoFi compatibility campaign.

## Remaining work and retained setup

Both devices are now published. The original FRAGMENTS artifact needs normal
vendor authorization for its save/recall workflow. Its later operator replacement
has a distinct updated binding and the focused checks in the final section.
The remaining pair qualification includes both processing editors, recorded and
replayed FRAGMENTS automation, pair save/quit/session restart/reopen, and removal
of FRAGMENTS while Pure LoFi continues. LoFi's own edited state and recorded
automation recall are verified below.

Registered mode fixes added delay at **512 frames per proxy**, preserving mailbox
delivery and honest gap accounting. A serial pair's bridge contribution is
therefore **1,024 frames / 21.33 ms at 48 kHz**. Actual whole-chain latency, vendor
latency and gap counts are **unmeasured for AP12**; no matrix or buffer change was
used to mask the current state-access failure.

The intended new Arturia environment and automatic service remain installed.
Existing vendor environments, old publications/projects and unrelated audio
settings remain intact. Temporary subprocess tests ended with confirmed cleanup.
The temporary standalone window and diagnostic environment owner have retired
with confirmed cleanup. The installed product service now owns playback startup.
Both publications, the original AP12 Pure LoFi Check project and the new AP12
State Repair Check automation project remain installed. Temporary tracing is off,
test hosts are retired and Bitwig is closed. The pair's everyday-use qualification
remains pending; the intended installation is available.

## State-repair continuation: actual result and remaining limits

On `e836901`, the disposable **AP12 State Repair Check** project loaded the older
saved envelope, then the actual LoFi editor changed LoFi Amount and Filter Cutoff
with transport stopped. Parameter 1066 again returned -1, reproducing the former
trigger. Three subsequent genuine opaque captures succeeded with that readback
explicitly unavailable. Bitwig saved and removed the device without its former
state-error dialog. The Windows SDK completed close and the owner positively
retired transport; the independent FRAGMENTS access session stayed alive.

A new Applications-launched Bitwig process reopened the edited sound: the actual
editor showed LoFi Amount 36.5% and Cutoff 193 Hz (native normalized 0.3440).
Editor close/reopen kept the same processing session. A complete actual editor
gesture on LoFi Amount produced 0.4227 from 0.3653. Bitwig recorded that point in a
free-time launcher automation clip; its playback range was trimmed to two bars
around the recorded point, without drawing replacement automation. With writing
off, consumed Windows-controller commands and the visible vendor knob alternated
the two values. Local speaker-monitor output remained nonzero. The final installed
package reopened the saved automation and repeated hands-off playback. This is
musical routing/control evidence, not a deterministic vendor DSP comparison.

The first large mouse-drag attempt emitted no vendor value and was not counted as
recording. The native control readback and a smaller real gesture established the
successful recording. The recorded point is at 88.3.3.74 within its free-time clip;
playback/loop start is 88.1.1.00 and length 2.0.0.00. The note clip loops one bar.
The prior two-control-only save is retained privately as well as the final
60,700-byte automation project. The operator's original saved project is unchanged.

| Enclosing repaired LoFi session | Processed frames | Priming | Missed frames | Expired output | Gap intervals | Rejected silent frames |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Two controls, save and removal | 8,799,232 | 1,024 | 7,424 | 7,424 | 4 | 0 |
| Recall, editor lifecycle, automation setup/replay/save | 48,759,808 | 512 | 9,728 | 9,728 | 8 | 0 |
| Final installed package, automation recall and browser check | 5,006,336 | 512 | 4,608 | 3,584 | 5 | 0 |

These are enclosing sessions with stopped processing, UI operations and saves,
not matched steady-state benchmarks. No terminal delivery timeout, state failure
or protocol fault reproduced. Earlier terminal timeouts remain retained and are
not declared fixed. The first two sessions' initial gap was request 15 at position
2816, before editor work; queue/reply times were 7.300/9.516 ms and 8.022/8.044 ms.
Their exact computation/blocking/scheduling split remains unknown. Later queue
stalls reached about 80 ms around control/UI work; queued followers alone do not
identify an initiating vendor cause.

The final installed run's first gap is better classified: callback admissions
went from a 23.599-ms interval to only 2.254 ms. Request 4818 needed 2.557 ms from
admission to publication (151 us queue, 2.372 ms reply), exceeding its shortened
2.257-ms presentation budget by 0.300 ms. This demonstrates a callback-burst limit
for that event, not why Bitwig/device scheduling created the burst. Its Windows
request is absent from the persisted trace, so reply CPU versus blocking is unproved.
The original registered launcher dropped the Windows diagnostic opt-in; `3bbcfb5`
now propagates the existing explicit flag before audio-host launch. The final
Windows summary reports four triggers/298 retained requests, but only 58 request
rows reached the supervisor report. The host emits destructor diagnostics after
`scanner_completed`, and the supervisor can stop reading before draining them.
This incomplete trace cannot establish whether request 4818 was in the host ring.
The flag propagation is an observability repair, not a claimed latency repair;
terminal diagnostic draining and silent-startup retention remain limitations.

512 added frames remains selected. LoFi reports 48 vendor frames at 48 kHz, for
560 frames / 11.67 ms before the DAW/device path. Two bridge delays alone would be
1,024 frames / 21.33 ms; actual whole-chain vendor delay and operating reliability
remain unmeasured in this installed environment.

The installed FRAGMENTS editor-only route confirmed the real unactivated-vendor
restriction and closed with successful SDK completion and positive cleanup.
Normal Arturia authorization in this same environment is the operator action
needed for the full chain's save/recall work. FRAGMENTS DAW audio/automation,
removal with a healthy LoFi audio sibling, pair recall and Deck/user-session
restart remain pending. No demo restriction, module or licensing state was changed.
The explicit per-process Windows accessibility workaround remains selected only
for FRAGMENTS, with its screen-reader/UI Automation limitation.

Final source checks pass: 46 native backend tests, production Windows SDK state/
editor/output tests, production native SDK/host-consumer tests, seven Rust manager
tests, seven Python supervisor tests on Linux, six descriptor tests and Clippy.
The final helper tests are in CI run 34281771825; all four workflows pass at
`3bbcfb5`. The final Windows binary comes from CI run 34278568084 / merge source
`1049ad8e7fdc31924826ece695fa7996ce891b76`; its only change from the main commercial
checks is the vendor-access title encoding repair. Native binaries are reused
unchanged from `e836901`. Exact hashes and retained limitations are in the evidence.

The final update was an explicit reversible development replacement with private
rollback packages/registration/publication targets, not a shipped general update
transaction. Diagnostics are off; all three LoFi test hosts and the vendor-access
host have positive cleanup/retirement receipts. The service is enabled and active
with no live leases, and both exact publications, the Arturia environment and the
saved automation project remain installed. Automation writing is off and the
previous Latch mode is restored. No lower buffer or unrelated audio setting changed.

## Later operator replacement and removal-error follow-up

The operator replaced FRAGMENTS' VST3 to address its login limitation. The installed
file now has SHA256 `d7ed0361558d6275e6ea3ce217bd0fe69cb32e9881d967899ab659b0d367fa26`
(20,673,024 bytes), replacing the earlier `edb358…` artifact. The service refused
four admissions with `artifact missing or changed`; the existing native proxy was
bound to the earlier file. Those attempts did not enter the Windows plug-in.
The agent did not alter either vendor module or licensing state.

A fresh managed inspection of the replacement returns real component state
(50,754 bytes), the same Arturia `Fx|Tools` class and 2,348 parameters. Its generated
descriptor differs only in the module fingerprint. The rebuilt native proxy is
`579fcafa9fb9e4de79fbcce13c216a9756d9729f35caf1d41fd4c21bb54ecc3d`, built from
`fd313ba` with the unchanged pinned SDK/build lane. The installed Windows host
`5a289d9c…` was reused. An explicit inactive development update preserved the old
registration/publication target and installed the new binding. This does not
claim a general shipped module-update transaction or a rollback copy of the
operator-overwritten vendor file.

After a normal Applications launch, Bitwig loaded FRAGMENTS, opened its actual
editor without a login dialog, captured state for Save, and closed successfully.
The separate FRAGMENTS instance survived both subsequent LoFi removals. Its
48-kHz reported delay is 512 bridge + 192 vendor = 704 frames / 14.67 ms. This
was a silent load/editor/save/close check, not musical effect, automation or
reopened-state qualification. The saved **AP12 Operator Load Check** project is
44,944 bytes; its digest is retained in the evidence.

The operator's new LoFi report is a different failure from the fixed getter:
`delivery response deadline` at position 823,296, followed later by GetState
`failed_instance` with 155 callbacks already rejected. Earlier genuine captures
had succeeded. The operator confirms the editor was open and transport was
stopped with no audio playing. Bitwig still supplies silent processing blocks
in that state. The missing response's computation/blocking/scheduling cause is
unknown; tracing was off in that failed session. The owner positively cleaned up,
but the audio session was failed and no successful SDK close is claimed.

| Current follow-up session | Admitted frames | Priming | Underrun frames | Expired output | Rejected silent frames | Result |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| Operator LoFi failure | 1,063,424 | 512 | 240,128 | 512 | 173,056 | Delivery timeout; later save refused |
| Longer idle LoFi removal | 4,045,312 | 512 | 2,304 | 2,304 | 0 | Save/removal/close succeeded |
| Shorter idle LoFi removal | 2,767,360 | 512 | 4,864 | 4,864 | 0 | Save/removal/close succeeded |
| Concurrent FRAGMENTS | 2,917,888 | 1,024 | 0 | 0 | 0 | Editor/save/close succeeded |

Rejected frames are separate from admitted frames. These enclosing sessions are
not comparable steady-state performance windows. The two LoFi removals kept the
real editor open and transport stopped; no control edits, notes or input were
required. Read-only 200-ms mailbox/thread sampling ran for about 89.82 and 64.40
seconds, respectively, without suspending threads or changing priority. It added
diagnostic overhead. Neither long terminal stall reproduced. Actual admitted
processing durations were about 84.28 and 57.65 seconds, so GUI pacing prevented
an immediate-after-open removal; a narrow startup/removal race is not excluded.
The observed Fontconfig warnings also occur in successful sessions and do not
establish the cause. No timeout repair is claimed from these successful retries.

This follow-up changes only the FRAGMENTS publication binding and retains useful
observations. Core state/audio/Windows source remains `fd313ba`; no buffer or
runner changes were made. Native compilation and actual host loading succeeded;
existing production SDK/transport tests remain applicable. The original vendor
installation and both operator-replaced candidates remain separate claim scopes.
The serial musical chain, both automation gestures, restart/recall and operating
reliability still need verification; qualification or authorization of the original installed FRAGMENTS does not
follow from these replacement-artifact observations.

Bitwig is closed, both publications are valid, the automatic service is enabled
and active, and no live leases remain. The original user project and earlier
LoFi automation project are unchanged. The new test project remains available.
Temporary trace flags and both bounded sampling jobs are off. Final ownership,
frame accounting, exact artifact identities and limitations are in
[operator-replacement-load-removal.json](../evidence/AP12/operator-replacement-load-removal.json).
