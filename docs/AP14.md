# AP14 — Profile-driven managed publication and rollback

Implementation and exact Deck checks for issue #83 / PR #85, based on integrated AP13 `ca0e2f7d5c85515c8ec22b67d434c6288ca611f7`. Technical review 5158330660 supports the architecture and fixture result; the R1 repair below awaits focused rereview. The PR is unmerged and not universally qualified. AP13 audio/state/delivery results and AP12 commercial evidence remain separate authorities. Safe readback and results are retained in [evidence/ap14](../evidence/ap14/README.md).

## Ordinary installed operation

Product setup installs `~/.local/bin/linux-vst-bridge`. The command operates on the existing owned Arturia environment:

```text
linux-vst-bridge managed environments
linux-vst-bridge managed preview
linux-vst-bridge managed publish
linux-vst-bridge managed status
linux-vst-bridge managed rollback PRODUCT_NUMBER EXACT_RETAINED_REVISION
linux-vst-bridge managed reconcile
```

An environment number is required only when selection is ambiguous. Product numbers and retained revision IDs come from structured status. The ordinary publish operation discovers installed modules, performs supervised inspection when its exact census is absent/stale, matches embedded profiles, derives registrations and publishes both supported classes. It accepts no caller-supplied class ID, digest, compatibility boolean, registration JSON, native build path or runner command. The old raw registration command remains a development surface and refuses changed or managed bindings.

## Profile and artifact contract

`compatibility/arturia-pure-lofi.json` and `arturia-efx-fragments.json` are schema 1, immutable revision 3, `verified_exact_fixture`, for the exact AP14 fixture. Revision-2 review candidates and older targets remain unchanged in Git and installed history. Exact observed module SHA-256, vendor, SDK class name/version/subcategories/tier and instrument/effect role bind each match. Requirements separately pin the runner's critical artifact set and entry points, environment family/revision, Windows host/source manifest, native image/source commit and generator descriptor hash.

Capabilities are closed Rust enums: process-scoped disabled Windows accessibility; the existing detached vendor window with native open/close panel; AP13 concurrent read-only state capture; float32 only; 512 recommended with 256 unqualified. Profiles contain no commands, arbitrary capability maps, local environment IDs/paths, vendor state or authorization material. Evidence references are bounded, relative, non-authoritative paths. Input is limited to 16 KiB per profile, 16 profiles, fixed typed nesting, bounded strings and collections. Unsupported schema/capabilities, unknown fields and duplicate identities/revisions are refused before scanning/publication.

The bounded AP14 native route adopts the existing exact, already owned generated proxies during product software setup and verifies them against the pinned profiles. It installs copies and an exact catalogue with immutable product software. Subsequent publication does not depend on the original generator checkout or build tree. This is not a native compiler/download service for unknown products. The native binaries are reused exactly from AP13 source `16efd270ee7f00cbd365974d167bfe8448355acb`; they are not relabeled as the later AP13 validator-correction head. A demonstrated environment-desktop lifetime defect required the narrow Windows keeper repair described below. Profile revisions 2 and 3 pin that host and its exact source manifest separately from the manager and native artifacts.

Supervised census uses the existing Windows factory/inspection code and installed owner service. A coordinated inspection excludes DSP admission while sharing the persistent environment keeper. Internal standalone inspection retains exclusive locking. Factory Windows TUIDs are normalized to portable SDK FUID strings; only the selected declared audio-module class is matched. Parameter metadata completeness is checked without making successful parameter value readback or state saving a prerequisite. Cached normalized observations are checked against the exact retained supervised report, current module stamp/hash, environment/runner/host and a ten-minute freshness bound.

## Publication and recovery contract

The existing registry, owned bundle store and DAW discovery link remain authoritative mechanisms. Registry entries add an optional exact managed revision reference. Each immutable revision binds the profile/claim, exact census, registration, stable external IDs, performance selection, native target, transaction and parent. Legacy adoption records the exact existing target as a retained parent; it does not reconstruct it from mutable source files.

A durable intent records prior and candidate before candidate creation and activation. The candidate native image, bundle provenance and revision record are exact and fsynced before the discovery link changes. Linux uses `renameat2` exchange/no-replace; macOS fixture tests use the corresponding exclusive/exchange primitive. Displaced links are checked and foreign entries are preserved. Registry commit and result retention follow the pointer exchange. Recovery reads the durable intent and physical link: prior still active means abort, candidate active means validate/complete, and an invalid activated candidate restores the recorded intact prior. Results are retained and cleanup is repeatable.

Rollback selects an exact retained ancestor and refuses incompatible current module/runner or performance bindings. It never rebuilds an old target. Active target DSP leases block publication, removal and rollback; a keeper or healthy sibling does not. A failed operation can retain inactive candidate/staging files as diagnostic artifacts; AP14 does not implement a general history garbage collector. Same-user tampering is detected through hashes/physical checks; this is not a security sandbox against the account that owns the files.

Canonical Rust status reports physical versus recorded publication, exact profile/claim, module/build and environment/runner/host/native validity, prior rollback target, selected performance, capabilities, limitations, pending recovery and typed refusal. Missing files are failures, not positive status inferred from registry intent.

## Focused verification

Production-path tests cover strict profiles, exact/zero/ambiguous matching, class/role/metadata mismatch, stale or changed census/module, wrong runner/environment/host/native, unsupported precision, derived registration, stable UUIDv5 identities, installed artifact independence, idempotent publication, independent classes and DSP lease refusal. Transaction injection covers sixteen durable boundaries, plus rollback/removal boundaries, corrupted activated candidates, exact prior preservation, foreign entries and physical readback. Linux process tests retain AP12/AP13 ownership, failure visibility and positive retirement coverage; coordinated inspection adds a focused lock-mode regression.

`managed check-candidate PROFILE` is an explicit nonactivating qualification path, including for `ReviewCandidate`. It always returns `activation_permitted=false` and cannot create an intent, candidate bundle, publication revision, registry mutation or discovery link. Fresh observation/cache records may be retained. `managed fault-check PRODUCT_NUMBER` injects only the fixed candidate-ready-before-activation boundary against disposable candidate files; normal reconcile leaves the recorded prior active. All post-activation injection remains in deterministic fixtures.

Local Rust validation at implementation head `1236e377ab3f8035f5e918ae193465d6e7ff82a1` passes 23 library tests plus one binary test; Clippy with warnings denied passes. `python3 -m unittest discover -s bridge-manager/runtime -v` passes locally on macOS with six passes and eight Linux-only skips, and passes all 14 in Linux CI. The same runtime sources copied to the Deck pass all 14 via `python3 -m unittest discover -s $TASK -p test_session.py -v`. The manager, Windows host, native result-lifetime and PX2 CI lanes all pass on that head. The later evidence/documentation commit does not change production or test sources. Exact run links and preserved earlier failures are in [validation.json](../evidence/ap14/validation.json).

The new Windows test uses the production `EnvironmentDesktop` helper: owner-thread enforcement, hidden window, independent short-lived window retirement, destruction and loss-of-window refusal. The existing SDK/state/result tests remain. An AP13 capture-test race surfaced in PX2: its processing counter advanced before the actual result was published. The test now waits outside the callback for the existing release/acquire publication counter. Production native code is unchanged. An unrelated Chrome apt index hash mismatch twice prevented the native lane from building; the lane now updates only its signed Ubuntu sources. Signature/hash checks remain enabled.

## Demonstrated desktop failure and repair

The first managed scan/publication completed, but opening LoFi's real editor then terminated its Windows host with X11 `BadWindow`, `X_UnmapWindow`, resource `0x1400001`. A stopped-transport, single-open reproduction failed identically. FRAGMENTS' sibling remained healthy. Native/owner fault records retained the pending request and editor service stage before positive containment. These failures remain in [editor-failure.json](../evidence/ap14/editor-failure.json); they are not replaced by successful retries.

A bounded debugger attachment to that exact LoFi process caught `XUnmapWindow → ungrab_clipping_window → X11DRV_ClipCursor → process_wine_clipcursor → peek_message`. The old environment keeper only slept. It kept the shared Windows environment alive without initializing the GUI desktop. Source inspection and the controlled comparison identify a desktop resource-lifetime mismatch: the short-lived first GUI client can leave a stale cursor-clipping window reference after retirement. Restarting only the old keeper and skipping a new scan allowed the same old host/editor to open; that comparison alone was not a fix.

The environment keeper now creates one hidden, nonactivating top-level Win32 window before announcing readiness and pumps bounded messages on its owner thread. It never loads a plug-in or accepts editor commands. This establishes GUI-desktop ownership in the process whose lifetime spans scanner and DSP clients. Loss of the window/message lifetime fails the keeper's healthy claim. The vendor editor and native Open/focus/Close panel remain unchanged. No runtime or graphics DLL family was changed.

After deploying the repaired keeper, a fresh managed scan completed and its scanners retired. Both actual vendor editors opened and closed. Both DSP instances then retired cleanly; the saved project reopened through Applications and both edited controls read back correctly from newly opened real editors under the same keeper. The exact older LoFi host also opened its editor under that keeper during rollback. This is fixture evidence for the lifetime repair, not a general Wine/X11 guarantee. The earlier Fontconfig warnings were not established as causal; the focused checks found no OOM or GPU reset.

To preserve exact rollback across this bridge-host update, admission may serve an older host only from a complete retained managed revision with its exact host/source bytes and declared AP13 protocol/state capability, while current embedded policy pins the current installed host. Raw registrations retain exact installed-host equality; unknown host/source, altered provenance and unsupported claims fail. The keeper always uses current product software. A focused Rust regression and actual Deck rollback cover this distinction.

## Exact installed workflow and results

Fixture: SteamOS 3.8.16, kernel `6.16.12-valve24.5-1-neptune-616-gb2f7cfe85e45`, Bitwig Studio 6.1 Flatpak commit `8a048e733e74dda8b897339436153a2d5df952f29d362dfcaca9d8e0d6f6c231`, runtime `org.freedesktop.Platform/x86_64/25.08`. The pinned Proton/SLR environment and exact vendor artifacts are unchanged. Full runner/module/native/source bindings and normalized physical readback are in [installed-identities.json](../evidence/ap14/installed-identities.json).

- Pure LoFi 1.0.0.6121: Arturia, `Instrument|Synth`, class `417274754156495350724C4650726F63`.
- Efx FRAGMENTS 1.0.0.2925: Arturia, `Fx|Tools`, class `41727475415649536772616E50726F63`.

The ordinary installed `managed publish` route scanned/selected/derived/published both products without operator class IDs, hashes, registration JSON, compatibility flags, build paths or Wine commands. Initial fresh publication took 21.557 seconds; repaired-host fresh publication took 18.651 seconds. These are bounded operation durations, not performance comparisons. Repetition with the same binding was idempotent. An incompatible runner profile was refused without changing either pointer. The fixed candidate-ready interruption left prior targets intact; normal reconcile deterministically aborted the inactive candidate. All sixteen durable-boundary interruptions, post-activation corruption, foreign ownership, active DSP and sibling cases run through production manager fixtures.

The completed pre-R1 installed publications were (retained unchanged as history):

| Product | Profile / claim | Active revision | Exact prior rollback revision |
| --- | --- | --- | --- |
| Pure LoFi | `arturia-pure-lofi`, revision 2, `review_candidate` | `c6766455f13f73c3756db665b1bf92be` | `18f3da27dda66a44ed19677d4921af8c` (adopted exact AP13 target) |
| Efx FRAGMENTS | `arturia-efx-fragments`, revision 2, `review_candidate` | `d2fb889b4a3797a2b90c0a57152b0296` | `9b8970282584cc344c4f2dbd9339fd71` (retained profile revision 1) |

LoFi rollback selected the physical retained AP13 target and its original host, preserved FRAGMENTS' target exactly, and passed normal Bitwig admission/playback/editor access. Ordinary publication restored revision 2 afterward. [transactions.json](../evidence/ap14/transactions.json) retains before/after pointers, revision hashes, refusal and recovery readback. Candidate update does not overwrite the prior target. No vendor files, state, authorization data or original projects participate in these transactions.

Bitwig needed a normal application relaunch to refresh discovery after a pointer update while its dashboard was already open. Before that relaunch it displayed missing devices without attempting to admit them. Native bytes and stable external IDs were unchanged; after Applications relaunch the saved chain loaded normally. AP14 does not force-refresh the DAW's cache or promise live discovery refresh.

The protected `AP14 Publication` copy reused the existing two-bar notes and LoFi Amount / FRAGMENTS Grain Mix automation. Local speaker-monitor output was nonzero during the hands-off replay. Real vendor edits changed LoFi cutoff from 666 to 1350 Hz (host value 0.6200) and FRAGMENTS grain volume from −2.70 to −5.00 dB (0.8520). Explicit File → Save succeeded. After normal quit and Applications relaunch, both real editors read back 1350 Hz / −5.00 dB. Both instances and their editors then closed cleanly. Existing AP13 recorded-gesture evidence is reused; no new automation qualification is claimed.

A separate `AP14 Removal` copy removed FRAGMENTS during playback without a state-save error. LoFi retained session `53a180300accbdfcf757c4d31405ba66`, continued local output, and added no gaps across the before/after removal bracket. The LoFi-only result was saved separately. The AP12/AP13 original project hashes remain unchanged; the edited full chain remains installed as `AP14 Publication`.

## Timing, remaining limits and installed cleanup

Negotiation remains 48 kHz, float32, host maximum 512, internal chunk maximum 256 and **512 added frames per proxy**. LoFi reports 48 vendor frames plus 512 bridge frames; FRAGMENTS reports 192 plus 512. The chain therefore reports **1264 frames / 26.333 ms**: 1024 bridge frames and 240 vendor frames, excluding the DAW/device path. Moonlight supplied control, not latency measurement.

The first repaired session includes idle, editor, musical playback and close intervals; it is not a uniform benchmark. Final counters were LoFi 2 gaps / 1792 missing frames and FRAGMENTS 6 / 6912. Both ended with zero callback rejections and clean SDK/transport retirement. The focused 32.48-second playback/editor bracket added zero gaps for either device. Reopen/readback totals were LoFi 1 / 512 and FRAGMENTS 2 / 3072, also clean. Removal-copy totals were LoFi 3 / 768 (all present before removal) and FRAGMENTS 0 / 0. Full per-session admitted/delivered/priming/expired counts and first delayed requests are in [musical-smoke.json](../evidence/ap14/musical-smoke.json).

The first retained short LoFi gap still shows 30.664 ms in native preparation, followed by sub-millisecond response work; FRAGMENTS also retains 24–30 ms preparation delays. These are the existing unclassified AP13 performance constraint. The editor-lifetime repair is not presented as a dropout-rate improvement, and AP14 does not reopen that performance campaign.

Final readback found an unrelated runtime `CPUWeight=10000` override whose file mtime was 2026-09-09 18:01:51.550387 UTC, during the late rollback interval. The operator reported no intentional change. Its source is unknown; no CPU or gap-rate comparison is inferred from that interval. The earlier edited save/recall/removal checks preceded that timestamp. The exact runtime override was removed; final systemd state is unset and live cgroup weight is 100. The observation, original override hash and restoration are retained.

The intended environment, automatic service, product software/catalogue, revision history, both discovery publications and saved projects remain installed. Service is active with zero automatic restarts after final setup; the same environment keeper remains and no DSP lease remains. Temporary debugger units ended, debugger command scripts and local audio captures were removed, and heavy tracing is off; bounded fault visibility remains. The registry-lock/service-start setup defect found during deployment was repaired before final setup. A service restart and normal Applications relaunch tested the affected route; no full Deck reboot was needed or claimed.

**512 remains installed, supported and recommended; 256 remains tested but unqualified.** Claims cover only these exact operator-installed artifacts and reviewed conditions. Accessibility remains disabled only for each vendor process. Detached focus refusal, FRAGMENTS advanced redraw, short delivery gaps, general distribution/trust, history garbage collection and other products remain outside this result. Issues #80 and #84 remain deferred.

## R1 — verified activation authority

[technical review 5158330660](https://github.com/kasselvania/Linux-VST-bridge/pull/85#pullrequestreview-5158330660) selected this bounded repair against `50a985c4bce4ca9f9fb29c0bd196151adebd41ae`. `Claim` is the typed eligibility owner, with explicit activation and qualification purposes. Ordinary managed preview/publish permits only `VerifiedExactFixture`; candidate and withdrawn refusals are canonical typed results. Publication checks eligibility before even acquiring the registry lock or reconciling pending work. Current service policy requires exactly one verified profile before same-host admission or the existing bounded cross-version route.

Exact retained history uses the existing immutable revision/artifact/ancestry/physical checks. It is not reselected as new policy: revision-2 candidate targets and older ancestors remain loadable and rollbackable when those laws permit. Test-only historical fixture construction uses the existing record, bundle and transaction writers; no production bypass was added. Existing boundary, corruption, foreign-entry, lease, sibling, scanner and state/editor tests remain.

Both profile IDs advance to revision 3 with `verified_exact_fixture`. Exact module/build/role, runner/environment/host/native/descriptor bindings, process accessibility, native-panel detached editor, AP13 state capture, float32-only scope, 512 recommended / 256 unqualified and all limitations are unchanged. Regression fixtures retain exact revision-2 bytes and assert their original fingerprints and stable processor/controller identities. Evidence adds the completed AP14 publication/rollback, installed-fixture and musical-smoke references; it does not claim another customer installation.

Focused local validation passes 27 library tests plus two installed-command tests, including candidate qualification/refusal with complete filesystem snapshots, refusal with a pending transaction, same-host/cross-version current policy, duplicate verified authority refusal, retained candidate/legacy rollback and canonical claim names. Clippy with warnings denied passes. Python passes six local tests with eight Linux-only skips; Linux CI supplies the process lane. R1 Deck readback and final CI are recorded separately from the original AP14 campaign. No new musical, automation, editor or latency campaign is required by this repair.

### R1 installed result

The repaired manager was cross-built from `f4aecf855ca1c23fda7fddf68cd76ec3b3b1e3f6` (tree `caaaee33b66c61ee2de0eb939307c9058ce2eca1`); its installed SHA-256 is `1cd35705c4c0d8e0d56316b63f7aec1d75da7d425074c98cc83b5c8cdc0029c5`. Exact existing Windows host/source, supervisor/ownership and native bytes were reused. Product setup left the registry and all retained history unchanged. One ordinary `managed publish` completed in 19.047 seconds and produced:

| Profile (revision 3, verified exact fixture) | Profile fingerprint | Active publication | Exact revision-2 parent |
| --- | --- | --- | --- |
| `arturia-pure-lofi` | `13b02bb8a4643772039df81b30b681f66d58c2b9d4976e39c4daabe087a18766` | `2452b7ebaeb57a847ba957637e3369a7` | `c6766455f13f73c3756db665b1bf92be` |
| `arturia-efx-fragments` | `f9819ff473e0670eb44c52f4479a00f6addc8c4020e8ffbbee380fa8e22b4abc` | `213f36d41e964bcbdccd30093a6cded0` | `d2fb889b4a3797a2b90c0a57152b0296` |

One Applications-launched Bitwig 6.1 reopen of `AP14 Publication` admitted both devices under unchanged Linux processor/controller IDs and displayed saved controls. Transport stayed stopped. Normal quit yielded positive SDK/transport cleanup for both sessions; there is no active DSP lease or pending transaction. All 47 pre-existing profile/publication files and all four protected project hashes remain exact. Service is active, zero automatic restarts, keeper `6bbb8a503fbe5f836403e32f2d6ddb5d`; heavy tracing is off and CPUWeight remains unset/effective 100. Both devices select/recommend 512; 256 remains unqualified. Exact host/native content bindings remain unchanged while their copy locations advance with immutable software/publication revisions.

Idle native counters retain one LoFi gap / 256 missing frames and zero FRAGMENTS gaps, with zero callback rejections and clean retirement. This is the bounded admission check, not a musical or performance campaign; unchanged AP13/AP14 evidence and limitations apply. No vendor software, runtime, authorization, editor panel, #80 or #84 changes were made. See [R1 readback and validation](../evidence/ap14/r1/README.md). The same PR remains open and unmerged for focused rereview.
