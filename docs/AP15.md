# AP15 — Direct detached vendor-editor lifecycle

Current result: revision-6 engineering qualification is recorded below for both exact Arturia devices. Both verified revision-3 publications are restored. Candidates remain immutable `review_candidate` profiles; independent review and ordinary support are separate. [Windows provenance](../evidence/ap15/qualification/windows.json) preserves the real synthetic build commit and reviewed common tree. Earlier capacity blocks and failed candidates remain historical evidence, not relabeled passes.

Preparation authority for issue #84, based on integrated AP14 `main` at `84abd6a9755ab10b015c3744549b1e46ab1b85cb`.

## Product result

The bridge should disappear from the normal editor interaction. Bitwig’s editor action directly shows or focuses the exact detached vendor editor. Closing that vendor window retires the editor generation, not the plug-in. Reopening creates one new editor generation for the same DSP instance. Closing Bitwig’s editor view closes the vendor editor once.

## Source-grounded starting point

`native-vst3-proxy/source/vendor_panel.h` currently returns a fixed mapped X11 `IPlugView` that draws bridge-owned open/focus and close buttons. `attached()` maps the child and invokes `PanelOwner::panelOpen`; `onFocus(true)` forwards focus through the controller; `removed()` invokes `panelClose` and destroys the native child.

`native-vst3-proxy/source/commercial_controller.h` creates the panel and translates host lifecycle into native UI commands. `native-vst3-proxy/include/ap11_gui.h` defines the fixed command/status ABI, including `Open`, `Close`, `Focus`, `EditorStatus`, activation identity, target X11 ID and view epoch. `native-vst3-proxy/backend/src/gui.rs` and `queued.rs` own the mapped native UI session and generation.

On Windows, `windows-factory-probe/source/gui_channel.h` owns the mapped channel, `editor_session.h` services commands and publishes generation-aware status, and `vendor_view.h` owns the real VST3 vendor view and detached Win32 parent. `VendorView` already defers `WM_CLOSE` teardown out of the callback, preserves owner-thread operations, tracks open/close/focus counts, and supports plug-in-requested resize. AP14’s `environment_desktop.h` keeps the shared Wine/X11 desktop lifetime alive across scanner and DSP clients.

The panel therefore combines two different responsibilities:

1. the VST3/X11 object Bitwig requires and owns; and
2. a visible bridge control surface the musician should not see.

AP15 separates those responsibilities. It retains a host-correct native lifecycle delegate while removing the visible operational panel. It does not remove the VST3 view contract, embed the Windows editor, or make DSP depend on editor health.

## Primary engineering questions

- What minimum native view shape does Bitwig accept and retain without exposing an ordinary bridge window?
- Which VST3 calls correspond to Bitwig editor open, hide, focus and removal on the exact fixture?
- How does vendor-side Win32 close return an exact generation-scoped notification to the native delegate?
- Which existing status fields are sufficient, and which generation/lifecycle transitions need an explicit addition?
- How are duplicate open/focus calls made idempotent?
- How are stale close/resize/focus messages prevented from touching a replacement editor?
- Does the same mechanism affect FRAGMENTS expansion, or is #80 still independent?

The implementation should answer these through deterministic fixtures and bounded Deck observation rather than assumptions.

## Preserved boundaries

- AP14 environment-desktop ownership remains.
- Win32 and VST3 thread affinity remains.
- Audio callbacks remain free of editor work and waits.
- Opaque state, automation, parameter gestures and stable identities remain.
- Editor failure remains isolated from processing.
- The standalone vendor-access command remains separate.
- Revision-3 profiles remain immutable known-good records.
- 512 remains supported/recommended; 256 remains unqualified.

## Qualification and profile law

Direct lifecycle changes the profile’s editor capability and likely the native artifact digest. The implementation must not edit revision 3 or allow an AP15 review candidate through the ordinary activation route. Keep the integrated AP14 publication as the known-good target and use only an explicit reversible engineering qualification route. Retain exact physical before/after/rollback evidence. Independent review determines the final verified profile revision.

## Completion threshold

AP15 is complete only when both deterministic and exact-fixture evidence show:

- one DAW action presents one vendor editor with no visible bridge panel;
- vendor close leaves the same DSP instance alive;
- DAW close closes the editor once;
- reopen creates one fresh generation;
- duplicate and stale requests are harmless;
- repeated cycles clean up all editor-specific ownership;
- editor failure is truthful and isolated;
- touched save/recall, automation, resize/popup and sibling behavior remain intact.

Do not call a hidden-but-still-clickable panel, an unmanaged floating process, a zero-size view Bitwig immediately discards, or a vendor window that cannot close/reopen cleanly a completed result.
## Implementation progress — initial host observation

The existing revision-3 proxy was observed without changing its installed bytes. Retained native objects were relinked for debugger symbols and their stripped SHA-256 exactly reproduced the installed image. In Applications-launched Bitwig 6.1, each subsequent editor action after host closure calls createView, setFrame, getSize, attached, getSize and onSize. Host closure/removal calls removed then destroys that view. The allocator can reuse its address. Vendor closure alone does not close Bitwig's native shell; the next toggle closes it and a second toggle is required to reopen. No onFocus or checkSizeConstraint callback appeared in this bounded sequence. The preparation's focus-forwarding description was inaccurate: the original panel had no onFocus override. See [initial observation](../evidence/ap15/host-sequence.json).

Implementation is in progress. The first delegate candidate uses a noninteractive, nonzero SDK view, an unmapped host shell and its advertised WM_DELETE_WINDOW protocol. It has not yet been accepted on Bitwig; no direct-lifecycle or ordinary-support claim follows from this candidate. The verified revision-3 publications remain installed.

## Local implementation checkpoint — Windows qualification pending

The implementation now separates native view tokens, vendor view epochs, activation serials and the existing processing generation. The native controller retains a bounded lifecycle record. A completed native removal emits one Close bound to that view and its acknowledged editor epoch; an opening with no acknowledged epoch may be cancelled only by the same native token. Retired results cannot validate or close a replacement. Editor epochs and native token/activation counters fail rather than wrap.

UI ABI **3** and mapped UI protocol **4** use a 608-byte message and 320-byte header. Both languages check the message layout/version; Rust checks the eight-byte ABI prefix before forming a full message reference. The separate close mailbox uses a bounded seqlock snapshot containing native token, expected editor epoch and command cutoff. The Windows owner never spins on an incomplete close snapshot. Audio IPC and AP13 state semantics are unchanged. The supervisor's bounded fault reader recognizes both retained UI-v3 and candidate UI-v4 layouts.

The experimental delegate creates no X11 child, paints nothing and selects no mouse/keyboard input. It retains nonzero SDK geometry, a host frame and run-loop timer. It admits only a supplied X11 parent advertising `WM_DELETE_WINDOW`, automatically requests the vendor editor after attachment, unmaps the host shell on MapNotify, and sends that parent's advertised close request when the vendor epoch retires. It never destroys the host parent. SDK/Xvfb tests cover this mechanism, owner-thread rejection and repeated reference/timer cleanup. **Actual Bitwig acceptance of this unmapped one-pixel hypothesis remains unproved.** In particular, no claim is made yet about absence of a visible flash, Bitwig toggle coherence, or foreground activation using the captured host timestamp.

`qualify-editor stage EXACT_PRODUCT_PACKAGE | publish | restore` is a separate engineering command. Its candidate roster is compiled with the manager; callers cannot supply profile policy or arbitrary native/host authority. Candidate artifacts live separately from the ordinary installed software. Publication reuses the existing immutable AP14 transaction, records `qualification: ap15_editor`, requires the exact verified revision-3 parent and inactive DSP, and retains the physical parent. Explicit reconcile/service startup restores that exact parent using normal rollback; active DSP and foreign links refuse mutation. `managed preview/publish`, current-host authority and `check-candidate` retain their AP14 eligibility laws.

**The production candidate roster deliberately refuses with `qualification_artifacts_pending`.** GitHub Actions cannot currently start its jobs because the account's billing/spending allowance is exhausted. The operator instructed continuation of local work and deferral of Windows issues. No new Windows binary, complete exact candidate profile, engineering activation or commercial lifecycle qualification is claimed. The production transaction regressions use the existing SDK/manager fixture and exercise all 16 publication boundaries; these are not installed commercial results.

Local results and exact source fingerprints are retained under [evidence/ap15](../evidence/ap15/README.md). Windows editor tests were extended for close during opening, repeated vendor/DAW close, stale focus/close, GUI failure, unexpected window loss, epoch exhaustion and the shared standalone helper, but require the normal Windows lane. Neither that source nor a Linux test substitutes for the requested Windows/Bitwig evidence.

The installed AP14 manager, host, vendor modules, revision-3 profiles, publication links and project files remain unchanged. The disposable AP15 project copy was closed without saving observation edits. The service/keeper remain active, with no DSP lease or pending publication. A runtime-only CPUWeight=10000 override appeared after the initial readback; its writer is unidentified. It was reset to the baseline unset setting, physically read back as cpu.weight=100, without restarting the service. No performance result is derived from this observation interval. Heavy tracing is off; the temporary debugger and build container are retired. 512 remains supported/recommended and selected; 256 remains unqualified.

## Source repair checkpoint — Windows capacity deferred

The source-hardening pass requested against `dd596b3e1031655e844d4ee7438d6c564f6db7e7` repairs seven ownership gaps before spending future Windows capacity. It does not qualify the experimental delegate on Bitwig. Previous observation, builds, failures and CI-start receipts remain unchanged under `evidence/ap15/`; this pass is recorded separately under `evidence/ap15/source-repair/`.

The controller now prepares an Open without advancing its lifecycle, and commits ownership only after the processor returns actual GUI-mailbox acceptance. Lifecycle command acceptance no longer depends on allocating a second reply message after the mailbox was already changed. Before Open, the controller reserves one retirement IMessage. Close needs no new message allocation; a refused send retains one exact pending command and controller reference. Disconnect/terminate refuse to discard it. The UI timer retries at most eight sends, then requests explicit GUI-only failure retirement with the reserved message. A host refusing even containment leaves removal refused and the single owner retained; it is not reported as cleanup. No retry, allocation, messaging or X11 operation enters audio processing.

XCB retirement now checks the send and flush before recording acceptance. Three failed transient sends are the maximum; an accepted send is never repeated. A flush failure after server acceptance is ambiguous delivery and stops resends. BadWindow/parent destruction, connection loss and other server refusal have distinct mechanical outcomes. Unmap failure triggers exact editor failure/retirement rather than pretending the shell is hidden. After one accepted WM_DELETE request, failure of the host to retire within 100 UI ticks is reported as refusal, with no duplicate request. XCB acceptance is not host application acknowledgement; actual Bitwig behavior remains pending. The bridge never destroys the host's parent.

Both native UI timers use an independent registration handler. Unregister refusal retains the exact registration and returns failed removal. The delegate retains its resources for retry. If a host releases a refused view, its destructor detaches the handler's callback before releasing the view; a small inert handler/loop receipt remains until unregister succeeds. It cannot call a destroyed delegate. Successful unregister releases the registration once, including hosts which do not retain handler references. A broken run loop which permanently refuses unregister can retain this bounded receipt; claiming full cleanup in that case would be false.

A processing/UI replacement records a monotonic retired-token watermark rather than erasing the old attached view's terminal projection. Old view A continues to observe `EditorFailed` and requests normal host-shell retirement; its removal acknowledges the already-retired old mapping locally. New view B uses a greater native token and a fresh activation context. Stale A Open, Close, Focus, status or scoped edit traffic cannot validate B.

Windows source now retains the exact HWND until `DestroyWindow` succeeds or `WM_NCDESTROY` confirms destruction. The callback clears GWLP_USERDATA even during intentional teardown, and intentional destruction is not `WindowLost`. Failed destruction retains ownership and refuses a replacement; retry does not double-count successful SDK `removed()` calls. The standalone vendor-access route still uses this mechanical view helper with its own admission/receipt and no DAW/project authority. These changes and their Windows fault-injection tests are **uncompiled and unrun** in this pass.

### Callback identity classification

| Windows-to-native traffic | Scope and retirement law |
| --- | --- |
| Begin, Value, End, GroupBegin, GroupEnd, Dirty, RequestOpen from an editor-installed handler | Immutable native view token and vendor epoch. A separate handler is installed before that editor is created. Close removes the view, completes accepted gestures, retires the handler and restores the instance handler. Cached old handler references become inert; queued old packets are refused by the native lifecycle. |
| The same callbacks from the persistent instance handler | Explicit zero/zero origin; legitimate controller-instance activity remains available without an editor. Gesture/group ownership cannot mix origins. |
| EditorStatus and Focus | Existing exact native token, editor epoch and activation identity. |
| RefreshBegin, Parameter, RefreshEnd | Instance scoped. A current editor's accepted restart may request a full controller refresh, but the resulting metadata/readback is instance data and is not stamped as an editor gesture. A retired handler cannot request a new restart. |
| Latency/topology restart notifications and host-originated Set | Existing instance policy, revisions and host ownership. No editor generation is invented. |

UI ABI **4** and mapped UI protocol **5** explicitly introduce callback-origin semantics; message/header extents remain 608/320 bytes. Both language boundaries reject the prior semantic versions. The diagnostic reader retains its bounded support for historical layouts and recognizes version 5. Audio IPC and state-envelope versions are unchanged. This protects cached handler references and queued traffic; it does not claim to identify arbitrary vendor-internal work which deliberately calls a replacement handler as if it were current.

Retained-host admission now checks retained claim authority on both same-host and cross-version routes. Verified activation claims remain eligible. A review candidate needs the exact `ap15_editor` marker, direct-editor capability, compiled/sealed product artifact identity, complete current census/module/environment/runner bindings, external IDs, physical active target, and exact retained verified revision-3 parent. Rollbackability of historical revision 2 does not grant serving authority. The finite candidate roster is still absent, so no production AP15 candidate can be served or published in this pass. Ordinary managed preview/publish remain verified-only.

The checkpoint is source hardening with Windows build and Bitwig qualification blocked. It is not AP15 acceptance or review readiness. The exact revision-3 installation, projects, keeper/service and 512-frame selection are preserved; 256 remains unqualified.

Available repair results: backend 57 tests, manager 31 library + 3 CLI tests, both all-target clippy checks, Linux runtime 15 tests and native CMake/CTest 6/6 pass. Both registered native products were rebuilt from source `b182449c8127371cac478af6a9192aade3e99ce4`, without installation. Exact commands, hashes, intermediate failures and unchanged installed readback are in [the separate repair evidence](../evidence/ap15/source-repair/README.md). Windows source and tests remain uncompiled/unrun; no candidate roster or Bitwig qualification was added.

## Focused R2 — independent XCB operation outcomes

[Technical review 5160946354](https://github.com/kasselvania/Linux-VST-bridge/pull/86#pullrequestreview-5160946354) found that a rejected unmap incorrectly supplied the terminal guard for host-close delivery. The prior one-shot unmap fixture could also recover on a later unmap and overwrite that shared result, hiding the defect. Historical source-repair evidence above remains unchanged.

`VendorPanel` now retains separate unmap, SendClose and flush results. Parent disappearance and connection loss are independent terminal delivery facts; the diagnostic failure result does not authorize or suppress other operations. A rejected unmap stops further unmap attempts; three transient refusals exhaust its existing bound. Both report the exact editor failure once and still attempt checked WM_DELETE against a live parent. Only SendClose's own rejection/exhaustion, an already accepted send, ParentGone or ConnectionLost suppress subsequent host-close sends. The bridge never destroys the host-owned parent.

Checked X-server acceptance commits the one close delivery before the subsequent flush. An ambiguous flush therefore cannot cause a resend or erase accepted delivery. Application acknowledgement remains distinct; the existing 100-tick host-retirement refusal is unchanged. Controller command ownership, timer removal/retry, editor generations, Windows teardown, qualification, callback identity, state, transport and performance are unchanged.

The exact same final `panel.cpp` fixture fails with the reviewed `f359240` production header and passes with the repaired header. Persistent rejected unmap produces one exact parent/protocol WM_DELETE, one editor failure and no later duplicate. Normal host removal balances the editor close, timer, frame/loop and controller references; the surviving parent's event-mask readback proves the delegate XCB connection's selection retired. Additional focused assertions cover exhausted transient unmap and conclusive SendClose rejection. Existing transient send retry, connection loss, parent disappearance, ambiguous post-send flush, failed unregister, destructor-safe inert timer and no-child/control cases remain.

[R2 evidence](../evidence/ap15/r2/README.md) binds the before/after test, available local validation, rebuilt Linux artifacts and unchanged revision-3 installed readback. This proves SDK/Xvfb mechanics only. Windows compilation/tests and actual candidate Bitwig qualification remain blocked and unclaimed. No candidate profile/roster, activation, publication or installed AP15 support is added. 512 remains selected/supported/recommended; 256 remains unqualified. PR #86 remains draft, open and unmerged.

### First actual revision-4 qualification: native attachment rejected

Normally Applications-launched Bitwig loaded both exact candidates into the saved protected chain, but rejected the 1×1 SDK size and reported that no child had been created inside its editor parent. A bounded exact-symbol observation shows successful `attached()` followed by `removed()` about one second later. Windows had opened the matching vendor epoch and then acknowledged the exact DAW close; the DSP sessions remained alive. This is a failed direct-editor mechanism, not qualification success. No musical or latency claim comes from this stopped/debugged interval. Both exact revision-3 physical targets were restored through `qualify-editor restore` after normal project quit; no DSP lease remained.

The narrow native repair supplies 64×64 SDK attachment geometry and one initially unmapped child with no event selection, background or controls. The private XCB connection owns the child, and normal timer-safe removal retires it without destroying the host parent. The new production fixture fails against the previous candidate and passes with the change; all six Linux CTest fixtures pass. Actual Bitwig acceptance of the replacement remains pending. Revision 4 and its artifact/publication history remain immutable. See `evidence/ap15/qualification/attachment-rejection.json` and `attachment-native-regression.json`.

### Revision-5 host result and activation repair

Bitwig accepted the 64×64 child, mapped that child itself, and left it unviewable under the delegate-unmapped host shell. One LoFi vendor close retired epoch 1; the next normal Bitwig action opened epoch 2 in the same DSP session. A DAW close retired epoch 2. The local output sink remained nonzero across vendor close and the sibling remained active. The replacement still failed the foreground requirement on reopen: Bitwig remained active while the new LoFi window was behind it. Both original revision-3 targets were restored after this interval.

Direct X11 readback showed Bitwig supplies no `_NET_WM_USER_TIME` or `_NET_WM_USER_TIME_WINDOW`; the native helper had refused the observed zero timestamp before sending an activation request. The repair retains zero as unknown/CurrentTime, sends the normal application-source request only while the exact initiating window remains active, and preserves the existing deadline, generation checks, desktop refusal and foreground-plus-keyboard verification. It invents no click timestamp or pager authority. This uses the [EWMH active-window request](https://specifications.freedesktop.org/wm/1.5/ar01s03.html); [KWin's handling](https://github.com/KDE/kwin/blob/Plasma/6.4/src/netinfo.cpp) explicitly accepts CurrentTime and considers the requestor window. No KWin code is copied. Actual focus acceptance of the repaired candidate remains to be observed.


## Revision-6 exact-fixture qualification

The final native images were built at `a3cb624f126afc5270519012d7af39485fd6fbb6`; profile/manager source is `f1df1f780d988d1e419ff48f835dcb949d5e1a0b`. The original Windows artifact remains exact because no Windows-owned code/test/shared ABI changed. Its manifest retains build commit `46c7b85fce2370644cccec6197a6f1a06c188be1`, reviewed head `0d82c75` and common tree `9d9ea7de6111a32e8359187c88802deff8f005fb`. Final Linux identities are in [focus-artifacts.json](../evidence/ap15/qualification/focus-artifacts.json); the sealed package and actual local/hosted checks are in [validation.json](../evidence/ap15/qualification/validation.json).

On SteamOS 3.8.16 / Bitwig 6.1 / the unchanged pinned Proton-SLR Arturia environment, both devices opened their real editor directly from the normal editor button. No ordinary bridge controls or persistent blank surface were visible. Native readback confirmed both foreground and keyboard focus. LoFi completed four editor generations and FRAGMENTS three in their unchanged initial DSP sessions. Vendor close retired the exact epoch; a centered normal editor action reopened a greater epoch. DAW-toggle close retired the current editor without retiring DSP. Two vendor editors coexisted without duplication. Some remote clicks near the tiny toggle edge emitted no Open; these input ambiguities are retained and excluded from successful single-action trials. The successful LoFi epoch-2 and FRAGMENTS epoch-3 reopen trials each used one normal action.

The selected attachment shape is 64×64 SDK geometry with one initially unmapped native-owned child. Bitwig maps that child; it stays unviewable beneath the delegate-unmapped host parent. The child has no paint, event mask, labels or controls. The parent remains Bitwig-owned and is never destroyed by the bridge. WM_DELETE acceptance/retirement and the run-loop timer retain the reviewed bounded failure laws. The initial exact host sequence and failed candidate sequence are retained; neither guessed geometry nor Xvfb mechanics alone is called Bitwig qualification. OnFocus/checkSizeConstraint calls not observed in a particular host interval are not invented. No high-speed no-flash or compositor frame-rate claim is made.

A real LoFi cutoff gesture produced parameter 99 = 0.7200000882 and FRAGMENTS FX produced parameter 5 = 0.3240000010, each as editor-scoped Begin → Value → End. Bitwig reflected both. Existing LoFi Amount and Grain Mix automation played hands-off, with alternating instance-scoped Set values delivered to the vendor controllers and visible control/audio activity. Explicit File → Save while stopped succeeded with the LoFi editor still open. After normal quit and Applications relaunch, both devices/editors returned with cutoff 0.7200 and Macro 2 0.3240. Removing FRAGMENTS with its editor open left the same LoFi DSP/editor and local output active. That removal was discarded, retaining the saved edited two-device copy.

[Lifecycle and cleanup receipts](../evidence/ap15/qualification/lifecycle-and-cleanup.json) retain processing generation, native token, activation, vendor epoch, focus status, editor summaries and component-termination order. All four DSP sessions report cleanup and transport retirement. First-session summaries are LoFi 4 opens/4 closes/4 confirmed focuses and FRAGMENTS 3/3/3; recall sessions are 1/1/1 each. No duplicate vendor window or final DSP lease remained. The actual revision-4 attachment refusal established editor-failure isolation; deterministic production SDK/native fixtures retain deliberate stale/failure coverage. No additional vendor-file or licensing fault was injected into the commercial installation. The standalone inspection route is unchanged by these Linux-only repairs, with its existing Windows ownership fixture retained.

This is a lifecycle result, not a delivery improvement: the first interval recorded LoFi 7 gaps / 6,656 missed frames and FRAGMENTS 6 / 6,400; recall/removal recorded 5 / 1,536 and 1 / 256 respectively. All report fault=0 and clean retirement. Host maximum was 512, internal maximum chunk 256, and bridge delay 512 per proxy at 48 kHz/float32. Reported chain latency is 1,264 frames = 26.333 ms (1,024 bridge + 48 LoFi + 192 FRAGMENTS vendor frames), excluding device/acoustic latency. Moonlight supplied control; local X11 and output-sink readback support the bounded observations. No new reliability, CPU or latency promotion claim is made.

`qualify-editor restore` returned both physical links, revision fingerprints, native artifacts and prior ancestry exactly to verified revision 3. Original projects/modules/authorization were preserved; the saved AP15 copy hash is `6a10e5446e8411dee0fca8925bae716b992de9d3619ba5b2334408c383f7202c`. The updated manager remains installed with the original AP14 ordinary Windows host. Service and keeper are active, DSP leases/pending transactions are empty, heavy tracing/debugger are off, CPUWeight is unset/effective 100, and 512 remains selected/supported/recommended. Earlier unexplained CPUWeight overrides remain recorded and were restored; no confounded performance claim is derived. [Exact installed readback](../evidence/ap15/qualification/installed-final.json) binds the candidate profiles, transactions, targets and restored parents.

Revision 6 remains `review_candidate` and only the sealed engineering route can activate it. Revision 3 is unchanged ordinary support. Independent review must select a later immutable verified revision; this pass does not approve itself or merge PR #86. FRAGMENTS Advanced redraw #80, residual gaps #72, other environments/vendors/DAWs/precision, and 256-frame qualification remain outside the claim.
