# AP18 — Arturia Software Center to Pigments

## Decision

AP18 closes the first official vendor-managed installation vertical. The bridge already runs and manages exact installed Arturia VST3s; it does not yet own the path by which a user obtains, authorizes, installs, discovers, and publishes one through Arturia's normal software.

The selected result is:

```text
official Arturia Software Center installer
→ managed Arturia environment
→ real ASC application and user-owned account flow
→ Pigments download and installation
→ exact automatic discovery and registration
→ reversible Pigments candidate publication
→ real Bitwig qualification
```

This is intentionally one end-to-end product slice. Internal gates may fail honestly, but AP18 should not be split into disconnected “ASC launches,” “files appeared,” and “Pigments maybe works” milestones that never complete the user journey.

## Integrated basis

AP18 begins from integrated AP17 main:

```text
2329706a6e797137e68d719edbbbe5cc1e0cdbf1
```

Accepted foundations:

- native Linux VST3 proxy and supervised Windows VST3 host;
- persistent exact Proton-SLR Arturia environment;
- automatic service and keeper ownership;
- private tmpfs audio/editor/state transport;
- direct detached vendor-editor lifecycle;
- exact opaque state, automation, save/reopen, publication and rollback laws;
- typed startup admission;
- six-instance exact-fixture operating envelope;
- revision 10 ordinary source authority, revision 7 immediate rollback, revision 3 retained ancestry;
- 512 added frames selected/supported/recommended;
- 256 available but unqualified.

AP17's one historical Bitwig quit anomaly is retained and nonreproduced. It is not an AP18 gate. Residual delivery issue #90 and lease-retirement follow-up #93 remain separate.

## Existing code to reuse

### Environment ownership

`bridge-manager/src/main.rs` already creates/imports exact persistent environments and verifies their pinned runner. The accepted Arturia environment is a vendor-family state container rather than a disposable one-process prefix.

### Installer launch

The current CLI already exposes:

```text
linux-vst-bridge install ENV_ID INSTALLER_PATH SHA256
```

It canonicalizes and verifies the installer, writes a private installation job, and launches the supervisor through a bounded user systemd unit.

`bridge-manager/runtime/session.py::install` currently:

- obtains the environment operation lock;
- verifies the installer and runner files;
- launches the installer through the exact Proton runner;
- tracks the launcher and descendants;
- bounds captured stdout/stderr;
- enforces a one-hour operation deadline;
- contains owned processes on exit/failure;
- writes a private report with launcher result, cleanup, and diagnostics.

That is useful but insufficient for ASC. It treats installation as one foreground process whose completion ends the operation. ASC is a persistent companion application that may start helpers and later run long downloads/installations after its initial installer has exited.

### Vendor application access

The service already supports a separate vendor-access owner for one plug-in editor. AP18 may reuse its environment exclusivity and ownership ideas, but ASC is not a plug-in editor and must not be forced through a fake VST3 class request.

### Module inspection and publication

The manager already verifies exact modules inside the environment, runs supervised Windows factory/class inspection, derives registrations, builds native proxy artifacts through descriptor-owned routes, publishes immutable revisions, and restores exact parents.

AP18 should compose these accepted pieces. It should not create a second independent environment registry, scanner, publisher, or process owner.

## New product records

Private representations are implementation choices, but the following meanings need one canonical Rust owner.

### Installer artifact

At minimum:

```text
installer id/digest
original filename
private retained path
byte size
file type
available publisher/signature facts
operator source assertion
admitted environment
first-use timestamp
```

The installer bytes remain private and uncommitted.

### Vendor application

At minimum:

```text
vendor application id
vendor family
exact environment/revision
installed executable identity
application/version metadata
known helper/service identities
launch policy
observed data/cache/download/resource roots
last operation/result
```

No arbitrary executable path supplied at launch time may become vendor-application authority.

### Vendor operation

The manager must distinguish observable states such as:

```text
prepared
launched
waiting_for_user
running
product_downloading
product_installing
completed
failed
cancelled
cleanup_unconfirmed
```

Do not invent a state solely from window title text or launcher exit. Unknown is a valid result.

### Installed vendor product observation

For Pigments, retain non-secret facts such as:

```text
product identity/name
observed ASC status
installed VST3 module path/digest
factory/class census
standalone executable identity where present
resource/content/preset roots
safe registry/file manifest deltas
authorization posture: confirmed/required/unknown, without payload
```

An observation becomes a bridge compatibility claim only through a separate profile and qualification result.

## Installer intake

The operator has placed unopened ASC installer file(s) in `$HOME/Downloads` on the Steam Deck.

Intake must be bounded:

1. Enumerate only plausible regular files in the immediate Downloads directory; do not recursively census unrelated personal files.
2. Preserve original filename and bytes.
3. Calculate SHA-256 and size.
4. Identify PE/MSI/container type without execution.
5. Read Authenticode publisher/signature facts when supported; inability to validate a signature must be reported rather than silently converted to trust.
6. If multiple distinct ASC installers remain plausible, stop before execution and present their bounded identities for operator selection.
7. Copy the selected exact file to a private content-addressed manager artifact location with restrictive permissions.
8. Verify the copy byte-for-byte before use.

Do not upload, commit, unpack into the repository, rename or delete the original, or fetch a substitute installer from an arbitrary URL.

## Pre-install transaction

ASC will mutate the existing Arturia environment. Before execution:

- close Bitwig;
- require zero DSP leases and no pending publication/qualification transaction;
- require no unresolved vendor/install operation;
- read current service/keeper and environment health;
- activate accepted ordinary revision 10 once and verify physical publications;
- retain exact LoFi/FRAGMENTS modules, profiles, publications, external IDs, resource roots, authorization posture, and protected-project hashes;
- measure environment size, free storage, and expected temporary headroom;
- capture an allowlisted filesystem and safe-registry baseline;
- create a private, non-running rollback snapshot or exact backup of every root ASC is allowed to modify.

The backup is recovery material, not another runnable authorized environment. Do not launch it or treat it as a second licensed device.

If full-prefix copying is selected for rollback, account for Pigments/ASC download size and disk pressure before starting. Prefer a filesystem snapshot/reflink or bounded changed-root backup when available and trustworthy.

## Installing ASC

Use the current installer command as the starting owner. Extend it only where the real installer proves necessary.

Success requires more than launcher exit zero:

- expected ASC executable exists in the exact environment;
- executable identity and installed version are retained;
- required helpers/services are identified;
- no unresolved installer process remains;
- relevant before/after roots are recorded;
- the application can be launched again without the installer;
- rollback can identify what the transaction changed.

Keep ASC and vendor infrastructure in the same Arturia environment as LoFi, FRAGMENTS, and Pigments unless actual incompatibility demonstrates a need for a new environment family.

## ASC application owner

Implement a narrow product-owned companion-application route rather than an arbitrary Wine command.

The manager should be able to:

```text
asc status
asc launch-or-focus
asc stop-owned-operation   # only where safe and necessary
asc rescan-products
```

Exact CLI spelling is an implementation detail. The meanings are not.

Requirements:

- launch only the registered exact ASC application in the registered exact environment;
- preserve foreground UI and modal children;
- retain process-tree ownership by PID plus start identity privately;
- distinguish application UI, vendor services, and active product-install helpers;
- avoid killing helpers required for a completed installation or future authorization;
- block conflicting environment mutation/inspection and live DSP as required by existing laws;
- survive launcher handoff when another helper becomes the visible/continuing owner;
- permit normal close and later relaunch;
- report unresolved ownership honestly.

Do not make ASC an environment keeper replacement. The existing keeper has a different job and loads no vendor application.

## Account interaction

Credential handling is a hard boundary:

- the user interacts directly with ASC;
- the agent may position or focus the real application but must not inspect secret text fields, clipboard contents, cookies, tokens, account emails, serials, or license payloads;
- no screenshot or committed log may expose account identity or authorization material;
- pause at login, MFA, consent, purchase, license choice, or other sensitive actions;
- resume only after the operator states the action is complete.

The system may observe non-secret resulting application/product state, but it may not reverse-engineer or automate Arturia's private service protocol.

## ASC usability and manager UI decision

The real ASC UI is the preferred product surface for account, catalogue, download, installation, and activation.

Do not build a manager UI merely because a UI is planned eventually.

A bridge-owned frontend is justified only if a demonstrated problem prevents the flow, such as:

- ASC cannot be found or relaunched reliably;
- its window opens behind other applications and cannot be recovered through normal activation;
- launcher exit loses continuing download/install ownership;
- the user cannot tell whether bridge-owned discovery/publication has caught up;
- a rescan or recovery action has no usable existing surface.

The minimal surface may expose:

```text
Arturia Software Center: installed / running / needs attention
Launch or focus
Current vendor operation
Detected products
Rescan
Pigments: installed / candidate / published / failed
Exact non-secret error and next action
```

It must read canonical manager state. It may not become a second store, login form, license service, or profile authority.

## Arturia paths and content

Do not assume ASC installs only a `.vst3` file. Observe at least:

- VST3 module/bundle;
- standalone Pigments application;
- shared Arturia Resources;
- factory presets, samples, and databases;
- per-user configuration/preferences;
- program-data and download cache;
- helper/service data;
- authorization posture.

Keep vendor paths stable and Windows-native inside `C:` first. External Linux bind paths can introduce drive/remount and graphics/resource behavior that the project has not qualified. Use them only if real capacity or compatibility evidence requires them.

Do not index or commit paid content. Product/resource discovery retains identity, count, bounded metadata, and roots—not transferable content.

## Post-install discovery

A generic “find Pigments.vst3 by filename” is insufficient.

The manager should:

1. compare the allowlisted pre/post installation manifests;
2. identify new or changed VST3 module candidates;
3. verify each candidate is inside the exact environment;
4. run the existing supervised factory/class census;
5. select the Pigments class from exact factory/class metadata;
6. retain module digest, class ID, vendor, name, version, role, subcategories, buses, parameters, state capability, latency, and editor facts available from the scanner;
7. refuse ambiguity or multiple plausible classes until resolved;
8. derive the source-owned native descriptor and stable external IDs;
9. build an exact native proxy;
10. form one immutable `review_candidate` profile.

The profile should contain no installer command, ASC executable command, credential path, mutable download root, or user-entered class ID.

## Capacity-policy extension

AP17's class limits are an exact LoFi/FRAGMENTS fixture, not a generic two-entry maximum and not a universal Arturia table.

Pigments requires a versioned policy extension that preserves:

- service workers 16;
- global DSP envelope 6 unless new evidence explicitly changes it;
- LoFi maximum 3;
- FRAGMENTS maximum 4;
- native-image hard ceiling 4;
- maintenance ownership;
- exact over-capacity refusal.

Begin Pigments at a class limit of one for qualification. Do not rerun the AP17 maximum matrix or claim a six-instance Pigments project. A later capacity slice may broaden the class envelope.

## Candidate publication

Use one sealed AP18 qualification route for the exact Pigments candidate. It may accept a private package location, but every artifact/profile/module/class/environment/parent identity must be compiled or otherwise closed by source-owned policy before mutation.

It must not accept arbitrary:

- profiles;
- module/native/host paths;
- hashes;
- class IDs;
- environment IDs;
- commands;
- downloads;
- vendor application executables.

LoFi and FRAGMENTS ordinary revision 10 remain active and unchanged. Pigments begins as engineering-only authority and is restored/unpublished after the campaign pending independent review.

## Pigments qualification scope

The first qualification asks whether Pigments is a usable exact VST3 instrument through this bridge—not whether every Pigments feature is supported.

Required real checks:

- exact candidate is discoverable in Bitwig;
- one instance admits and processes notes;
- local output is nonzero;
- real editor opens directly, navigates, resizes, closes, and reopens on the same DSP instance;
- one factory preset loads through Pigments' own browser;
- one real parameter gesture reaches Bitwig as begin/value/end;
- host automation replays into sound and UI;
- protected project save/reopen restores intended preset and visible state;
- opaque state capture/restore is classified truthfully;
- one accepted Arturia sibling remains healthy alongside Pigments;
- Pigments removal retires only its own editor/process/transport/state;
- normal Bitwig quit leaves positive ownership cleanup.

Retain the exact Pigments-reported latency, buses, sample-size support, parameter count, state size, editor scaling/rendering limitations, and delivery gaps observed.

Not required for AP18:

- every preset;
- every synth engine;
- MPE qualification;
- sidechain/external audio input;
- float64;
- exhaustive popup/file-dialog behavior;
- multiple Pigments instances;
- 256 frames;
- universal Arturia compatibility.

## Existing products and vendor updates

ASC can update existing products or shared resources. The operator should select only Pigments.

After installation, compare LoFi and FRAGMENTS exact modules and critical shared roots against baseline. If ASC changed them despite operator selection:

- retain the exact private delta;
- do not publish changed bytes under revision 10;
- determine whether the change is shared-resource-only, module update, authorization change, or another vendor mutation;
- restore baseline if the change breaks accepted projects;
- create a separate immutable candidate/update transition only when needed.

A successful Pigments installation must not silently invalidate existing accepted Arturia projects.

## Failure and recovery

At each phase, preserve a useful stopping point:

- installer rejected before execution;
- ASC install partial;
- ASC launches but sign-in is blocked;
- authorization unavailable;
- Pigments download interrupted;
- download complete but installation failed;
- module installed but census fails;
- census succeeds but bridge feature unsupported;
- candidate publishes but Bitwig fails;
- project works but cleanup or state fails.

Do not erase partial vendor state until its ownership and recovery implications are understood. Prefer the vendor's own resume/repair/uninstall mechanism where available. The private baseline snapshot remains the last-resort exact rollback.

## Validation

Run tests for changed owners only, plus the normal final-head lanes.

At minimum when corresponding code changes:

```text
cargo test --manifest-path bridge-manager/Cargo.toml --locked
cargo clippy --manifest-path bridge-manager/Cargo.toml --locked --all-targets -- -D warnings
python3 -m unittest discover -s bridge-manager/runtime -v
```

Run native backend/audio-client/CMake tests when Pigments proxy or native startup code changes. Run Windows host CI when Windows source, Windows tests, shared ABI, scanner behavior, or host inputs change.

Use actual ASC and Pigments evidence for claims synthetic tests cannot establish. Do not repeat AP17's six-device, reboot, service-loss, and capacity campaigns.

## Final handoff

The implementation PR should report:

- exact final head, parent, and tree;
- selected installer metadata and private artifact identity, without bytes/path disclosure;
- exact environment and rollback posture;
- ASC installation and application ownership;
- user-owned authorization result without secrets;
- actual Pigments product/module/resource observations;
- automatic census and profile derivation;
- native/Windows/profile/publication identities;
- Bitwig editor/preset/audio/automation/state/save-reopen/sibling/removal result;
- existing-product preservation;
- local and hosted validation;
- final installed physical state;
- failures, limitations, and nonclaims;
- confirmation that 512 remains recommended and 256 unqualified.

## Initial installed preflight

At prepared head `5708192ae91983d6a2c37bc9b7a30c4002efa32d`, ordinary
`managed preview`, `managed publish`, and physical status verification activated
both accepted revision-10 products using the already installed AP17 software.
Each retains its exact revision-7 parent and revision-3 ancestry. No capacity or
recovery campaign was repeated.

The immediate Downloads check found ASC 2.12.0.3157 as a macOS `.pkg` (xar),
not a Windows installer. It was neither executed nor changed. The operator was
asked to provide the official Windows installer. ASC/Pigments installation and
qualification remain pending. The environment has ample measured disk headroom;
a consistent private rollback backup must still be created after normal keeper
retirement and before installation. See [preflight](../evidence/ap18/preflight.json)
for bounded identities and final physical readback. PR #95 remains draft.

## Windows ASC installation continuation

The operator supplied the official Windows ASC 2.12.0.3157 installer. Its exact
private copy was verified, and the inactive environment was backed up before
execution. Every archived regular file was compared by SHA-256 and every symlink
target was compared without running the backup. ASC was already present; the
new installation changed its main executable, agent, updater and related program
files. The operator completed setup; the existing installer owner reported exit
0 and positive cleanup. Both accepted VST3 module hashes remain unchanged.

The first companion-owner implementation introduces an exact ASC application
record and launch/status/rescan/cancel commands. It does not accept a caller's
executable path, claim a DSP instance, record vendor stdout, or terminate live
helpers merely because the launcher exited. This owner is still being tested;
actual launch/focus/relaunch and the Pigments vertical are not yet established.
See [installation checkpoint](../evidence/ap18/asc-installation.json). Account
posture remains unknown, and all credential interaction remains operator-owned.

## Post-login blocker: bounded stopping point

The three registered launches all ended with launcher exit 5 and positive
cleanup after the operator reported that the library window did not appear
following sign-in. Exit 5 identifies the launcher result; it does not establish
an ASC UI error, invalid credentials, a crash, or which process caused failure.

The installed vendor shortcuts specify the executable directory as their working
directory, with no arguments. Commit `a3ab31c907676c6334e0b0c862e0047d0ecee56f`
makes the companion launch match that contract. The actual main process used the
correct directory, but the third attempt still failed identically. This corrects
the launch contract; it is not a fix for the observed post-login failure. A
deterministic command-shape test, 24 Linux runtime tests, 53 manager library and
8 binary tests, clippy and the Linux release build passed. Those results do not
qualify ASC or Pigments.

The bounded check inspected main/agent PE imports, file versions, explicit Wine
version settings, ASC-specific accessibility settings, vendor-named service
configuration and matching coredump availability. It did not identify the
failure owner. The current owner discarded 529 diagnostic bytes on each attempt,
so their contents cannot be recovered from these receipts. No account logs or
authorization databases were inspected. Retrying login without fixing that
diagnostic gap would not test a new hypothesis.

Further retries are stopped for a focused technical handoff. The next missing
observation is the main/agent/launcher exit sequence and a non-secret failure
origin at that boundary. Capture only allowlisted process/runtime facts before
asking the operator to repeat sign-in. Do not infer a runtime defect, delete
preferences, change authorization or reinstall from these results.

[The blocked checkpoint](../evidence/ap18/asc-login-blocker.json) retains all
three receipts, the unsuccessful comparison, exact installed software and
bounded physical readback. LoFi and FRAGMENTS remain valid ordinary revision 10
with unchanged physical targets and ancestry; checked project hashes match.
The service is active, with zero DSP or keeper leases, no pending transaction,
no tmpfs session directories, no active ASC owner, and tracing off. The keeper
was retired for the exclusive ASC operation and is lazily created by the
service; it is not claimed running at this checkpoint. CPUWeight is unset with
effective value 100. The installer and private inactive rollback remain
available. Pigments has not been acquired, censused, built or qualified; the
companion owner's full failure/handoff coverage, focus route and canonical
installer admission integration are also unfinished. PR #95 remains draft.

## Focused post-login diagnosis: fatal main-process exception

The instrumented operator attempt captured `0xC0000005` in the ASC main process,
followed by `NtTerminateProcess(..., -1073741819)`. The outer launcher returned
5. This is now evidence of an access-violation termination for **this attempt**,
not an Arturia UI error code or proof about the three earlier discarded logs.
The fault IP is retained, but the diagnostic did not retain the loaded module
ranges required to resolve that relocated address to a DLL/function. A scan of
pinned-runtime PE preferred bases did not resolve it. No dependency, graphics,
authorization or runner change is justified by that missing attribution.

The exact main and Agent were both alive before the operator signed in. Main
disappearance was observed about 42.029 seconds after the first journal event;
Agent disappearance was about 71.441 seconds. The supervisor stayed alive until
the cgroup was empty, then returned a truthful failed result with positive
cleanup. It sent no cancellation/containment signal during this attempt. Thus
this failure is not explained by the supervisor removing a continuing Agent or
replacement main. No surviving invisible library window remained. The Agent's
IPC semantics and authentication result remain unknown.

The companion owner now observes its exact dedicated systemd cgroup, including
unrecognized members, rather than making Linux descendant sampling completion
authority. PID/start identity and mapped device/inode identify actual registered
PE images across runner path aliases. Outer exit is a handoff observation while
any member remains. The operation lock survives the handoff; explicit
cancellation uses identity-checked pidfds against this cgroup only. KillMode
remains `control-group`. These changes repair a demonstrated source ownership
gap, **not the captured ASC crash**. Audio/session supervision is unchanged.

Private stdout, stderr and process journals are separate mode-0600 files in a
mode-0700 directory. Each retains at most 2 MiB for 600 seconds; streams continue
to drain with discarded-byte accounting. Current/group process bounds are
4096 identities and 64 cgroups. Diagnostic Wine channels cover process, SEH and
DLL import errors. The pinned Proton's `PROTON_LOG=1` opens an unbounded merged
file, so the selected route keeps that redirection off, uses a private
`PROTON_LOG_DIR`, and collects Wine output through the bounded pipes. Public
status exposes counts and fixed error categories, not private paths or output.
This is a bounded diagnostic surface, not permanent audio profiling.

Static inspection found two main-executable shortcuts, no arguments, the ASC
working directory, and no separate matching Run/RunOnce/Startup/service/task
Agent registration. Main requests administrator execution; Agent/updater request
`asInvoker`. One Agent-only probe created local socket endpoints and exited
naturally with status 0 and positive cleanup. No IPC payload was inspected.
Both one non-login `runinprefix` probe and one full-initialization `run` probe
created exactly one mapped main and Agent and a visible login window; both were
cancelled with positive cleanup. This does not establish a post-login benefit
from changing runner invocation, so the final attempt used `runinprefix`.

Two diagnostic defects found during those probes are retained honestly: initial
argument matching overcounted wrappers as main; independent image-map inspection
corrected the observation, and source commit `87c0bdec` removed argument reads.
Also, cancellation after the Agent's natural exit initially reported an absent
collected unit; cancellation now accepts the exact completed receipt
idempotently. Neither correction is presented as an ASC crash repair.

The final attempt retained 177 stdout bytes, 327705 stderr bytes and 18878 process
journal bytes, with zero overflow. Raw contents remain private. `id.ascd` was
absent in the pre-install archive metadata and at subsequent sampled boundaries;
its contents were never read, copied or modified. This is not a claim that a
transient file could never have existed. The operator's account interaction was
not captured. Wine timestamps and Python monotonic timestamps are separate;
only within-domain ordering/elapsed values are used.

All 32 Linux runtime tests passed, including production-owner double-fork exit,
reparenting, unrelated-process survival, exact cancellation and bounded capture.
Manager tests (54 library, 8 binary), all-target clippy, macOS runtime tests
(with 17 Linux skips) and the Linux release build passed. Windows/native/audio
owners did not change and were not rebuilt. See the [observation](../evidence/ap18/post-login/observation.json),
[validation](../evidence/ap18/post-login/validation.json), and
[physical final state](../evidence/ap18/post-login/installed-final.json).

A new immutable manager software revision is installed. Ordinary LoFi/FRAGMENTS
revision 10, exact physical targets, revision-7/3 ancestry, modules and checked
project hashes remain unchanged. Service is active; keeper remains deliberately
retired/lazy, with zero DSP/keeper leases. No active ASC operation, tmpfs session,
pending transaction, debugger, temporary unit or diagnostic process remains.
CPUWeight is unset/effective 100. 512 remains recommended; 256 is unqualified.
ASC's library and Pigments remain blocked. PR #95 remains draft and unmerged;
no further unchanged login retries are authorized.

### Validated attribution follow-up

At the operator's request, commit `498b708` adds bounded loader/unwind channels
before one further login. A source-owned Win32 fixture deliberately faults,
unwinds through two known functions, catches the exception and exits 0 under
the exact pinned runner. The capture resolved `ap18_fault_site + 6` and
`ap18_fault_caller + 15` against the fixture's actual load base and exported
RVAs, with zero overflow. No debugger, memory dump, account UI or arguments/locals
were used. Existing manager/clippy and all 32 Linux runtime tests pass. This
validates the diagnostic mechanism; it does not fix or qualify ASC. See
[attribution preparation](../evidence/ap18/post-login/attribution-preparation.json).
The newly authorized operator-owned attempt is recorded separately when complete.

### Exact attribution and scoped mitigation candidate

The one validated-capture login reproduced the same access violation. Its first
frame is Wine builtin `UIAutomationCore.dll + 0x7988`, with module SHA-256
`33a195f56bd5a84ace582ef480db2e272f21856df6437843eda406af295c6049`.
The installed binary's symbols resolve this to
`create_uia_node_from_elprov + 0x18`; its instruction dereferences RCX, captured
as zero. The next frame is `UiaDisconnectProvider + 0x31`, followed by ASC
frames. Microsoft describes that API as [provider resource cleanup](https://learn.microsoft.com/en-us/windows/win32/api/uiautomationcoreapi/nf-uiautomationcoreapi-uiadisconnectprovider).
This attributes the exact failure path, without assuming that the faulting DLL
alone owns the caller's invalid state or that all runtime versions behave alike.

A second account-free fixture dynamically calls `UiaDisconnectProvider(nullptr)`.
Default execution catches the access violation (fixture exit 42); the exact same
fixture with operation-local `WINEDLLOVERRIDES=uiautomationcore=` receives DLL
unavailable and exits 0. Both test units retire, with no overflow. No Wine/vendor
binary is patched and no global registry setting is changed.

A closed `diagnose-accessibility` mode now allows that exact comparison through
the existing ASC owner. It is not ordinary launch policy. Operation-result
schema 3 explicitly reports `windows_accessibility_disabled`; retained schemas
1/2 remain readable. The limitation is lost Windows accessibility for this ASC
operation and its descendants. Normal launches, plug-in profiles and the pinned
runtime remain unchanged. Real ASC usability with this mitigation remains
pending; see [exact attribution](../evidence/ap18/post-login/uia-attribution.json).

## Pigments installed and automatically discovered

The operator completed normal ASC sign-in using the scoped accessibility
mitigation, selected Pigments, completed installation, and reports activation.
ASC then closed normally with launcher exit 0 and positive empty-cgroup
retirement. Its timed diagnostic retained 679,790 bytes and explicitly discarded
354,993 later bytes; the completed long operation is not a zero-overflow trace.
The ordinary exact ASC launch now uses the demonstrated process-local
accessibility selection, while diagnostic comparison modes remain explicit.

`vendor-product scan pigments` derives the environment from installed ASC and
the native catalogue. It nominates one bounded local module, invokes the existing
supervised `first-audio` SDK inspection, and checks the full factory/class census.
No operator-authored class, hash, compatibility flag or native path is required.
It identified Pigments 7.0.1.6772, Instrument|Synth, 4,446 parameters, float32 only,
44 reported latency samples, and successful 155,038-byte opaque state capture.
Only state size/result were retained, never the payload. Existing module hashes
remain unchanged. See [the census](../evidence/ap18/pigments/census.json).

Pigments exposed a real bus-layout limitation: stereo auxiliary input at index
zero with no main input. The generator and Windows owner now preserve this
inactive bus without falsely requiring a main input. Sidechain activation still
fails explicitly; no sidechain support is claimed. The focused SDK regression
reads this layout from a real SDK AudioEffect and rejects auxiliary activation.
The new Windows host passed the hosted SDK/editor/state lanes; the previous host
remains ordinary LoFi/FRAGMENTS authority.

The generated `arturia-pigments` revision-1 ReviewCandidate is isolated under
`compatibility/ap18`. `qualify-pigments stage EXACT_PRODUCT_PACKAGE` accepts only
its compiled exact artifact bytes and retains exact verified revision-10
baseline references. `qualify-pigments publish` obtains a fresh supervised census
through that exact candidate host and reuses the existing immutable publication
transaction. Pigments is a new class: it has no fictional same-class parent.
`qualify-pigments restore` removes only its discovery publication and retains its
immutable history. Ordinary activation remains verified-only. The engineering
capacity overlay allows one exact Pigments class within the existing global-six
mechanical envelope and does not extend AP17's verified musical capacity claim.
Actual Pigments Bitwig editor/audio/automation/recall qualification remains
pending; a successful scan and build are not those results.

The first successful standalone scan exposed a manager bookkeeping defect: its
supervisor retired the session and wrote a positive receipt, but the new CLI had
not consumed that receipt to release the lease. The subsequent setup preflight
correctly refused `active_lease_unresolved`; no setup mutation occurred. The scan
command now uses the existing PendingAdmission owner, validates the exact stdout
receipt and absent session directory, then removes its own lease. A production
helper regression proves that a receipt with a still-present directory refuses
retirement, followed by successful exact cleanup. The retained first scan was
reconciled by normal service startup only after its positive private receipt and
absent directory were verified. This is separate from the AP17 lease race.

### First native admission and auxiliary default activation

The first normal Bitwig load of Pigments revision 1 was refused before audio:
`ap10_unsupported_bus_activation` identifies audio input 0, the declared
auxiliary Sidechain bus. The native proxy had kept the vendor's default-active
flag while refusing auxiliary activation. This was a contradictory native
capability advertisement, not an authorization failure. State capture and
512/48,000-Hz setup had succeeded; no audio callback ran. The Windows owner and
transport retired positively.

The native bus remains visible with its SDK index, type, name and arrangement.
Its unsupported default-active bit is cleared on the native-facing bus only;
the census and descriptor retain the exact vendor observation. An explicit
auxiliary activation request still fails. No external/sidechain audio support
is claimed. The actual Processor/SDK test fails before this repair and passes
after, including main activation and exact endpoint cleanup. Bitwig behavior
must still be checked separately.

The first immutable Pigments candidate is retained under
`compatibility/ap18/revision-1`. Revision 2 binds the corrected native artifact;
only the exact compiled replacement is permitted after the old candidate has
been unpublished. Neither candidate can authorize ordinary managed activation.


## Sole auxiliary input implementation checkpoint

The operator's explicit input-bus ruling selects one stereo transport lane by
SDK topology: index-zero main input, or sole stereo auxiliary input with no main.
It does not select a source; Bitwig does. Native bus metadata and supported
default activation stay intact. Unsupported additional auxiliaries remain
represented and refuse activation. The Windows owner maps the same setup bus
contract to its processor buffers. No wire-layout change is needed: the existing
contract already identifies media, direction, index, type, activation and stereo
arrangement, and process carries one stereo pair.

The paused stash was inspected and retained. Selected routing hunks were reused
with stricter stereo qualification, explicit Windows mapping and replacement
SDK tests. The source-owned native Processor fixture checks routed channel
order, silence/source removal, inactive null buffers, malformed active buffers,
alias refusal, guards and zero-frame event/parameter delivery. The Windows SDK
fixture checks actual processor input through production BusLayout mapping, plus
main-plus-auxiliary refusal and a no-input instrument. Seven Linux CTest lanes
and 63 backend tests passed, including the bounded input witness.

Revision 1's default-active refusal and revision 2's explicit activation refusal
remain historical failures. At that source checkpoint no new profile/artifact had been installed. The
subsequent revision-3 test and exact restoration are recorded below.

The final input-path Windows workflow `34642762129` passed. Its synthetic
merge commit `e93f1db2637b7eaafbd7af010546770128af4cfd` and source head
`36c02af2425e0bebeb940d7372cf3e10ab1da6ad` share tree
`c91e8a2210fad93bc24576790d3ebad22a894102`; the artifact's source receipt is
unmodified. Pigments revision 3 is generated from that exact host, the matching
native build and the retained supervised census. Revisions 1 and 2 remain
byte-identical history; only exact consecutive replacement is allowed. All
three claims remain ReviewCandidate and cannot ordinary-activate. Manager
tests pass for identity mismatches, mutation refusal, history and replacement.
Actual Bitwig routing and musical qualification remain pending.

## Revision-3 live activation and returned-result blocker

The exact built candidate loaded in normally Applications-launched Bitwig. Its
normal VST sidechain selector showed **No input**, preserving the auxiliary
role. The real Pigments editor appeared with Welcome. No source was selected
and no track signal was substituted. This establishes activation and UI
representation, not end-to-end routed audio.

The Windows processing worker subsequently failed after 18,852 blocks with
`malformed or oversized process results`. Production `AP10Results::Collector`
uses one failure bit for malformed event/parameter content and bounded capacity
refusal; this evidence cannot distinguish them. Processing stopped and joined,
deactivation succeeded, the supervisor reported unsuccessful host close, and
positive containment removed the DSP lease and transport. Two later local
monitor captures were silent. No nonzero note output or routed-input result is
claimed. The terminal snapshot retains one gap / 512 missing and expired frames;
those counters are not a full musical interval. The terminal path did not retain
the Windows input-witness dump, so no two-sided live hash proof is claimed.

Normal quit of the disposable project produced a truthful state-save refusal
after the terminal failure; continuing without that temporary state closed
Bitwig fully. Pigments was removed through its sealed restore route. Exact
ordinary revision-10 targets, revision-7/3 ancestry and protected project hashes
match the baseline. Keeper/service are active; zero DSP leases, no pending
transaction or stale tmpfs session, tracing off, CPUWeight unset/effective 100.
An unexplained 10000 weight was observed earlier and reset; its origin remains
unknown and no performance comparison uses that interval.

The next investigation must retain the first exact collector refusal before
selecting a returned-result repair. Raising capacities, clamping values or
silently dropping output is not justified by this error. The source-routing and
remaining AP18 musical/state campaign are blocked, and PR #95 remains draft.
Exact sanitized results: `evidence/ap18/pigments/input-bus-live-result.json`.

### Returned-result custody continuation

Revision 4's latest attempt exited during editor teardown, after successful
`IPlugView::removed()` and `setFrame(nullptr)`, with the last view stage immediately
before `release()`. The outer launcher returned 5; an exact Windows exception or
terminal code was not retained. No processing-thread join or typed collector
report was emitted. This is independent of revision 3's rejection after 18,852
blocks. The later Bitwig state-save warning followed host absence and is not
an established installation or save-location defect. See
[retained failed attempt](../evidence/ap18/pigments/revision4-teardown-exit.json).

LVRS v1 now retains the first scalar rejection in a 1024-byte session-owned
mapping. The Windows processing worker commits a complete inactive slot after
`process()` returns, before throwing or risky teardown. The Linux supervisor
reads it before containment; post-join JSON is secondary. An empty record means
no rejection was committed, not proof that a pending vendor call returned.
Collector acceptance, capacities, native bytes, sidechain routing, and audio
protocol are unchanged. Production parent/child tests exercise persistent editor
removal/Win32 destruction failure and existing `std::terminate` containment.
The Linux supervisor test verifies persistence precedes cleanup. Revision 4 is
retained byte-for-byte; revision 5 binds the exact tested diagnostic Windows host
and remains an engineering candidate. See
[custody contract and tests](../evidence/ap18/pigments/result-custody-checkpoint.json).

The one revision-5 attempt captured **EventChannel**, request 360 / callback 358,
with 357 successful blocks before refusal. The emitted event is Note On, bus 0,
channel 0, pitch 60, offset 144/256. Retained output-event metadata declares zero
channels; event, point, queue and payload counts were all zero before refusal.
Both independent custody and the post-join projection agree. No capacity or
validator was changed. The early refusal preceded editor initialization; a later
open request is not evidence of a sustained Welcome/open-editor interval.

The [pinned SDK BusInfo contract](https://github.com/steinbergmedia/vst3_pluginterfaces/blob/4f547e8e102b47de4a8b8aaf343c73b700786372/vst/ivstcomponent.h#L88-L91)
associates event channel count with supported MIDI channels and requires checking
used channel counts after arrangement negotiation. The current BusLayout keeps
its earlier metadata; this run did not retain a post-negotiation requery. Resolve
that boundary and vendor intent before claiming a vendor defect or adding an
exact compatibility rule. Zero-to-sixteen substitution, event discard and
capacity increases are not authorized by this observation.

[Live result](../evidence/ap18/pigments/result-custody-live.json) and
[final restoration](../evidence/ap18/pigments/result-custody-installed-final.json)
retain the bounded facts. Pigments is inactive; LoFi/FRAGMENTS revision10 and
protected projects match baseline. 512 remains recommended; 256 unqualified.


### Event-output operational census (review 5183863039)

A separate nonprocessing inspection mode now captures the same component's raw
output-event BusInfo after initialization, arrangement negotiation, processing
setup, event-bus activation, component activation, and processing start. The
production inspection owner retains its normal admission lock, timeout and
positive cleanup. No editor or state capture is needed. The SDK fixture covers
unchanged zero, zero becoming sixteen, and an unchanged positive count.

The exact installed Pigments reported **zero at all six stages**, with one active
main event-output bus and unchanged stereo audio arrangements. All SDK calls and
cleanup completed. This excludes a positive post-negotiation channel count for
this fixture. No raw value was replaced, compatibility policy selected, or
result validator changed. Bitwig's event/parameter sink presence remains the
next required observation. Revisions 1–5 and both ordinary revision-10 products
remain unchanged; no revision-6 profile has been created.

See `evidence/ap18/pigments/event-output-operational-census.json`. PR #95 remains
draft and unmerged. This is census evidence, not successful Pigments processing.

### Native sink observation interrupted

The one authorized external callback snapshot found both Bitwig result sinks
non-null, output-event bus active and advertised channels zero, but it occurred
28.360 seconds after the supervisor's pre-containment snapshot of an already
failed Windows session. It therefore does not establish sink presence on a
healthy or rejected callback. Case B/C remains unresolved; no normalization or
revision 6 has been created. A replacement live snapshot requires renewed
operator authorization; the first authorized snapshot has been consumed.

The preceding session independently retained EventChannel at request 90385,
callback 90383, position 23137792, 256 frames, Note On bus0/channel0/pitch58,
offset32, zero input notes/parameters and zero prior output use. Processing
returned, stopped and joined; mapped custody and post-join JSON agree. The
outer launcher exited 5; the final editor stage was 215, before vendor view
release. Exact Windows termination cause remains unknown. The operator reported
a possible touchscreen UI close; this is context, not causal proof.

Pigments was restored inactive. Ordinary LoFi/FRAGMENTS physical publications,
software, protected project hashes and Flatpak permissions equal baseline.
Service/keeper active; zero DSP/pending/stale sessions, tracing off, no Bitwig or
debugger, CPUWeight unset/effective100. An earlier unexplained CPUWeight override
is excluded from performance evidence. See
`evidence/ap18/pigments/event-output-sink-interrupted.json`.

### Direct correction authorized after the interrupted observation

The operator explicitly removed the pre-repair sink-observation prerequisite.
Implement the exact-profile reported-zero/effective-sixteen interpretation in
the native descriptor, Windows setup comparison and Collector bus limit. Keep
the existing result-delivery path, all other validation and capacities. Preserve
revisions 1–5; build corrected revision 6 through the existing sealed route.
No further pre-repair live investigation or desktop control is authorized.
Use SSH/background tools for the corrected C3 check, then continue AP18 if it
plays. The attempted replacement debugger wait expired without finding an
instance or taking a snapshot.

### Corrected revision 6 built

The exact Pigments profile now declares the closed event-output policy. Its
native descriptor retains raw zero separately and advertises effective 16;
Windows preserves raw BusInfo while setup and Collector use effective 16.
Channels 0/15 pass unchanged; negative/16 remain rejected by the existing MIDI
field validator. No capacities, result encoding, input routing or sink-delivery
behavior changed. Revision 5 remains byte-identical in its historical path;
replacement extends only 5→6, never ordinary activation.

The Linux SDK/CTest lane and 37 Linux runtime tests pass. All four hosted source
lanes pass at eb593b3. Exact Windows and native identities are retained in
`evidence/ap18/pigments/event-output-correction.json`. Corrected C3 playback is
the next check; no live playback success is claimed yet.

### Pigments retained editor lifetime (revision 7 implementation)

Revision 6's exact MIDI-output correction is retained. The subsequent operator
session produced audio and no Collector rejection, but stopped at vendor view
release while the processing worker's last complete stage was inside process().
The operator selected a bounded `retain_editor_view_until_instance_retirement`
policy for this exact Pigments candidate.

This optional closed profile field reaches only the registered Windows session.
Ordinary close cancels focus, ends editor gestures/groups and hides the existing
parent. It retains the attached IPlugView, HWND and IPlugFrame. Hidden editor
callbacks are refused, while synchronous host-origin parameter echoes retain
their existing suppression. Reopen uses the same view/window with a fresh
logical epoch and a new generation-bound handler; the previous handler becomes
inert. An invalid retained parent cannot be replaced speculatively.

Final controller unbinding follows run_offline_processing's positive quiescence:
processing stopped/joined and component deactivated. Only there is retained
ownership physically retired, in the order setFrame(nullptr), removed(),
release(), DestroyWindow(), then original handler restoration. Failed retirement
keeps the existing containment law. Ordinary LoFi/FRAGMENTS and standalone
vendor-access behavior are unchanged. No audio callback, wire layout, result
capacity, event interpretation or auxiliary-input routing changes in this repair.

The generated Windows fixture checks hide without SDK retirement, exact parent
reuse and logical epoch advance, host-value updates while hidden, final teardown
order and one-time release, and refusal after external parent destruction. Its
first run caught host-update echo suppression being lost on hide; that production
regression was corrected without weakening the fixture. Commercial close/reopen
qualification remains pending until the exact revision-7 artifact is installed.

The one revision-7 live check passed logical close/reopen and continuous local
output in the same DSP session (epoch 1→2, same vendor window). Final retirement
failed again at release, now **after** successful setProcessing(false), worker
join and setActive(false). This distinguishes the remaining failure from unsafe
concurrent view release; it does not identify the exact Windows exception/cause.
Positive containment is not successful SDK retirement. Pigments was restored
inactive and remaining AP18 qualification stopped at this result, as directed.
See `evidence/ap18/pigments/retained-editor-live.json` for bounded evidence.

### Process-scoped final retirement after revision 7

Review 5184501713 accepts revision 7's logical editor close/reopen and continued
processing. Final SDK destruction failed at `IPlugView::release()` after the
worker had stopped and joined, the component had deactivated, and frame/view
detachment had succeeded. No further release experiment is required.

The next Pigments-only candidate selects `process_scoped_vendor_retirement`.
Ordinary editor hide/reopen is unchanged. At final retirement, the existing
processing owner supplies successful stop/join/deactivation custody; the mapped
owner requires no pending processing or state request. The UI owner ends editor
gestures and focus, detaches the frame/view and destroys its owned parent. It
acknowledges the existing endpoint, then commits LVRT v1, a required 256-byte
session-bound mapping with eight atomic scalar words and a first-write commit.
The record is separate from optional diagnostic mappings.

The Windows owner retains its vendor references while the existing Linux
supervisor contains the exact process cohort. The final owner wait is bounded
by five seconds and exits abnormally if containment does not arrive; it never
unwinds vendor destructors. Missing/incomplete custody and early host exit
remain failures. Only positive cohort and transport retirement produces the
`process_scoped_vendor_retirement` disposition. This does not claim clean SDK
object destruction. Other profiles retain their existing SDK teardown.

Revision 8 binds the exact successful Windows build and unchanged native proxy.
The single revision-8 functional check passed: audio continued while hidden and
after reopening, then typed process-scoped retirement and positive cohort,
transport and lease cleanup completed. See
`evidence/ap18/pigments/process-retirement-live.json`. This is not clean SDK
destruction or a performance qualification.

The operator authorized reporting-only revision 9: drain the existing fixed
Windows input observations after processing joins and before the retirement
commit, since the ordinary destructor is deliberately bypassed. Revision 8
remains immutable. The remaining input and musical checks are unfinished.
Revisions 1–7 are immutable; ordinary LoFi/FRAGMENTS revision 10 is unchanged.

## Revision 9: routed-input reconfiguration stops qualification

The authorized reporting fix and sealed revision 8→9 binding passed manager
tests/Clippy and all four hosted lanes. The protected C3 loop produced nonzero
local audio with No input. Selecting Audio 2 (PRE), carrying Bitwig's built-in
Test Tone, then caused a new lifecycle-correlation failure. Native input records
show silence then nonzero samples, but the Windows witness was not emitted: the
second processing interval failed before retirement-ready could commit. This is
not two-sided routed-input proof, a Collector rejection, or vendor-view release.
No reload or repeated attempt followed. The exact mismatching lifecycle field
is not yet known; no transport repair was made. See
`evidence/ap18/pigments/r9-routing-transition.json`.

Normal Bitwig quit completed; Pigments is inactive, ordinary LoFi/FRAGMENTS
revision-10 physical targets and project hashes are unchanged, zero DSP leases
and pending transactions, no stale transport, service/keeper active, tracing off.
SteamOS-owned CPUWeight read back 10000 after the run (100 before); it was not
manually changed and this is not performance evidence. 512 remains recommended;
256 unqualified. PR #95 remains draft; the remaining AP18 checks stopped at this
new material failure.

### LC1 — same-session processing reconfiguration

The exact requested Configure/Activate/Start, Stop/Deactivate, repeated Configure,
Activate/Start sequence passes with the real native queued worker and Windows
MappedSession at sequence 104687. It did not require a sequence reset or relaxed
epoch validation. The retained routing failure also records native operation 14:
Deactivate. A generated two-ended test reproduces the failure when the second
Activate is followed by Deactivate before Start: Windows waits for kind 10,
receives kind 14 at the correct session/sequence, and reports lifecycle
correlation failure with zero blocks in that interval.

The Windows processing owner now selects Start or Deactivate before creating a
worker. An activation-only interval performs no processing call and does not
advance sequence or epoch. A later Start uses the same sequence and next epoch,
with position zero. The original two intervals and the formerly failing
intermediate activation both pass. A wrong Deactivate sequence remains refused;
stale Stop/Start epochs remain refused. Existing returned-result, stale-output,
state, editor and containment laws are unchanged. See
`evidence/ap18/lc1/regression-before.json` and `regression-after.json`.

Pigments revision 10 binds only the new Windows artifact and source manifest;
its native artifact, bus/event policies, retained view and process-scoped final
retirement are unchanged. Revisions 1–9 remain immutable, ordinary activation
still refuses this candidate, and the sealed chain adds only 9→10. Live
route/unroute and the remaining AP18 qualification are still pending at this
source checkpoint. LoFi and FRAGMENTS ordinary revision 10 are unchanged.
