# NAUI1 implementation-agent handoff

Act as the Linux-VST-bridge implementation engineer for the selected slice:

**NAUI1 — Native Access renderer identity and blank-window attribution.**

Read `CURRENT_SLICE.md` and `docs/NAUI1.md` first. Those documents are the active
contract. Do not broaden the work into a graphics workaround or another installer
attempt.

## Exact basis

Work from this planning branch and preserve its basis:

```text
Retained evidence head:
2a4846958ad176785b7bece9a5e99d263b4d9e5a

Retained evidence tree:
c43c0ea0521e562585c04ed5228a93c722b131f9

Installed merged source:
8fb2f20c822320340e44f9cbae399633dd6e8a53

Installed merged tree:
947461f96dc2648b13c9b450c761beae97880571

Environment:
627d2cba97edbecf113c22504eb4c81b

Operation:
e4143128adc87de3fdbbdfb44f186ee5
```

The retained result is authoritative for what occurred: one adapter-bound Native
Access continuation reached a blank white window, closed normally, outer exit 0,
zero survivors and positive cleanup. Durable installation classification remains
partial. No rendering cause was established.

## Required implementation

Implement a source-owned, read-only diagnostic owner under `tools/naui1`.
Recommended components are `identity.py`, `records.py`, `classify.py`, `run.py` and
focused tests, but preserve behavior rather than filenames if a cleaner small design
emerges.

The implementation must:

1. Consume a closed committed manifest, not caller-selected paths.
2. Resolve only the exact retained managed environment and operation evidence.
3. Verify regular owner-controlled no-symlink inputs, exact digests and bounded
   sizes/counts before parsing.
4. Perform a bounded census only inside the exact environment’s Windows prefix.
5. Establish one exact Native Access application root or explicitly refuse.
6. Identify Electron/Chromium only from exact local artifact/process evidence.
7. Read selected ASAR/package metadata without extraction, execution or mutation.
8. Parse retained private process/log records using an allowlist of renderer-role
   switches and canonical failure categories.
9. Keep Windows and Linux process identities separate.
10. Publish only sanitized counts, hashes, roles and dispositions. Raw paths,
    command lines, URLs, tokens, account data and vendor file contents stay private.
11. Return exactly one disposition from the closed law in `docs/NAUI1.md`.
12. Treat dropped/missing records as unavailable evidence, never as absence.

Do not copy a community project wholesale. `dguedry/nilinux` commit
`e332e69c03d697c60b369c0fbca73be34ba31e65` is comparative research only. If a
small licensed algorithm is genuinely reused, retain exact provenance and license,
but prefer a minimal source-owned bounded reader.

## Required generated proof

Add deterministic fixtures for:

- exact and misleading Electron-style application trees;
- missing, duplicate and aliased roots;
- changed executable/resource/ASAR bytes;
- GPU crash-loop;
- sandbox child/broker failure;
- renderer-only failure;
- GDI/font exhaustion;
- clean launch evidence;
- unrelated lookalike diagnostics;
- truncation, saturation and dropped records;
- duplicate/reordered generations;
- Linux/Windows PID collision;
- privacy-field refusal.

Every field capable of selecting a disposition needs negative mutation/removal
coverage. The screenshot fact must never select a technical cause in a test.

Add NAUI1 tests to the appropriate hosted workflow. Run focused tests, affected
repository tests, AP12 and PX2. AP8 is required only if you introduce a Windows
binary/helper. AP10 remains out of scope.

## Read-only Deck observation

Commit all executable diagnostic source before touching the Deck evidence. Then run
one read-only observation against the exact retained environment.

Before running, establish canonical idle/read-only state. During the observation:

- do not launch Native Access;
- do not launch Wine, Proton, an installer, updater, daemon, DAW or plug-in host;
- do not focus or manipulate a vendor window;
- do not reinstall, scan, modify registry, alter files or automate account input;
- do not use `--disable-gpu`, `--no-sandbox` or any experimental argument.

Retain private raw evidence according to existing project convention. Commit only
sanitized generated result, validation and preservation records. The final evidence
commit must change no executable diagnostic source.

Post-observation readback must prove installed software, retained environments,
products, publications, projects, service/keepers and all prior evidence remain
unchanged, with zero live operation cohorts.

## Delivery

Return one draft, open, unmerged PR for independent review. Its body must include:

- exact base/head/tree;
- executed diagnostic source head/tree;
- manifest and result digests;
- selected disposition and why it satisfies the closed law;
- unavailable evidence and drop counts;
- generated test and hosted-check results;
- final preservation readback;
- explicit statement that Native Access was not launched and no remedy was applied.

Stop after the draft PR. Do not prepare or execute NAUI2 within this branch.
