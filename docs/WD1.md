# WD1 — one directly hosted Windows VST3 in FL Studio

## Claim and fixture

WD1 uses the existing manager-owned FL Studio workspace on the maintainer's
Steam Deck. FL `26.1.5.5618` remains in trial mode. The selected runner is
`proton-11.0-2c-fl-crypt32-order-v1`; the selected audio device is not yet
known and is not changed by WD1. The operator's official Serum 2 `2.1.5`
installer has exact SHA-256
`507b726d97bf78920157f3817aff003b9ee38ee961f4efd318cf43216370f695`.
Its presence in private manager custody must be verified at selection time.

The intended claim is an exact manager-owned Serum installation in the FL
prefix, followed by FL's own verified VST3 scan, audible piano-roll playback,
one preset change, one parameter automation, clean close, and a fresh FL
session that adds and plays Serum again. The operator deferred WAV export as
unnecessary for this pass, so it remains unqualified. Attempt FLP save only if
the current demo permits it; the operator reports that project saving is
unavailable in their present trial session. Trial mode does not establish
project recall.

## Owner and action boundary

Serum is a product installed into the FL workspace. Its selected installer,
active installation operation, append-only result history, installed Windows
VST3 module and bundle root are separate from the FL application selection,
current installed executable, and installation/uninstall histories. The
manager offers exact typed selection, install, and finish-install actions in
Workspaces. Selection verifies the admitted artifact but executes no Windows
code. Installation uses the existing supervised installer cohort in the same
prefix and runner. The operator handles lawful vendor UI and activation.

The manager does not claim an arbitrary VST3 found in the prefix. Existing
Serum files without matching product authority block installation. Installer
exit, module discovery, FL scan, audio, automation, export, and fresh-session
use are separate observations. A nonzero installer exit with a discovered
module remains `needs_user_action`; it is not silently called a clean install.

The native Bitwig bridge retains its six publications and separate Serum
candidate-D environment. No Linux proxy, bridge transport, native publication,
DSP lease, or six-instance capacity claim is created by direct FL hosting.

## Physical acceptance and limits

Start with read-only status of the installed manager, FL workspace, imported
installer and native publications. Keep the present prefix, user roots, runner,
and audio settings. Use one installation attempt and preserve its exact result.
If the first attempt fails, record the exact failure and stop rather than
editing workspace JSON or copying another installation.

After FL's verified scan, use mouse/trackpad for one short Serum pattern,
preset change, and parameter automation. If FL offers project save,
retain an FLP without claiming trial-mode reopen. If it refuses, record the
trial limitation and continue. Close FL normally, verify exact cohort retirement,
launch a fresh session, add Serum again and confirm playback. Record only
observed results in [SUPPORT_MATRIX.md](SUPPORT_MATRIX.md) and the failure ledger.

## Current Deck result

The exact official Serum `2.1.5` installer was selected from manager custody and
installed once into the existing FL prefix. The manager's product completion
readback verified `Serum2.vst3`, retained a separate product installation record,
and left the FL application history and native registry unchanged. The operator
reports FL discovery, audible piano-roll patterns, a preset change, and working
Serum parameter automation. The initial editor exceeded the Deck's visible area;
Serum's logo-menu zoom made it usable. The exact size was not recorded.

The operator elected not to export a WAV in this session. Export is therefore
unqualified and outside the bounded WD1 acceptance. FL was then quit normally,
and the manager retained a completed session receipt with zero owned processes
and confirmed cleanup. The refreshed
manager/frontend generation kept the FL workspace, Serum history, and native
products exact. The operator then opened FL again, loaded Serum into a fresh
session, entered a pattern, and reported successful playback. That second exact
session retired with confirmed cleanup and zero owned processes. This is fresh
use, not FLP state recall. These limits are recorded in the
[physical evidence](../evidence/wd1/deck-serum-direct-2026-09-25.md). A saved
FLP and licensed project recall are also unclaimed.
