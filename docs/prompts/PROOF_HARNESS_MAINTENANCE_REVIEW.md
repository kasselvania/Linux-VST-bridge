# Focused test-helper review

```text
Review the actual helper diff and its focused regression tests. Does it
preserve useful errors, owned cleanup and truthful observations without
changing the product claim? Explain concrete defects, not missing ceremonies.
Routine helper repairs can be reviewed in the same implementation PR. Do not
require a separate maintenance campaign or full product replay merely because
the helper changed. Preserve historical receipts and never bypass tool policy.
```
