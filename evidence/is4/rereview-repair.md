# IS4 focused production-integrity repair

Reviewed predecessor `c0e4edb427f39382362978910b2603ceab9f63b9` remains retained.
Its sealed campaign is credible and unchanged. This amendment repairs the two
production states outside that successful campaign.

## Admission and application authority

Rust binding and Python validation require `pe_executable` and the exact verified
IS2 adapter. Format and adapter are explicit policy fields; adapter identity must
match the full software record and installer spec. Missing, differing or changed
adapters and non-PE formats refuse before prefix or target creation. The policy
action is offered only on the eligible route. Ordinary schema-2 direct PE/MSI
behavior remains unchanged and is not claimed to have IS2 root authority.

The target environment is prepared before creation; it is not published as effective.
Only after Popen succeeds and the existing ledger registers the child does the owner
commit the effective receipt. Forced target creation failure after successful prefix
initialization retains requested policy, null effective policy, terminal failure and
positive cleanup. The frontend says application was not confirmed. The positive
control retains an effective receipt matching the exact environment passed to Popen.

## New sealed campaign

Executed source: `9af1ff1def83a5055aa12acb371ff6479dc61763`.
Executed tree: `8ee95abdb6a10ba2e109f94b4e17770b4be80169`.
Manifest SHA-256: `3e8347b12502150bb5f7d21db03556f77e941d90d59abbdc47cff083cb417f19`.
Comparison identity: `ecbf356092ab08eaa328b0fafcf3f7f8a027d136650df4b3de86dcb275141f0c`.
Generated software binding: `bd7c9b2bd8531c9b192479292da62eb2b36839041b701d99eb8e8452eb6694b1`.

| Policy | Operation | Result |
| --- | --- | --- |
| Inherited | `32b86955b6bba121858995bc5a28bc2f` | False-success baseline |
| Intentionally unavailable | `d7652c5cb899fae9b883b1b6040ecaac` | Nine PowerShell refusals 126; three source-owned fallback exits 43 |
| Restored inherited | `1f7f3e30da0a261fe813a55b496efb9b` | False-success baseline restored |

Every session has one bound root, outer exit 0, positive cleanup, zero survivors,
retired unit and removed scratch prefix. The committed campaign owner generated
`rereview-production-policy.json` without post-processing; the manifest and private
proof digests identify the exact executing owners. Only these three Windows sessions
were run. The earlier campaign, manifest, staged source files and private proof hashes
were independently rechecked and unchanged.

## Verification and preservation

126 manager-library and 72 binary tests passed; 20 frontend tests and both strict
Clippy checks passed. Local runtime: 120 tests, 37 Linux/opt-in skips. The sealed
Deck package passed 41 tests, including actual Linux supervisor positive/failure
loops. IS3: 24 tests; IS4: 25 tests. Hosted AP8/AP12/PX2 results for the final head
are linked in PR #115. AP10 remains out of scope.

One initial projection test omitted its catalogue prerequisite and failed; the
corrected fixture passed. A source transfer exceeded the existing transport bound
and was compressed without changing that bound. Neither was a Windows campaign
failure. No failed Windows run was discarded or repeated.

Final canonical readback: installed software unchanged, installed operator schema 4,
service active, two healthy keepers, zero DSP/maintenance leases, pending transactions
or stale transports, cleanup unblocked, capture off. All 307 retained files and five
protected projects match. Products, publications, imports and environments are
unchanged. The implementation remains uninstalled, draft and unmerged.

No Native Access run, interpreter installation, registry mutation, runner replacement,
product publication or native/audio change occurred. Source-owned fallback success
is not vendor fallback qualification or proof of historical Native Access causality.
