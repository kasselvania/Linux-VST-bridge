# IS1 — one reviewed, human-operated Native Access attempt

The fresh attempt stopped at the installer's **running-application detection / close
check**. The operator supplied screenshots of “Native Access is running” followed
by “Native Access cannot be closed,” reported that Retry repeated the dialog, and
described Cancel as a crash. Machine evidence establishes outer exit **2**,
**partial installation**, and positive cleanup. It does **not** establish a crash,
which process the check detected, or why closing it failed. No second attempt ran.

## Installed source and custody

Independently install-cleared implementation:
`0543c3d3d021a5d9821302c085ec93fdd69ae92b`, tree
`697a47c517109457b0fb12533525f09741ee53ad`, stacked on the unchanged #111/#110
dependencies. The authorized idle stop / immutable setup / restart route installed
the exact reviewed manager, frontend, supervisor and ownership helper. Existing
Windows host, source manifest, native artifacts and preparation kit were reused.
Their exact installed SHA-256 identities are in
[the receipt](native-access-attempt-3.json). Previous immutable software remains
available. No runtime source changed to obtain this real result.

The existing 179,877,344-byte import
`82d7b7977d4fbc19db32aee8778c75906f6ddf935bcf2958f2d8d45141b2b2cc`
was verified in manager custody and reused. No file picker or source reimport.
The installed manager's offered, closed operator actions created exactly one
unpublished successor and launched the installer once:

| Fact | Exact identity |
| --- | --- |
| Previous partial attempt | `49c19ffeb4e01fc11f8a26e2edbead82` |
| New isolated attempt | `da0f50e64aa8c8aeb91d8e66cda3fb1d` |
| Environment creation operation | `e9c79c0d95ec4283170021f5fcad0e38` |
| Installer operation | `be613124b24190ca01a6735bb3ffd839` |

All five creation lock acquisitions succeeded in one attempt, with 29–66 µs
elapsed acquisition time, retained in the receipt. The installer used the pinned
runner and bounded IS1 process/service/MSI diagnostics. CA1 stayed off. The human
provided all vendor interaction; the agent supplied no vendor keyboard or mouse
input. The manager frontend was opened from its installed desktop entry and may
remain open as a user-owned application.

## Separate results

| Boundary | Retained result |
| --- | --- |
| Prefix initialization | Exit 0 at relative 5.281 s |
| Target runner | Started at 5.347 s |
| Imported target image | Observed at 8.209 s against the import digest |
| VC prerequisite packages | Both `vcRuntimeMinimum_x64` and `vcRuntimeAdditional_x64` logged apply result `0x0`; VC bootstrapper logged exit `0x0`, no restart |
| VC process observations | Windows ordinals 15–17 each retained self-exit 0; exact launch-request image hashes retained |
| Visible later boundary | Operator screenshots: running-app detection, then failed close request |
| First attributed child failure | Unavailable; none acquired exact target-tree authority |
| Outer result | Linux wait status 2, separately retained |
| First failure selected by reporter | Direct launcher / target_runner / role unknown / linux_wait / status 2 / cause unestablished |
| Exception / stack | Unavailable; no captured fatal exception |
| Manager cancellation | None; the reported vendor Cancel action is separate |
| Durable outcome | Partial installation; VC package cache and uninstall entries added |
| Product/service outcome | No application registration or service delta witnessed in the allowlisted surfaces; dependency health and post-install launch unproved |
| Retirement | At relative 247.901 s; cleanup confirmed, owned processes 0 |

The operator worker's `completed` state means that supervision and its receipt
completed. The installer result itself is `failed`, transaction outcome
`outer_nonzero_stage_unknown`; it is not successful installation.

The durable delta contains three prerequisite executable/package files, three
uninstall registrations and three prerequisite logs. There are no changed or
removed entries, no application registration, and no service changes. This is an
allowlisted installation observation, not a claim that every prefix location was
examined. The prior two environments and all their retained records remain intact.

## Attribution limits exposed by this run

The private Linux ledger retains 70 exact process identities. Four Linux exits
were observed; 66 remain `unavailable_parent_reaped_or_unobserved`. The separate
Windows trace retains 43 creation observations and 33 self-exits, all observed
self-exit statuses 0. Neither ledger substitutes the outer status for an unknown
child exit. Sanitized per-domain ordinals are in
[the ledger projection](native-access-attempt-3-ledger.json); raw process identities,
image paths and timestamps remain private.

**The Windows imported-target root chain is missing.** There are zero
`target_root` and zero `target_tree` rows. The independent Linux startup observer
did verify the target image, but that cannot establish a Windows process identity
by PID equality. As a result, the Windows local child chains and exit observations
cannot authorize imported-target failure attribution. This is an authority gap
in this real route, not proof of a successful child tree. The source-owned matrix
remains valid for its declared fixtures; it did not prove this root observation.

Thirteen observed PowerShell processes and their console children exited 0.
Their commands/output are not retained by the closed process projection, so those
names and counts do not prove what the installer's application check queried.
Twenty-one Wine service-start return observations include two numeric 1114
results, but lack RPC child association; no product dependency or NTK failure may
be assigned to them. Process names, service noise and Xalia output do not select
the installation cause.

The bounded runner sink retained 30,330 bytes and dropped 2,245,814 bytes / 27,292
records while continuing to drain. The separate structured Windows parser records
zero dropped observations, which does not fill its missing root chain. Accessibility
diagnostics retained 800 bytes separately. Three private prerequisite-log tails
retained 53,754 bytes with no recorded truncation. There is no direct-MSI verbose
stream for this EXE; no undocumented EXE logging switches were added. Raw private
logs and screenshots are not committed; hashes and the operator transcriptions
are retained in the public receipt.

## Final readback and review direction

Service active, both keepers healthy; DSP 0, maintenance 0, pending transactions 0,
stale transports 0, cleanup-unconfirmed false, capture off, and no service-resume
record. Installer and operator units have retired, with no owned cgroup remaining.
All 276 pre-existing retained files, including five protected project files,
matched their pre-install hashes. Existing physical publication links and canonical
product revision/disposition/history fields also matched.

Pigments 18 / rollback 11, Pure LoFi 10 and Efx FRAGMENTS 10 are unchanged. Serum's
existing candidate B experimental revision-1 publication remains unchanged; Serum
FX remains separately unqualified. No product session, scan, candidate or ordinary
publication was introduced by this attempt.

The evidence selects **review of the running-application detection/close handoff**
and the **missing Windows target-root attribution**. It does not select a Wine,
Proton, Xalia, elevation, NTKDaemon, graphics or vendor defect. No speculative fix,
security expansion, unrelated process stop, first launch or further installer
attempt was performed. A managed first-launch review is appropriate only for an
established installed outcome; this receipt is partial.

Reviewed-source AP12 and PX2 passed. This continuation adds installation and real
evidence only; accepted generated fixtures are not rerun or rewritten. Applicable
final evidence-head workflow results are reported on draft PR #112. The PR remains
open and unmerged for independent review of this exact result.
