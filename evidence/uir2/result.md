# UIR2 result

Disposition: **UIR2_INSUFFICIENT_PHYSICAL_COVERAGE**.

The retained UIO3 release records contain old Windows MSG.time values but no
independent old X-server/Windows clock calibration or per-release queue-availability
observation. Their three contacts remain unpaired across systems. Phase A cannot
select admission versus retrieval, and the original evidence is unchanged.

The source-owned fixture built and passed its generated self-test under Windows
and the exact pinned runner. The separate source-owned PropertyNotify fixture
passed on the physical XWayland route without input. The human-only fixture then
opened one bounded observation window. A single-finger action was requested, but
no operator completion arrived and no touch/core/Windows release was recorded
before the 135-second companion bound expired. This is missing physical coverage,
not evidence that an attempted physical touch was lost. No retry occurred.

The window remained live for 32776 pump turns and 1350 posted heartbeats; 1312
source-owned timestamp sentinels passed. Both clocks supplied 68 independent
brackets. The fixed collector retained 181105 records with zero drops. These facts
prove tooling operation, not a touch latency result.

The fixture closed normally. Parent and child were destroyed; the exact cohort
and process group are empty, both transient units inactive/successful. Canonical
readback shows the ordinary products unchanged, service/keeper healthy, capture
off, zero DSP/maintenance leases, transactions and stale transports. No Bitwig or
Pigments session, agent input, scratch compositor or compatibility change occurred.

Return draft PR106 for independent review. UIR2 does not select a repair owner;
no further interaction or workaround is authorized by this result.
