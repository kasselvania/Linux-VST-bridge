# BR1 live recovery: source-owned result

Base: `36b87ad1c5f55db5c241e5c961a604cd2b18a3be` (tree `150736ab379cff98d08e952595c713b587f51dd2`). This is an opt-in same-instance queued-backend repair. PR #167 and its Serum evidence were not changed.

## Policy and state machine

Ordinary paths remain disabled. The Pi appliance may select `LVB_RPI2_LIVE_RECOVERY=horizon_frames:worker_deadline_ms:1` before its first START. The horizon is 256–8192 frames, the worker-return deadline is 20–5000 ms, and one automatic recovery is allowed per instance. Invalid values are refused. The callback compares `current_position - presentation_delay - next_result_position` with the configured horizon, after consuming already published results. Queue high-water counters are historical observations, not the trigger.

`normal -> requested -> restarting -> acknowledged -> reconciliation -> re-prime -> normal`. Once requested, ordinary admission closes and output is silence. Shared stage remains non-normal until the first delivered new-epoch audio, so state control cannot enter during reconciliation or re-prime. The callback tracks current player intent and completion-confirmed plug-in intent in fixed 256-note/256-parameter views. A fixed 64-request/4096-event journal records published intent by epoch, position and sequence; publication does not advance the confirmed view. The worker's final successful old-epoch sequence and position include an obsolete completion suppressed after the request to recover. At acknowledgement, the callback confirms only through that frontier and treats the remaining journal tail as discarded.

Reconciliation releases confirmed old notes, reissues currently held notes, and applies only the latest parameters whose updates were discarded or arrived while admission was closed. It does not replay a historical completed parameter value that a plug-in preset may have changed internally. Post-ack note edges are bounded; repeated parameter movements coalesce to the latest value. An unrepresentable note, event history or journal terminates with recovery-capacity fault 8, without truncation. The callback performs fixed-size work, atomics, queue operations and monotonic reads only; it does not wait for the worker, allocate, log, or perform lifecycle/I/O.

After an in-flight call returns, the worker suppresses its obsolete result, discards old requests from the consumer side, and sends STOP/START on the same Session and plug-in instance. The callback discards old results, clears retained audio and returned events, advances the epoch, sends old-note releases followed by currently held notes and latest parameters, and withholds presentation through re-priming. A second late incident is fault 7 (`recovery_exhausted`); a missed worker-return deadline is fault 6 (`recovery_timeout`); failed/conflicting lifecycle work is fault 9. No `ap6_recover()` or old opaque snapshot is used. STOP/close after a completed recovery use the existing lifecycle.

## Source-owned validation

- Mode A: mapped/TCP fake peer delayed one process call while the callback received an old note-off, new held note and parameter updates. It resumed with old note inactive, new note active, latest parameter 0.9, finite nonzero stereo sample 0.72, exactly one new epoch, and normal STOP/DEACTIVATE/CLOSE.
- Mode B: worker held past the 100 ms deadline, then released for containment. Fault 6 was observed, with admission closed and no queue-overflow fault or second recovery.
- Mode C: a second delay after successful recovery produced fault 7, without a second epoch or queue growth.
- Focused checks also cover explicit ledger-capacity refusal, coalesced pending parameters, and discarding one old result plus pending returned state before the new epoch.
- Review repair: a queued NOTE_OFF discarded before worker processing still produces exactly one new-epoch cleanup NOTE_OFF; a queued parameter 0.9 discarded after completed 0.2 is replayed once; a completed historical 0.2 is not replayed over a source-owned internal preset change to 0.7. The delayed mapped peer checks these as distinct cases.
- The finite-stall fixture verifies the state-control stage gate is closed after worker acknowledgement and during re-prime, then opens on first delivered new-epoch audio. Ordinary `service_us_max` ends after conversion, notice handling and output preparation as it did at the base. The suppressed call uses recovery-specific return timing.
- Updated macOS serial backend suite: 83 passed, 1 fixture-dependent test ignored. The native event suite: 12 passed. The standalone appliance library suite: 28 passed. The focused backend recovery tests with the `rpi0` feature: 11 passed. The unchanged broad parallel backend run at the earlier head could contend for the global test registry and returned two `Full`/`Busy` setup failures; both passed in the serial suite. No base-equivalence claim is made for that parallel contention.

## Pi smoke

The initial-head smoke ran only the source-owned finite-stall mapped transport fixture on the Pi 5. Its callbacks were paced at 256 frames per 48 kHz block, with a 2048-frame presentation reserve. JACK remained at 48 kHz/512 frames; no plug-in, Wine, FEX, priority, affinity or device setting changed. At the earlier head `eec722464771606187e81ce7656e6e6521ce7aa4`, staged `queued.rs` SHA-256 was `2bd058464e4ec93f6fc8b7790cd3a66f2fdafdaeaab9236f87dd7ddda6788f81`; the source-owned fixture SHA-256 was `acfe843cd7e04043c5c27ddcd9684bee26b756e4ab8ed0dfb4e28fba1c5ed99a`. Both hashes matched the local files.

The paced run passed: epoch 1 -> 2, one recovery, historical request high-water 9 of 2048, 9 old requests discarded, 0 queued old results to discard, 17.192 ms from recovery request to worker return, and 49.997 ms from worker return to first resumed current stereo audio in the paced fixture. The old stalled completion was not published. Normal source-owned retirement completed. JACK had system-only ports before and after, with no leftover fixture process. A post-run device read was 47.7 C and `get_throttled=0x0`.

The mapped peer and paced callback exercise the actual queued worker, epoch lifecycle and audio output on ARM; they do not establish JACK callback deadline behavior, analog latency, a vendor preset transition, or DSP capacity. This slice stops before another Serum session.

The one updated paced smoke used the repaired source hashes `queued.rs=dcb3f09b912e3d352f5a7a7ce53e7a0ed78ba154bc6c4254aba1285b3e56b836`, `live_recovery.rs=419681f46a2476c3bc466184b64a28fa4f91784f20911fa32a8034e38bd0fabb`, and `live_recovery_tests.rs=3ba6b1d78dd974e7c1174991a63adfb6906928a830733b3882f199e3de323c87`, verified equal locally and on the Pi before execution. The exact `rpi0` finite-stall test passed: epoch 1 -> 2, one recovery, historical request high-water 9 of 2048, 9 stale requests discarded, 0 stale queued results, 17.231 ms from recovery request to worker return, and 50.020 ms from worker return to first resumed current audio. Source-owned normal STOP/DEACTIVATE/CLOSE completed. JACK showed system-only ports before and after, no fixture remained, post-run temperature was 48.5 C and `get_throttled=0x0`. The measured intervals are from the paced fixture, not analog latency or DSP performance.

Two earlier short source-owned Pi runs informed the fixture: the first advanced callbacks faster than real time and was excluded from timing evidence; the next paced run passed before the final discard-count accounting correction. The numbers above are from the exact staged source after that correction. No commercial plug-in ran in any of these checks.
