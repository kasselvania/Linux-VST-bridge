# NAD1 completed read-only observation

Executed source `4d0121697631a8f73a239c010b314d7fface79b2` is sealed by
`3989f31bb9d3924b47d2a8ffd5730a6bf1c43578db974d1c942e2dc9f2743212`.
The unchanged public result SHA-256 is
`3d14b33cf14fef1528cc7ac594fcd3707f262a227280f25b9f991938bdd0d5b2`.
The prior refused passes and markers remain separately retained. This completed
pass was within the operator's explicit bounded-corrections/reruns authorization.

Disposition: **NAD1_IDENTITY_UNRESOLVED**.

The exact Native Access-bundled installer is an x86 PE, 35,769,456 bytes,
SHA-256 `5f2199f4e1409d6eea5edaea9c4a8af31e8ee8ac3790851aa44d33e87a46b218`.
Its optional version strings are unavailable under the strict parser. The filename
is a relative location, not independent vendor/version or signature authority.

No executable exists at the admitted daemon location, and the offline registry has
no `NTKDaemonService` entry. No candidate daemon or comparative-port listener was
retained. Five unreadable process records make foreign-daemon exclusion incomplete.
The observer did not retain per-record error categories, so the cause of those five
gaps is unknown. **Do not promote this to a complete dependency-absence claim.**
There is no runtime Windows service query and no admitted readiness contract.

The retained failure snapshot belongs to operation
`354af73fea5773244ac3ebd21425e4ed`, not comparison B. Its terminal result, ledger
and runner diagnostics match their exact operation-owned sources. Vendor-log
permission/timeout/missing-object lines are separately hashed; their attribution
to an exact Windows process is unproved. Installer text inside a process-creation
record is only a reference, not proof that that installer executed.

## Preservation

The completed before/after check retained 11,489 unchanged environment metadata
entries, 307 retained content witnesses, five projects and 23 predecessor-generation
files. Software, application resources, products, publications, onboarding, A/B/C
records and the operation set were unchanged. Native Access remained closed.
Service active; two keepers; zero DSP/maintenance leases, pending transactions and
stale transports; cleanup unblocked; capture off. No Windows process was launched
and no real daemon, registry or production software was modified.

## Implementation boundary

The selected transition law is `identity_unresolved -> refuse`. This result does
not admit installation, service start, foreign-process adoption or a readiness
claim. The requested production dependency owner and generated service lifecycle
qualification are **not implemented**. PR #119 remains an incomplete draft; green
existing workflows must not be presented as NAD1 lifecycle proof.
