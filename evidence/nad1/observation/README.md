# NAD1 initial observer refusal

The committed source and seal are retained exactly. The first package transfer
contained macOS tar sidecars and refused at package validation, before managed
inputs were read. A clean fixed-file transfer of the same source then ran the
single authorized observer. It refused at an invalid attribution invariant before
bundled-installer, daemon, registry, process or listener census.

The defect was requiring that only one operation contain a textual bundled-installer
reference. Such a reference is not execution or failure authority; it can occur in
a helper/query argument and can legitimately occur in several operations. The
initial exception did not retain the matching count. An interim commentary that
said more than one source was found was too strong; the exact retained result is
only that the expected singleton check failed.

This is an incomplete observation and yields NAD1_IDENTITY_UNRESOLVED. It does not
establish current dependency absence, readiness or a selected production mutation.
The one-use marker remains untouched. No real Windows process, installer or daemon
was launched; the observer has no such command path. It did not reach its final
before/after checks, so no completed preservation receipt is claimed.

The corrected source keeps operation-owned references separate from shared vendor
errors and verifies the private snapshot's operation binding. Permission for one
corrected read-only continuation has been requested; it has not run. No production
transition is selected until the physical disposition is available.
