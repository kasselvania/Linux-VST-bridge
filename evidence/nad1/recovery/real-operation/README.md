# Existing NTKDaemon recovered and readiness established

The explicitly authorized one-shot operation
`26c5f1e3ff31a786c8646e3764d60262` completed successfully on installed merge
`a6ea9a57358dec1b188f1b02cf47c532e70182c3`, tree
`dca72e2076e23fd75f7a3dd0c190c8769fdea915`. Its immutable schema-2 spec selected
`recover_installed`. Native Access stayed closed and the installer did not execute.

The exact daemon left by the earlier installer operation was recovered using the
fixed bundle-payload qualification and unchanged historical private sources. The
first service query found the exact registration stopped. SCM start established a
running service; subsequent exact queries observed endpoint ownership progress
from mask 2 to mask 3. Readiness required both admitted loopback endpoints, the exact
Windows SCM process/image generation and one exact same-prefix Linux image generation
inside this operation's cgroup. Linux and Windows process identifiers were not joined.

One SCM stop followed. Exact service retirement and final process cleanup passed,
without forced cleanup. The unit and cgroup are absent; the bridge is active with
two keepers, zero leases/transactions/stale transports and capture off. The manager
retained a new preparation record bound to this successful result.

The earlier installer's exit 100 remains a failed historical result. This success
does not reinterpret it: the installed payload now has independent readiness proof.
The daemon bytes, installed software, all 307 retained witnesses, five projects,
both 23-file predecessor generations and prior operation evidence are unchanged.
Of the 98 pre-existing protected records, only the dependency current-operation
selection changed. The new operation and preparation receipt are separately retained.
The Wine registry file hash changed during the authorized service operation; its
exact service registration remains present. This is not a whole-prefix immutability
claim or an attribution of every ordinary runtime registry write.

The service was deliberately stopped at the end of this test. A later managed
Native Access launch must freshly start and verify it. Native Access connectivity,
login, networking and product installation have not been tested by this operation.
No new application launch follows automatically.

Automatic approval review initially refused service execution against the earlier
installation-only instruction. No request ran on those refusals. The operator then
explicitly authorized one real daemon recovery test; exactly one request was admitted.
