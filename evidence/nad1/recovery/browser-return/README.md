# Browser-return and application-lifetime repair

The human reached Native Instruments' successful web sign-in page, but Linux
had no `native-access:` handler. Application authentication was not established.
The preceding real operation and its clean retirement remain retained separately
under `../child-memory/real-application/`.

Source `31c261eea5784175faa5f36f6c8f28dfb9c2c8b0`, tree
`8709c769f69ad62cb1cb7aef6bc6bf54dc62def9`, removes the manager's separate
650-second application wait cap and adds an opaque browser return to the exact
live Native Access owner. Setup registers the scheme handler without replacing
a foreign association. No renderer, sandbox, runner or dependency policy changes.

The sealed generated Deck operation `d9d41bf074f2bb9df96cea79b91dcc1e` proved:

- the fixture service was ready before its application started;
- one fake browser return reached the exact retained Windows adapter;
- its verified secondary instance delivered the return to the primary fixture;
- the Linux owner received one dispatch acknowledgment;
- raw renderer and shared-runtime diagnostics stopped before delivery;
- the fake return did not occur in retained operation files;
- renderer process completeness was explicitly false after privacy cutoff;
- application exit, SCM stop and process cleanup succeeded without forced cleanup;
- the unit was absent and its disposable prefix was removed.

This establishes transport, not vendor authentication or account success. The
fixture's private named pipe is instrumentation, not an implemented vendor
protocol. Real Native Access continues to own its normal second-instance handling.

## Development and preservation

The first invocation refused before operation creation because Python generated
`__pycache__` inside the sealed package. Invocation with `-B` corrected that.
Two generated runs then found the initial pipe-peek design ineffective under the
pinned Wine runtime. The fixed numeric diagnostic in the second run identified
Windows error 50 with input type 2 (console). Both operations retired cleanly and
their scratch prefixes were later removed; their original private results remain.

The corrected reader handles console or pipe input with a bounded ASCII line,
disables console echo, and joins its input reader within a bounded retirement
wait. Normal application lifetime remains owned by the session, not a deadline.
Hosted Windows and the pinned Deck runtime both passed the corrected handoff.

The successful operation's final campaign comparison nevertheless refused:
removing the two earlier retired scratch prefixes during that run decreased the
link counts of 6,701 shared runtime-cache inodes by exactly two. This refusal is
preserved, not rewritten into an original campaign pass. Separate comparison of
the retained before/after data proved every runtime content hash unchanged,
unchanged inode/mode/size/mtime, and only the explained link-count/ctime changes.
Real Windows-prefix and private-home metadata, installed software, products,
publications, application history, 307 witnesses and five projects were unchanged.
`generated.json` states both the successful transport result and this preservation
exception. No replay was needed to preserve the useful completed result.

## Validation and limits

AP8 run 35286472521 and AP12 run 35286474513 passed at the executable source above.
AP12 includes manager/frontend tests, strict Clippy and Linux runtime tests. Local
manager tests passed (141 library, 80 binary before the additional association
regression); the seven setup/association tests then passed. The 235-test Python
suite passed locally with 44 Linux-only skips; hosted AP12 exercises Linux.
NAD1's 23 tests and NAUI1's 47 tests passed. Local proof-policy tests passed except
sandbox-denied native-client socket cases; those four test methods passed when
rerun with loopback permission. No hosted PX2 result is claimed for this branch.

The initial hosted AP12 run found a generated fixture missing its application ID;
the fixture now explicitly uses the source-owned identity. Its failed run remains
visible. No production admission check was weakened.

No real Native Access or daemon launch occurred for this repair's qualification.
The candidate is not independently reviewed or merged. Installation is separately
recorded if the operator approves it. A fresh human sign-in remains necessary to
observe actual Native Access authentication.
