# Browser-return repair candidate installation

The operator explicitly approved installation of executable source `31c261eea5784175faa5f36f6c8f28dfb9c2c8b0`, tree `8709c769f69ad62cb1cb7aef6bc6bf54dc62def9`, for testing before review. This remains an unmerged, unreviewed candidate. The Windows adapter came from exact-source AP8 run 35286472521; AP12 run 35286474513 also passed.

One immutable generation was installed through idle stop/setup/restart. The manager, supervisor and adapter changed; the verified frontend and host were reused. The previous generation remains retained. The actual desktop default now resolves `native-access:` to the manager's closed browser-return handler. No callback was delivered during installation.

The first post-restart readback found the service active with zero keepers because the installation helper omitted the existing keeper-resume step. The exact registered-environment owner protocol was then invoked once and confirmed ready. No second setup or software installation occurred. The final readback confirms two keepers, no leases, pending transactions, stale transports or resume obligation, and capture off. Manager/frontend model 7 agrees. A transient snapshot refresh refusal during keeper restoration was retried without an operator action.

The exact application record, installed daemon, registry hashes, 172 historical records, publication links, 307 retained witnesses, five projects, 143 protected files and all three retained 23-file predecessor sets passed preservation checks. Native Access was closed; no real dependency install/start/stop, account interaction, product action or DAW launch occurred.

The prior readiness receipt remains unchanged and belongs to the previous software generation. A fresh managed dependency readiness operation is required before the new generation may open Native Access. This receipt establishes installation and association only; actual browser-to-Native-Access authentication remains untested.
