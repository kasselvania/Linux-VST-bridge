# Shared-runtime repair candidate installation

The operator requested testing before review. Candidate `98c2e15b44a2d7cea9e2c368b95485ffc9b31c75`, tree `7dd5cd994230766d7f6fbd4b12763b095f9bbda4`, was installed immutably through the idle stop/setup/restart route. This is an unmerged test candidate, not a reviewed release. Executable source equals the AP8/AP12-qualified `8b4cb0435f2608449aaa34cf305acdd4f472320b` generation. PX2 passed locally; hosted PX2 remains unavailable pending PR creation.

The exact adapter came from AP8 run 35275212756. Manager/frontend model 7 agrees. The bridge is active with two keepers, zero leases, pending transactions or stale transports, capture off and no resume record. All 307 retained witnesses, five projects, prior software files and 143 protected records were unchanged. Native Access and the exact installed daemon were not launched. Registry content was unchanged across installation.

The initial preflight referenced registry bytes from before the last real application run and refused before installation. The corrected check bound the current registry metadata to the final generated campaign readback and captured its hash for exact pre/post comparison. No registry repair or write occurred.

The predecessor remains retained. The new software generation requires a fresh dependency preparation receipt; the historical preparation remains unchanged. Automatic approval review blocked that service transition pending explicit approval. Therefore this receipt claims installation only, not new-generation daemon readiness or successful Native Access launch.
