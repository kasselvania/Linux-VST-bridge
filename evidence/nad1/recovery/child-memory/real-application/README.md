# Real shared-runtime Native Access application confirmation

Human-launched operation `efe3c75c228f1247cb669c08c55e67c4` reached rendered Native Access login content under the installed shared-runtime candidate and software rendering. Fresh exact daemon readiness passed. The human completed a fresh browser sign-in after an earlier callback error; the browser reported logged in and Linux requested an application for the `native-access:` scheme. The Linux scheme registration is absent and the manager source contains no return handler. Native Access account-session completion is therefore not established.

The human closed the browser and Native Access normally. The application result completed with exit zero, confirmed exact SCM retirement, no forced cleanup and zero owned survivors. The exact unit/cgroup are absent.

Automatic bridge recovery did not complete: the manager waiting worker reached its separate 650-second `renderer_retirement_unconfirmed` limit during this long interactive session. The application remained alive until the human closed it. After positive application/service retirement, the existing exact `operator finish` recovery restored the bridge and cleared its resume record. The operator refusal remains unchanged. This is a distinct manager recovery defect, not an application or daemon crash.

Final readback confirms active bridge, two keepers, zero leases/transactions/stale transports, cleanup unblocked and capture off. All 307 retained witnesses, five projects, predecessor artifacts and checked historical records remain unchanged except the current application-operation pointer. Ordinary prefix/account-state immutability is not claimed after human sign-in. No login URL, authorization code, account identity, screenshot or password is copied into this evidence.

Next concrete work: exact browser return handling and removal of the manager waiting-worker normal-session deadline. No callback handler, alternate browser association, arbitrary argument route or new vendor launch was installed by this evidence step.
