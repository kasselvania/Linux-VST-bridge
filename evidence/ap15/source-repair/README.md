# AP15 source hardening, not fixture acceptance

This pass repairs the seven source-review items against `dd596b3e1031655e844d4ee7438d6c564f6db7e7`. The detailed ownership and callback-origin laws are in [docs/AP15.md](../../../docs/AP15.md#source-repair-checkpoint--windows-capacity-deferred).

- Linux-native command refusal, bounded retirement, timer removal, stale-generation and Xvfb fault fixtures exercise production helpers.
- Manager tests preserve the existing transaction-boundary suite while refusing unmarked retained candidates, preserving exact verified serving, and keeping the compiled qualification roster absent.
- GUI ABI 4 / mapped UI 5 distinguishes editor-originated callbacks; audio IPC/state envelopes are unchanged.
- Windows DestroyWindow/handler-generation tests are source only: **not compiled or run**.
- No candidate Windows artifact, complete candidate profile, candidate Bitwig qualification, AP15 publication, support or merge readiness is claimed.
- The existing CI billing/capacity evidence is retained in the parent directory. No manual workflow retries were requested in this pass.
- Both registered Linux products are built only into task/build caches. The installed revision-3 targets remain unchanged at 512 frames; 256 is unqualified.

Prior AP15 evidence is retained verbatim. Validation, artifact and before/after physical receipts for this repair are added here after their actual commands complete. No vendor binaries, state, presets, projects, credentials or raw audio are included.
