# AP15 exact Arturia qualification

- `windows.json`: original hosted artifact, real synthetic build commit and common reviewed tree.
- `attachment-rejection.json`: actual revision-4 1×1/absent-child rejection; `attachment-native-regression.json` is failing-before/passing-after mechanics.
- `attachment-artifacts.json`: immutable revision-5 native inputs and outputs.
- `focus-refusal.json`: actual revision-5 reopen foreground refusal; `focus-native-regression.json` retains the narrow zero-timestamp red/green test.
- `focus-artifacts.json`: exact revision-6 Linux build receipts.
- `campaign.json`: normal Applications launches, same-session lifecycle, gestures, existing automation, saved-project recall, sibling/removal result and local output metrics. No raw audio or opaque state is retained.
- `lifecycle-and-cleanup.json`: production native/SDK identity, timing, gap and positive cleanup records for both campaign sessions.
- `installed-final.json`: sealed profile/publication fingerprints and transactions, exact restored revision-3 targets and parents, unchanged original projects and final service/keeper/lease/CPUWeight state.
- `validation.json`: exact commands, local results, source-head hosted runs, and package hashes. Final PR-head CI readback is in the PR body so evidence-only commits do not create a self-referential receipt.

Both candidate profiles remain ReviewCandidates. Ordinary publication stays verified-only; both reviewed revision-3 publications are restored. This is bounded SteamOS 3.8.16 / Bitwig 6.1 evidence with the exact installed Arturia artifacts, not universal compatibility or a performance result. 512 remains supported/recommended; 256 remains unqualified. Independent review and merge are separate.
