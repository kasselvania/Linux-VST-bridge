# NAUI2 review repair

This amendment closes exact installed Python admission, uncertain systemd submission,
manager recovery and public diagnostic boundaries. The accepted initial campaign in
`../generated.json` and its original source seal remain unchanged.

Executed source: `ed33af9875ddacd163fe2870af5338101dae67d3`.
Tree: `54ecf30e8c79776c4e745d4156eee3b63cf7e67c`.
Sealed manifest: `46685f4627b88ae373139e308b37f4cf535fb23ec3f12505ace145ec7d708f13`.

## Production repair

The installed Python entry independently verifies the exact managed Native Access
environment/root, NAUI1 provenance, complete resource paths/hashes/sizes, retained
installation result, current software record and exact operation reservation/report.
Invalid admission never constructs the image cache, takes the prefix lock, launches
or writes to a supplied report. The sealed source-owned fixture enters a separate
internal owner with `naui2-source-owned` identity; it cannot enter the installed path.

The Rust lifecycle retains the reservation and resume owner after a lost acknowledgment.
An unavailable unit state cannot restore the bridge service. A shared exclusive
writer gate plus exact absent unit/cgroup permits a non-replacing terminal refusal;
a late writer refuses that terminal record. Loaded inactive units require explicit
Stop to cancel any queued job. Interrupted writer evidence is preserved in place,
with a separate recovery result. Source-generated operator tests exercise exact
request/resume ownership, wrong Stop, uncertain state, interruption, late results,
Focus refusal and one-time restoration. They substitute system/service I/O and do
not claim to have restarted the real bridge service.

Bounded ANGLE/D3D/OpenGL initialization facts are retained without selecting a remedy.
Public launch bindings omit Windows PIDs and creation times. Their exact private
counterparts remain in the operation ledgers and are hash-bound in `cleanup.json`.

## New generated campaign

All four sessions use the production Rust reservation/submission core. Cancellation
uses its exact Stop implementation rather than direct campaign `systemctl stop`.

| Case | Operation | Result |
| --- | --- | --- |
| Inherited | `7b2ab6e53520aabe4fa85e5ba1b46d35` | GPU child exit 5; outer 0 |
| Software rendering | `86d8e66f6435ab7a6cb2cd0da09f9095` | Role children exit 0; outer 0 |
| Restored inherited | `c7f8ba10bb5efae8a726efcd9394bca9` | GPU child exit 5; outer 0 |
| Exact Stop after failure | `aae6d6d9191d85d1c2919f94db3fb605` | Cancelled; prior GPU failure preserved |

Every admitted session retained one bound root, complete Windows trace, positive
cleanup and zero survivors. All units/cgroups and disposable prefixes were removed.
The 67 MiB fixture is process instrumentation, not Chromium rendering evidence.
Real GUI Focus and all Native Access presentation/causality remain unproved.

One earlier campaign failed before reservation because the source-owned driver had
not set a private umask. No prefix was initialized and no Windows process launched.
Its seal, refusal and scratch cleanup are separately retained. A Linux unit-suite
staging omission was also retained and corrected; only its failing test was repeated.

## Preservation and validation

Service active, two keepers, zero DSP/maintenance leases, pending transactions and
stale transports; cleanup unblocked and capture off. Installed software, products,
publications, imports/environments/history, 307 retained witnesses and five protected
projects compare exactly before/after. The earlier sealed package still verifies.
NAUI1 was not rerun physically. No vendor, DAW, installer, registry, product or
installation operation occurred.

`validation.json` retains focused suite counts and development errors. Final-head
AP8/AP12/PX2 are linked in PR #117. Keep this PR draft, uninstalled and unmerged for
independent review.
