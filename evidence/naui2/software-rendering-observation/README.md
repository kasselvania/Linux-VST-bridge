# Software-rendering Native Access observation B

The tech lead accepted inherited operation A and authorized exactly one human
software-rendering launch. The agent checked idle state and identities, then the
human selected the installed manager's software-rendering action. No agent
RendererOpen, Focus or vendor input occurred.

Application operation: `627e52ca72b7533a5774862da7df11cb`.
Human presentation: `rendered_nonblank` (the operator reported visible content).
Observation request: `a503686cb5cdf7f8c286fca74edb3016`.
The human closed the application normally; the observation was recorded once after
retirement through the exact offered RendererObserve action.

## Result

Requested and effective policy are `software_rendering`, mapping to exactly
`--disable-gpu` under the accepted installed adapter contract. The executable is
225,757,168 bytes with SHA-256
`7b2413e90db79538cd1edc7c6169ff384aea181a85635c43a363f4aa18b9e841`.
The exact root is bound at ordinal 7. Application identity, software generation
and executable match A. Windows, line and fact drop counts are zero; the retained
parser reports complete evidence. No causal diagnostic facts were admitted and
cause remains unresolved. Unknown child roles and unavailable exit observations
remain unknown; zero drops do not supply missing role authority.

A produced `blank_white`; B produced `rendered_nonblank`. This is a **provisional
software-rendering effect**, not a final graphics diagnosis or selected permanent
remedy. Restored inherited comparison C has not been run and remains separately
authorized after review. Login, network behavior, authorization and application
usability are unproved. The historical partial-installation classification is not
changed. No sandbox flag, registry change, runtime installation or runner change
was made by the agent.

## Cleanup and preservation

State completed, outer exit 0, no cancellation, no error, positive cleanup and zero
owned survivors. The exact unit and cgroup are absent. Service active, two keepers,
zero DSP and maintenance leases, transactions and stale transports; capture off;
no pending resume record. Operator schema remains 6.

Installed artifacts and registered application artifacts verify. The 307 retained
witnesses, five projects, 23 predecessor-generation files, product/publication
facts, imports, environments and onboarding histories match the prior witnesses.
This does not claim that application launch created no ordinary cache/log writes.
A's result and human presentation hashes remain unchanged. Only A and B are present
in the managed renderer operation directory. No restored control was launched.

The commit adds evidence and this bounded task update only. No production source,
installed software, original generated campaign or A evidence is modified. Physical
readback and JSON validation are the applicable checks; no source-test or Windows
fixture replay was needed for this evidence-only commit.
