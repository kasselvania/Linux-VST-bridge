# Observed inherited Native Access run

The human ran Native Access through the installed manager's inherited rendering
mode, reported a white window, and closed it. The agent did not submit RendererOpen.
The later human report was recorded once through the exact offered RendererObserve
operation as `blank_white`; recording it did not relaunch the application.

Application operation: `c44ae35d794f0a1f47ee97468e848a1c`.
Observation request: `1da0d60b88d5d70eef8d00c9e4904fc5`.

The retained result binds the exact Native Access executable and application
identity to one Windows root. Requested/effective mode is inherited. Windows, line
and fact drop counters are zero; the retained classifier reports complete parsing.
The role census is mostly unknown and there are no decisive renderer facts. Cause
remains unresolved; zero drops do not establish missing role or exit authority.
Unidentified child exits of 1 are not assigned to a GPU, sandbox or vendor cause.

Outer exit is 0, state completed, no manager cancellation, positive cleanup and
zero owned survivors. The exact unit is absent. Service restoration completed:
service active, two keepers, zero DSP/maintenance leases, transactions and stale
transports, capture off, no pending resume record. Installed software, all 307
retained files, five projects, 23 predecessor files and publication links match.

The white presentation is attributed human evidence, not a machine rendering
measurement. No account, networking, authorization or usability result is claimed.
No software-rendering test or new agent-launched application operation occurred.
This inherited result is ready for review; a software-rendering launch remains a
separate authorization. Existing installation and generated receipts are unchanged.
