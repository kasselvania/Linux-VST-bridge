# MF3 immutable-generation source repair

The seven independent-review findings are repaired without installing software,
changing a publication, running Serum or repeating the source-owned live build.
The original MF3 result and SV1 session records remain byte-for-byte unchanged.

Candidates and inspections are separate immutable histories. Exact actions bind
candidate, inspection, recipe and expected-current publication identities. SV1
adoption retains original authority and copied provenance rather than treating its
legacy record as a preparation kit. Ordinary acceptance follows policy A: later
failure evidence blocks new acceptance, while an existing sealed ordinary
publication remains admitted until explicitly withdrawn. Processing-restart
failure, untested normal retirement and positive terminal cleanup remain distinct.

The schema-2 kit owns the actual builder and descriptor-generator bytes. Mixed
or legacy unbound generations cannot use the current embedded implementation.

Local validation: 118 manager-library tests, 57 binary tests, 14 frontend tests,
7 recipe tests and strict manager/frontend Clippy passed. Runtime tests ran 70
cases with 36 Linux-only cases skipped on macOS; Linux runtime coverage belongs
to AP12. Final AP8/AP12/PX2 status is attached to the amended PR head.

These are generated source checks, not a new product result. The repair remains
uninstalled pending independent rereview; PR #109 stays draft and unmerged.
