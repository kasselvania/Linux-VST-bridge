# NAO1 generated Native Access owned-session qualification

Executable source: `4462e168462f88a3189202188b331d630f0eceb4`  
Tree: `f21f81e3f4a6d947dce83386ae45cb44bdbf4be7`  
Package seal: `579fe3bfe12b861d668551094f11a07f096a1f6fc276efd911cdd7b8375cf203`  
Selected authority: `CURRENT_SLICE.md` and `docs/NAO1.md` at
`b34c63013290b50bf2cc771566a509290802b97c`.

The Native Access application launch path no longer depends on a historical
successful `prepared.json`. Rust binds a closed session-admission record to the
exact application, installed manager generation, retained installation artifact,
bundled installer, daemon image, managed environment, physical prefix identity,
and immutable origin. The production Python renderer independently validates the
same record before establishing fresh operation-owned daemon readiness. The strict
standalone dependency-preparation result remains unchanged and is not created or
updated by an application session.

The terminal result distinguishes `graceful_service_retirement`,
`exact_owned_session_cleanup`, and `cleanup_unconfirmed`. Exact-owned cleanup is
accepted only after an available NAD2 observation, no second stop request, exact
owned-cohort cleanup, empty renderer cgroup, absent exact daemon generation, absent
owned listeners, no foreign or unresolved matching candidate, unchanged admitted
images, and normal bridge/keeper recovery. It remains explicitly unconfirmed SCM
retirement and sets `forced_cleanup_used=true`.

## Generated campaign

The sealed campaign used only source-owned Windows fixtures in disposable prefixes.
It crossed the production Python renderer, dependency owner, terminal handling, and
NAD2 parser. The public receipt is `proof.json`; private operation specifications,
prefixes, process identities, and raw fixture output are not published.

| Case | Terminal state | Dependency result |
| --- | --- | --- |
| `normal` | completed | graceful retirement confirmed |
| `repeat` | completed | a second fresh generated session succeeds |
| `cancel` | cancelled | cancellation remains cancellation |
| `not_ready` | failed before application launch | cleanup unconfirmed |
| `stop_failure` | failed | application/operation failure is not rescued by exact-owned cleanup |
| `application_failure` | failed | application failure remains failure after graceful cleanup |
| `lifetime` | completed | unlimited production lifetime law remains intact |
| `child_memory` | completed | child-memory ownership remains intact |
| `callback` | completed | browser-return privacy remains intact |
| `no_transition_cleanup` | completed | `NAD2_STOP_SUBMITTED_NO_TRANSITION`; exact-owned cleanup |
| `not_submitted_cleanup` | completed | `NAD2_STOP_NOT_SUBMITTED`; exact-owned cleanup |
| `stopped_residue_cleanup` | completed | residue removed and exactly reverified before completion |
| `observation_unavailable_cleanup` | failed | unavailable observation cannot qualify cleanup |

All completed compatibility-cleanup cases retained
`service_retirement_confirmed=false`, `process_cleanup_confirmed=true`, and
`forced_cleanup_used=true`, with an empty cgroup, zero listener mask, zero remaining
matching candidates, and unchanged admitted images. Every session used at most one
stop attempt. No session created a dependency-preparation receipt.

Rust and Python unit coverage supplies the remaining admission/refusal matrix:
changed or missing daemon and installer bytes; wrong application, software,
artifact, environment, prefix, and origin; aliases and malformed records; foreign,
deleted-prefix, same-prefix unowned, ambiguous, and unavailable candidates;
post-cleanup process/listener/candidate residue; result parsing; public/private
projection; frontend presentation; continuous output draining; and ordinary Arturia
completion.

## Validation

Local qualification passed 24 NAD1/NAD2 adapter tests, 47 NAUI1 tests, three NAUI2
tests, 259 runtime tests with 44 platform skips, 145 manager-library tests, 86
manager-binary tests, five manager report tests, and 32 frontend tests. Both strict
Clippy checks, Python compilation, the focused production C++ stop-law test, and
Windows x86-64 cross-compilation of the production adapter and source-owned fixtures
passed.

Exact executable-source hosted validation:

- [AP8](https://github.com/kasselvania/Linux-VST-bridge/actions/runs/35365247459) — passed at `4462e168462f88a3189202188b331d630f0eceb4`
- [AP12](https://github.com/kasselvania/Linux-VST-bridge/actions/runs/35365247485) — passed at `4462e168462f88a3189202188b331d630f0eceb4`
- [PX2](https://github.com/kasselvania/Linux-VST-bridge/actions/runs/35365247562) — passed at `4462e168462f88a3189202188b331d630f0eceb4`

The first archive transfer was refused by the seal preflight because macOS
AppleDouble files were present. It performed no fixture operation. A clean archive
with the same exact source was then sealed and run once; the refusal is not relabelled
as qualification evidence.

## Boundaries and preservation

All 693 evidence files tracked at the starting head are unchanged; their exact Git
manifest digest is in `preservation.json`. The campaign observed five protected
projects, 307 retained witnesses, unchanged protected/runtime/prefix/private-home
content, an active bridge, two healthy keepers, zero leases/transactions/stale
transports, and capture off after completion.

The candidate was not installed. No real Native Access application, NTKDaemon,
DAW, plug-in, installer, updater, or production operation was started. Generated
evidence does not qualify reliable graceful NTKDaemon shutdown. The next action,
only after source approval and separate authorization, is one installation and one
real Native Access session.
