# Delayed-registration ownership repair

Executable source `e3a06c3ebf9a94e0f18e1deeefa59410dd04366f` closes the
remaining delayed-registration ownership race without adding a service-registration
or shutdown strategy.

After the single delayed SCM re-observation reports exact registration, the production
owner revalidates the physical prefix and current daemon image and performs one fresh
bounded process census before granting retirement authority. A running or start-pending
service requires one exact same-prefix generation already owned by the renderer cohort.
A stopped service requires an empty census before the operation's single owned start.
Unavailable, ambiguous, foreign, deleted-prefix, same-prefix unowned, changed-prefix,
and changed-image observations refuse before application launch and retain zero Stop
requests.

The focused regressions cover an unowned generation appearing during the one-second
delay, an unavailable fresh census, ambiguous/foreign/deleted candidates, an owned
running or start-pending generation, exact stopped registration followed by one owned
start, and physical-prefix replacement. The sealed generated campaign retained all 15
recovery-origin sessions and explicitly checked the stopped delayed handoff's fresh
empty census before its owned start.

No candidate was installed. No second real Open action, commercial Native Access
launch, real NTKDaemon transition, installer replay, DAW, plug-in, update, or production
operation occurred. Reliable graceful NTKDaemon shutdown remains unqualified.
